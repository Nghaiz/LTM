verdict-closure R1 task R1.2 -- check 6 (E12) mutation proof
LaneBCheckpointRecorder.presentersWithNoBootstrap / -Count

E12's pass condition is the EMPTINESS of a set, and an empty set is exactly the shape
"green that proves nothing" takes: a recorder that stopped writing the field, a prefix
that no longer matched, or a guard that never wrote to the set at all would all render
identically to a healthy zero. So the subject was broken deliberately and the field was
observed carrying the wrong answer.

MUTANT  NetClientPresenterGuard.TryResolveClient returns the not-found branch
        unconditionally -- `if (false) return true;` -- so every presenter reports
        having found no bootstrap on a client start that is in fact healthy.

RUN     artifacts/lane-b/r1-e12-mutant  (smoke set, three clients, spawn pinned to 0)

RED, on all three clients, at the checkpoint:

  driver      count=6  ['ClientSeatRequester', 'NetClientLocalCombatDriver',
                        'NetClientObjectivePresenter', 'NetClientCombatPresenter',
                        'NetClientExplosionPresenter', 'NetClientProjectilePresenter']
  observer-a  count=6  (same six)
  observer-b  count=6  (same six)

CORROBORATION  `grep -c "found no NetClientBootstrap" driver.log` = 6. The artifact's
        count equals the log's line count, which is what the field claims to summarise.
        That equality is the point of reading the same set the warning writes to: a
        separate counter could have said 6 while the log said something else, and the
        artifact is what gets quoted in a verdict.

GREEN, unmutated  artifacts/lane-b/r1-grenade-03 -- count=0 and an empty name list on
        all three clients at all five checkpoints.

So B-6's PASS is a reading that can fail, and was seen failing.
