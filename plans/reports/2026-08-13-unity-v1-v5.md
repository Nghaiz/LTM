# Unity V1-V5 verification — 2026-08-13

## Environment

- Branch base: `develop` at `836b056`
- Unity: `6000.3.21f1 (c02631ffc030)`
- Platform: Windows Editor
- Unity-MCP: installed during the measurement
- Library refresh: `tools/build-libs.ps1 -Configuration Release`

## Results

| Gate | Result | Evidence |
|---|---|---|
| V1 | **0 errors** | All three `Ironfront.Net.*` projects built with 0 errors/0 warnings. Unity imported and compiled the project successfully; no `GUI Layer`, `TypeLoadException`, or C# compiler error was emitted. |
| V2 | **Census clean** | Every watched assembly produced exactly one census line and no `is loaded 2 times` diagnostic was emitted. |
| V3 | **Log written** | `ironfront-20260813-145338.log` existed and was non-empty, a marker was readable while Play Mode was active, and the file ended with `session ended`. |
| V4 | **Play Mode stops; quit log present** | Both the `Menu` scene and `Ingame UI Container` prefab have a non-null persistent `OnClick -> Quit` target. Invoking the Menu Quit button emitted `[AppQuit] quit requested` and returned the Editor to Edit Mode. |
| V5 | **Clean x3** | `Island.unity`, `Dustbowl.unity`, and `Splash.unity` opened without an error. `Menu.unity` was also reopened clean before Play Mode. |

## Assembly census

```text
assembly Ironfront.Net.Protocol: 1.0.0.0 from Assets/Plugins/Ironfront.Net.Protocol.dll
assembly Ironfront.Net.Replication: 1.0.0.0 from Assets/Plugins/Ironfront.Net.Replication.dll
assembly Ironfront.Net.Transport: 1.0.0.0 from Assets/Plugins/Ironfront.Net.Transport.dll
assembly System.Memory: 4.0.99.0 from Editor/Data/MonoBleedingEdge/lib/mono/unityjit-win32/Facades/System.Memory.dll
assembly System.Buffers: 4.0.99.0 from Editor/Data/MonoBleedingEdge/lib/mono/unityjit-win32/Facades/System.Buffers.dll
assembly System.Numerics.Vectors: 4.0.0.0 from Editor/Data/MonoBleedingEdge/lib/mono/unityjit-win32/System.Numerics.Vectors.dll
assembly System.Runtime.CompilerServices.Unsafe: 6.0.0.0 from Assets/Plugins/System.Runtime.CompilerServices.Unsafe.dll
```

The absolute user profile and checkout paths were shortened above; versions and assembly origins
are unchanged. Unity cloud connectivity produced `Curl error 7` after the verification had passed,
and Unity-MCP logged shutdown warnings. Neither was a scene, script compilation, assembly census,
logger, or Quit-path failure.

## Repository effects

Running `build-libs.ps1` refreshed the three committed Unity plugin DLLs so they match the current
`develop` source. Opening `Dustbowl` also exposed that `TerrainData.asset` is binary while the generic
`*.asset` attribute treats it as YAML; `.gitattributes` now overrides that specific file as `binary`
so a byte-identical Editor round trip is not reported as a source change.
