X-18 — mutation proof for ServerTickLoop.IsAnnounceable
========================================================
2026-08-22. Unity 6000.3.21f1 EditMode, filter
Ironfront.Net.Unity.Server.Tests.AnnounceableActorTests.

Per mutate-every-gate: a test is unverified until the real artifact has been broken and
the test seen going red, once per fault it claims to catch. Two faults are claimed here,
in opposite directions, so there are two mutants.


THE PREDICATE
-------------
    internal static bool IsAnnounceable(NetServerActor actor)
        => actor != null && !(actor.AvailableForPlayers && !actor.IsClaimed);


BASELINE — artifacts/x18-tests.xml
----------------------------------
    total="4" passed="4" failed="0"


MUTANT 1 — drop the slot-pool qualifier
---------------------------------------
    => actor != null && actor.IsClaimed;

Reads as "skip anything unclaimed" rather than "skip unclaimed PLAYER SLOTS". Bots are
never claimed, so this hides all fifty-five of them on Dustbowl. This is the failure the
fix could most plausibly have shipped with, because it looks simpler and passes the
X-18 case.

    artifacts/x18-mutant1.xml   total="4" passed="3" failed="1"

    Failed  ABotIsAnnouncedThoughItIsNeverClaimed      <-- the case written for it
    Passed  AClaimedPlayerSlotIsAnnounced
    Passed  ADestroyedActorIsNotAnnounced
    Passed  AnUnclaimedPlayerSlotIsNotAnnounced

Note which test did NOT fire: AnUnclaimedPlayerSlotIsNotAnnounced still passes. A suite
that only pinned the skip would have graded this mutant green.


MUTANT 2 — the pre-X-18 behaviour
---------------------------------
    => actor != null;

Announce everything, which is what the code did before this change.

    artifacts/x18-mutant2.xml   total="4" passed="3" failed="1"

    Passed  ABotIsAnnouncedThoughItIsNeverClaimed
    Passed  AClaimedPlayerSlotIsAnnounced
    Passed  ADestroyedActorIsNotAnnounced
    Failed  AnUnclaimedPlayerSlotIsNotAnnounced        <-- the case written for it


VERDICT
-------
Two faults claimed, two mutants, two reds, each caught by the test written for it and
by no other. Neither mutant was caught by both tests, which is what makes the pair
worth having rather than one assertion twice.

Predicate restored and re-run green before commit.
