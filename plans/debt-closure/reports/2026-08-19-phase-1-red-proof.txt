[client-wiring] KNOWN GAP - ClientMessageRouter.OnPlayerList has no production subscriber. V3 ships the codec and the router case; the name table has no presenter until the client-flow phase that also adopts ClientCombatState and ScoreUi.
[client-wiring] 14 of 15 ClientMessageRouter events have a production subscriber and the rest are named gaps above; G2-G5 clean across 406 file(s). No types were resolved - this says something subscribes, not that it renders correctly.
[asset-wiring] FAIL - 13 finding(s) across 7 check(s):
  [A1] NetClientProjectilePresenter is on no GameObject in this scene (ledger A-1). The script compiles and subscribes its event in code, and the delegate is never reached because nothing instantiates it. Add it to the object carrying NetClientBootstrap (&629676520).
  [A1] NetClientExplosionPresenter is on no GameObject in this scene (ledger A-7). The script compiles and subscribes its event in code, and the delegate is never reached because nothing instantiates it. Add it to the object carrying NetClientBootstrap (&629676520).
  [A1] NetClientCombatPresenter is on no GameObject in this scene (ledger X-1). The script compiles and subscribes its event in code, and the delegate is never reached because nothing instantiates it. Add it to the object carrying NetClientBootstrap (&629676520).
  [A1] NetClientObjectivePresenter is on no GameObject in this scene (ledger X-1). The script compiles and subscribes its event in code, and the delegate is never reached because nothing instantiates it. Add it to the object carrying NetClientBootstrap (&629676520).
  [A1] CosmeticTracerPool is on no GameObject in this scene (ledger A-5). The script compiles and subscribes its event in code, and the delegate is never reached because nothing instantiates it. Add it to the object carrying NetClientBootstrap (&629676520).
  [A1] ClientTurretDirectory is on no GameObject in this scene (ledger X-1). The script compiles and subscribes its event in code, and the delegate is never reached because nothing instantiates it. Add it to the object carrying NetClientBootstrap (&629676520).
  [A2] NetClientProjectilePresenter is on no GameObject anywhere, so _prefabsByKind has zero authored entries where 7 are owed (ledger A-1, X-1).
  [A3] ProjectileCatalogInstaller is on no GameObject anywhere, so _prefabsByKind has zero authored entries where 7 are owed (ledger X-2, X-1).
  [A3] ProjectileCatalogInstaller is on no GameObject in this scene, which runs a server (ledger X-2). The server steps no projectiles and announces no launches — degraded rather than broken, and silent.
  [A4] NetClientExplosionPresenter is on no GameObject anywhere, so _effectsByKind has zero authored entries where E6 requires two (ledger A-7, X-1).
  [A5] the remote-actor prefab carries no RemoteActorView, so stance, aim and death animate nothing and the muzzle has no origin (ledger A-2, E1).
  [A6] CosmeticTracerPool is on no GameObject anywhere, so _tracerPrefab is unassignable by construction (ledger A-5, X-1).
  [A7] LobbyShellOverlay is in no scene, so the lobby shell never runs and the master link it drives is never opened from the UI (ledger X-5).

  These are authoring gaps, not code faults. Fix them in the Editor and save; hand-editing the YAML risks a fileID this gate cannot tell from a correct one.
