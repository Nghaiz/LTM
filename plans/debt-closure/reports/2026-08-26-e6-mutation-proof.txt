E-6 / G9 mutation proof — FindUnpinnedLevelBoundsCall
=====================================================

Date       2026-08-26
Phase      debt-closure phase 6, task 6.2
Rule       G9, tools/ClientWiringGate/ClientWiringDetectors.cs
           (wired at GateRunner.cs in the per-file scan loop; completeness guard in Program.cs)
Command    dotnet run --project tools/ClientWiringGate
Exit codes 0 clean · 1 the gate found something · 2 the gate could not tell

Why a gate and not a test
-------------------------
Both `Vehicle` and `LevelBounds` compile into Assembly-CSharp, which no test assembly can
reference (E-11b) — the same wall G7 and G8 were built against. The ARITHMETIC is testable and
was moved out to `Ironfront.Net.Replication.World.PlayVolume`, covered by `PlayVolumeTests`
(8 tests). What no test can reach is whether the production call still exists, which is precisely
the fault E-6 records: `LevelBounds.IsInside` shipped with zero callers.

Baseline
--------
  [client-wiring] 15 of 15 ClientMessageRouter events have a production subscriber;
  G2-G5 and G7-G9 clean across 431 file(s).
  exit 0

Mutations
---------

A — Vehicle.cs: the `LevelBounds.ClampInside` call removed
    RED, exit 1, at Vehicle.cs:301
      [G9] KeepInsideLevelBounds calls no LevelBounds.ClampInside. Without it a vehicle leaving
      the play area keeps being simulated at a position the snapshot encoder clamps, so every
      client sees it pinned to the boundary forever and nothing is logged (ledger E-6).

    This is the exact state the tree was in before this task, for the whole of V5.

B — Vehicle.cs: the `NetContext.IsServer` guard removed
    RED, exit 1, at Vehicle.cs:305
      [G9] LevelBounds.ClampInside runs with no server-role guard above it. The clamp writes a
      rigidbody position; on a client it fights the snapshot corrections arriving for the same
      body, which presents as the rubber-band this rule exists to prevent (ledger E-6).

    A separate clause because a separate edit reaches it, and clause 1 stays green through it —
    the call is still there, it is just running on the wrong role.

C — Vehicle.cs: `KeepInsideLevelBounds` renamed
    RED, exit 1, method-level
      [G9] No 'KeepInsideLevelBounds' method. ... if this method was renamed, move G9 with it in
      the same commit rather than letting the boundary go ungraded (ledger E-6).

D — ClientWiringDetectors.cs: LevelBoundsScope re-pointed at a file that does not exist
    RED, exit **2** (could not tell — NOT a finding)
      [client-wiring] FAIL - a full-tree run discovered no file G9 grades, so the LevelBounds
      clamp went unchecked. Either Vehicle.cs moved out of the scanned roots, or it was renamed;
      re-point LevelBoundsScope in the same commit (ledger E-6).

    G9 is an absence rule inside a per-file loop, so it is SILENT on a file it never scans. A
    full-tree run that missed Vehicle.cs would otherwise report the boundary clean having graded
    nothing — the same hole Program.cs already guards for G8 (green-that-proves-nothing.md).

Result
------
  4 mutations / 4 REDs / 0 false greens.
  A, B and C land on distinct locations (301, 305, method-level), so a fix to one cannot mask
  another. D lands on exit 2, not 1.
  Vehicle.cs and ClientWiringDetectors.cs restored after every run.

Unity compile
-------------
  Neither file is visible to `dotnet build` — Assembly-CSharp is not in Ironfront.sln. Verified
  in the live Editor instead: tools/build-libs.ps1 (PlayVolume reaches Unity only as a rebuilt
  Ironfront.Net.Replication.dll), then a forced AssetDatabase refresh. Assembly-CSharp recompiled
  at 08:06:37 with pre-existing warnings only and zero errors.

  The first refresh, BEFORE build-libs.ps1, reported
    LevelBounds.cs(45,10): error CS0246: The type or namespace name 'PlayVolume' could not be
    found
  which is worth recording: a green `dotnet test` says nothing about whether Unity can compile
  against a library change.
