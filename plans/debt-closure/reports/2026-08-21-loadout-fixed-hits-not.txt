=== The player body is armed. Thirty rounds go out. Nobody is hit. ===
when: 2026-08-21T16:55:54Z   run: artifacts/lane-b/loadout-01 (combat set, sim=off)
three clients, 7 checkpoints each, exit 0, Unity build clean

BEFORE (artifacts/lane-b/respawn-02): weaponId 0, ammo 0/0 at every checkpoint.
AFTER:

checkpoint         weaponId  ammo    buttons  predictedShots
spawned            1         30/30    0        0
approach           1         30/30    0        0
in-range           1         30/30    2        0
firing             1         30/30    3        0
killed             1         0/30    0        0
victim-input-held  1         0/30    0        0
respawn-window     1         30/30    0        0

Read the ammo column, not the shots column. It goes 30/30 -> 0/30 -> 30/30: the SERVER
emptied the clip across the firing window and reloaded afterwards, and the client learned
both from the snapshot. buttons=3 at 'firing' is Fire|Aim, so the intent was produced,
packed and delivered. Both halves of the loadout gap are closed:
  server -- ServerCombatBridge.PlaceAtSpawn now calls Actor.EquipLoadout through a new
            IGameplayActorSource seam. Loadout only: no Actor.SpawnAt, no EnableInput.
  client -- X-11: NetClientLocalCombatDriver subscribes to OnSpawnActor and calls
            ClientCombatState.EquipWeapon. weaponId 1 and clipSize 30 are that.

AND THE NEXT LAYER, MEASURED RATHER THAN GUESSED: the victim finishes on 100 health.
Thirty rounds at six metres against a target the programme is aiming at, and zero damage.
That is check 1's subject and it is now the blocker.

Two separate things are visible here and they must not be conflated:
  * predictedShots stays 0 because ClientCombatState.PredictFire has ZERO production
    callers in the Unity client -- the identical shape to X-11's EquipWeapon, one method
    over. That is a client HUD/feel gap. It does NOT explain the missing damage, because
    firing is server-authoritative and the server plainly fired.
  * the missing damage is server-side hit resolution, and nothing in this run says which
    part: aim geometry, ShotOrigin, or BuildTargets. The server writes no per-shot
    rejection or per-shot hit line, so the next step is a measurement, not a fix.

--- victim, all seven checkpoints ---
  spawned            hp=100 alive=True
  approach           hp=100 alive=True
  in-range           hp=100 alive=True
  firing             hp=100 alive=True
  killed             hp=100 alive=True
  victim-input-held  hp=100 alive=True
  respawn-window     hp=100 alive=True
