=== D1/D2 landed, and the run that shows what is still in the way ===
when: 2026-08-21T16:49:46Z   run: artifacts/lane-b/respawn-02 (combat set, sim=off)
three clients, 7 checkpoints each, all exit 0, Unity build clean, zero NullReferenceExceptions

checkpoint         hp  weaponId  ammo   predictedShots  hitmarkerHits  kills
spawned            100 0         0/0     0               0              0
approach           100 0         0/0     0               0              0
in-range           100 0         0/0     0               0              0
firing             100 0         0/0     0               0              0
killed             100 0         0/0     0               0              0
victim-input-held  100 0         0/0     0               0              0
respawn-window     100 0         0/0     0               0              0

The driver aims at OBS-A, closes to 6 m and holds Fire for 8 seconds. Nothing happens:
no weapon, no ammo, no predicted shot, no hit, no kill -- and OBS-A finishes on 100 health.

So the scripted respawn added to combat-observer-a's respawn-window step was never reached:
the victim never died. D1's seam is pinned at unit level (ScriptedInputCursor.TryConsumeRespawn)
and its wiring by source assertion; it has NO run-level proof yet, and this is why.

This is not new, it was PREDICTED. debt-ledger X-11: 'Not the cause of combat-roster01's
weaponId: 0 (that is X-12 ...), but it will be the cause of the next one if X-12 is fixed
alone.' X-12 is fixed. This is the next one, and it has two halves:

  server -- ServerCombatBridge.PlaceAtSpawn's own remarks: MoveToSpawnPoint teleports and
            does not run Actor.SpawnAt, so SpawnLoadoutWeapons never runs and actor.WeaponId
            stays 0 for a claimed body. Left deliberately: 'Fixing the position without
            claiming to have fixed the loadout.'
  client -- X-11: ClientCombatState.EquipWeapon has 20 call sites and every one is a test.

CAVEAT ON THE SERVER HALF: the server writes no per-shot rejection line, so 'the server body
has no weapon' is read from that doc-comment plus eight seconds of point-blank fire doing
zero damage. It is not a direct log observation, and a FireRejection counter would settle it.
