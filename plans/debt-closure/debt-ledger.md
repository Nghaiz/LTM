# Debt ledger — every V0–V10 open item, re-verified against `develop`

- **Created:** 2026-08-19 · **Verified at:** `38ac29a`, working tree clean · **Phase:** `phase-0-ledger.md` ([spec deleted — index](phases/README.md))
- **Track:** [`plans/debt-closure/plan.md`](plan.md) · **Design of record:** [`2026-08-19-v0-v10-debt-closure-brainstorm.md`](../reports/2026-08-19-v0-v10-debt-closure-brainstorm.md)
- **Supersedes:** [`plans/replication/integration-checklist.md`](../replication/integration-checklist.md) round 8 (2026-08-16), and the STILL-OPEN tables of the nine closure reports under `plans/replication/reports/`.

This file replaces five sources of truth. Every row was re-checked against today's tree; no row
carries a status inherited from a document. **Every later phase updates the row it closes, in the
same commit as the closing work** — that is the only thing keeping this from becoming the sixth
stale document.

## How to read a row

| Status | Meaning |
|---|---|
| `VERIFIED-OPEN` | Still open today, confirmed by reading the tree. Ambiguity grades here, never to closed. |
| `ALREADY-CLOSED` | Closed, with a **positive citation** — a `file:line` showing the fix, or a commit whose diff shows it. The absence of a grep match was never accepted as evidence of closure. |
| `VOID` | The item no longer describes the world: a decision superseded it, or the thing it names does not exist. |
| `DECIDED` | A decision, not work. The decision and its citation are recorded; nothing is owed. |

Every negative claim states the paths searched. Where prefab YAML could not settle a question
without the Editor, the row stays `VERIFIED-OPEN` and says so.

---

## 1. Gate runs (Task 0.1)

| Gate | Command | Exit | Output |
|---|---|---|---|
| SpecChecker | `dotnet run --project tools/SpecChecker` | **0** | `[SpecChecker] OK — 90 constant(s) match plans/00-shared/protocol-spec.md.` |
| ClientWiringGate | `dotnet run --project tools/ClientWiringGate` | **0** | `KNOWN GAP - ClientMessageRouter.OnPlayerList has no production subscriber…` · `14 of 15 ClientMessageRouter events have a production subscriber and the rest are named gaps above; G2-G5 clean across 406 file(s).` |

Neither returned **exit 2**, so no row inherits a "the gate could not tell" verdict. The one named
gap corroborates row **C-2**; the weapon-registry half of `SpecChecker` settles **A-14**.

---

## 2. Group A — Editor/authoring

| id | item (source) | status | evidence | closes in |
|---|---|---|---|---|
| **A-1** | `NetClientProjectilePresenter._prefabsByKind` (V7) | **CLOSED** (Phase 1, 2026-08-19) | Component placed on the `NetClient` object in `Dustbowl.unity` and all **7** slots authored, one per `ProjectileKind`, each resolving to the component type the enum documents (Shell→`ExplodingProjectile`, Rocket→`Rocket`, GuidedMissile→`JavelinMissile`, Grenade→`GrenadeProjectile`, AmmoBag→`Ammobox`, Medipack→`Medipack`, Bullet→`Projectile`). Verified beyond non-nullness: `ProjectileCatalogBuilder.FromPrefabs` populates **7 of 7** kinds with live numbers. **Pinned by** `AssetWiringDetectors.PrefabsByKindIsComplete`, which reads its expected count off the enum rather than a constant, and was observed RED before the authoring. *Original finding:* `ProjectileKind` has **7** members (`Ironfront.Net.Protocol/Enums/VehicleEnums.cs:208-232`, Shell 0 … Bullet 6). Script guid `feedb881d60a4284c8e4425b7f3c2c46` (`NetClientProjectilePresenter.cs.meta:2`) has **zero** references outside its own `.meta`, so the component sits on no GameObject and 0 of 7 entries are authored. Not added at runtime either — the only `AddComponent<>` calls under `Assets/Scripts/Net/` are `RemoteVehicleRegistry` and `ClientVehicleStage` (`NetClientBootstrap.cs:270,273`). **Wider than the design states:** the task is *place the component, then fill the array*. Scope: zero guid refs across `Ironfront_Reborn/Assets/**` (61 prefabs, 4 scenes, all `.asset`), `ProjectSettings/**`, `Packages/**`. | 1 (see **X-1**) |
| **A-2** | E1 — remote-actor rig: animator, ragdoll, muzzle anchor, weapon mount (V10) | **DECIDED 2026-08-28 (verdict-closure R6 task R6.2) — three fields authored, `_actor` is WON'T-DO with a subject-keyed reopening condition.** Previously `PARTIALLY CLOSED` (Phase 1, 2026-08-19), which R6 was explicitly forbidden to leave standing: an inherited status nobody re-reads is the shape that produced this whole track. | **Three of four fields authored.** `RemoteActorView` added to `Remote Actor Proxy.prefab`, with `_animator` → the humanoid `Animator` on `character`, `_upperBody` → the avatar's `Chest` bone, and `_muzzleAnchor` → a new `Weapon Mount/Muzzle Anchor` pair parented to the avatar's `RightHand` (E1's weapon mount; the muzzle hangs off the mount so retuning one moves both). Fields rather than child names, because a child called `Muzzle` that nothing references passes a name check and renders no flash. **`_actor` is WON'T-DO, and with it the ragdoll rig — decided 2026-08-28, reason below.** **(a) The naive shape is actively wrong, not merely out of scope.** `ActorManager.Register` (`ActorManager.cs:55-62`) ends `if (!actor.aiControlled) instance.player = actor`, and `Actor.Awake` sets `aiControlled` from the controller type (`Actor.cs:208`). A remote proxy carrying an `Actor` is not AI-controlled, so registering it would **overwrite the local player's own actor** and repoint every `ActorManager.Player` read — the AI's position/team/health/resupply reads that the property's own remark exists to protect, and the hazard `Projectile.cs:128` already names. *(Registration is at `Actor.cs:216`, in `Start`; this row previously cited `Actor.cs:186`, which has drifted.)* **(b) The C4a seam does not make it cheap.** `_actor` was widened to `MonoBehaviour` behind `IGameplayActorPresence`, so the field no longer *demands* an `Actor` — but `Actor` is still the interface's only implementor (`Actor.cs:5`), and the members that matter here (`HasRagdollRig`, `MainRagdollBody`, `KnockOver`) map onto `Actor.ragdoll` / `ActiveRaggy` / `Actor.KnockOver`, none of which the proxy prefab has. **(c) So authoring is asset work plus a behavioural change to a predefined assembly**: a ragdoll rig and weapon models on `Remote Actor Proxy.prefab`, plus either a registration opt-out inside `Assembly-CSharp/Actor.cs` or a second presence implementation with its own ragdoll driver. That is not authoring and it is not an S/M task. **What stays lost, and it is cosmetic:** a remote death slides to the floor at a fixed pose and remote hands are empty. **Reopening condition, keyed to the subject rather than to a phase** — the same lesson D-2 records after its folder-keyed condition fired falsely: this becomes authoring the moment `Remote Actor Proxy.prefab` carries a ragdoll rig **and** some `IGameplayActorPresence` implementation exists that does not self-register with `ActorManager`. Check it by locating those two, not by re-reading this row. Neither exists on 2026-08-28. `RemoteActorView` announces both absences once at runtime by design. **The entry IS the record, so it stays** — and it cannot decay quietly, because the companion assertion hard-fails the moment `_actor` is assigned without it being deleted. **Pinned by** `AssetWiringDetectors.RemoteActorPrefabIsAuthored` for the three, and by its `KnownUnauthoredFields` entry for `_actor` — printed on every run, and a **hard failure** if `_actor` is ever assigned without the entry being deleted (`KnownUnauthoredFields_HasNoStaleEntries`). *Original finding:* `_remoteActorPrefab` → guid `6837a81a009b4af47bcb7863b2b20e21` = `Assets/Prefab/Remote Actor Proxy.prefab`, assigned at `Assets/Scenes/Dustbowl.unity:185866-185867`. That prefab holds four component classes across 941 lines: 26×GameObject, 26×Transform, 1×Animator, 1×SkinnedMeshRenderer. **Animator IS authored** (`Remote Actor Proxy.prefab:78-79`) — the only part of E1 done. **Zero MonoBehaviour blocks**, so `RemoteActorView` (guid `076337bd…`, zero refs) is absent and `_animator`/`_actor`/`_muzzleAnchor`/`_upperBody` (`RemoteActorView.cs:43,46,49,58`) are unassignable. **Zero Rigidbody/collider blocks** → no ragdoll rig. No muzzle or mount child among the 26 names. Corroborated at `plans/unity-client/reports/2026-08-18-round9.md:126`. | [R6](../verdict-closure/phases/phase-r6-human-pass-and-decisions.md) |
| **A-3** | E2 — `.meta` files for the new `Net/Client/` scripts and phase-05's three (V10) | ALREADY-CLOSED | Exhaustive sweep: every `.cs` under `Ironfront_Reborn/Assets/**` has a sibling `.meta`, and every directory under `Assets/Scripts/**` has a directory `.meta`; both loops printed no misses. Phase-05's three each have theirs: `Assets/Scripts/Net/Server/{ServerActorDamageSink,ServerCombatBridge,ServerCombatEvents}.cs.meta`. `Ironfront.Net.Replication/Client/` and `tools/ClientWiringGate/` live outside `Assets/`, so Unity never asks them for `.meta` — E2 named them but they were never an import concern. | — |
| **A-4** | E3 — per-weapon muzzle-flash and report refs (V10) | **CLOSED** (Phase 6 task 6.6, 2026-08-26) — **and the residue was resolvable from YAML, not "Editor-only"** | **The named residue is discharged, by a stronger method than the Editor eyeball it asked for.** The row said "YAML cannot resolve the `WeaponIds` member → prefab mapping". It can: `Assets/Resources/_Managers.prefab:90` holds `WeaponManager.weapons`, a list of `WeaponEntry` rows each carrying `NetworkId`, `name` and a `prefab` guid — the same asset `tools/SpecChecker` already reads to keep `WeaponIds.cs` and the spec from drifting. Resolving all 17 rows guid→`.meta`→prefab: **every row resolves to a real prefab, and all 17 carry an `AudioSource`**, so E3's report clause holds for the whole id space rather than for a sample. **8 of 17 carry a non-zero `muzzleFlash`** — ids 1 RK-44 (`ak`), 2 S-IND7 (`mk25`), 3 S-IND7 [SUP] (`mk25 suppressed`), 4 76 EAGLE (`shotgun`), 5 BEU AW1 (`smaw`), 6 SL-DEFENDER (`sniper`), 13 SIGNAL DMR (`DMR`), 15 RECON LRR (`RFB`) — exactly the eight this row already named, now tied to their ids instead of to their filenames. The other 9 are throwables and utility items (FRAG, SPEARHEAD, BINOCS, AMMO BAG, MEDIPACK, N.V. GOGGLES, WRENCH, SUPER WRENCH) plus id 12. **One asymmetry found and NOT silently fixed:** id 12 BIL SCALPEL (`javelin.prefab:95`) has `muzzleFlash: {fileID: 0}` while firing a projectile (`projectilePrefab` at `:98`), which is what id 5 BEU AW1 (`smaw.prefab:76`) does WITH a flash. Both are launchers; one flashes and one does not. That is an art call inside the deliberately-silent set E3 permits, not a code defect, so it is recorded here rather than changed. *Original finding:* eight firearm prefabs carry a non-zero `configuration.muzzleFlash`; `audio` is resolved at runtime via `GetComponent<AudioSource>()` (`Weapon.cs:144`, `:177`). | closed |
| **A-5** | E4 — `CosmeticTracerPool` tracer visual (V10) | **CLOSED** (Phase 1, 2026-08-19) | Both gaps closed. (a) `CosmeticTracerPool` placed on the `NetClient` object in `Dustbowl.unity`; Play Mode confirms it pre-warms its 32 streaks. (b) `Assets/Prefab/Cosmetic Tracer.prefab` created as a stripped copy of `AK Tracer.prefab` — `Projectile`, colliders and rigidbodies removed, leaving `Transform/MeshFilter/MeshRenderer` plus its `Tracer Glow` child, so the streak reads identically and can hurt nobody. **Pinned by** `AssetWiringDetectors.TracerPrefabIsCosmeticOnly`, which checks the assigned prefab is **inert** rather than merely non-null, and knows all six `Projectile` subclasses (`Rocket`, `JavelinMissile`, `GrenadeProjectile`, `Ammobox`, `Medipack`) — a check naming only the base class would pass a tracer built from a frag grenade. *Original finding:* two independent gaps. (a) Component on nothing: guid `188a29154b294b60bc5577fb9b082e01` has zero refs across `Assets/**`, so `_tracerPrefab` (`CosmeticTracerPool.cs:32`) is unassigned by construction. (b) **The asset E4 asks for does not exist.** E4 requires a streak with no collider, no `Projectile` component, no `source`; all six existing tracer prefabs are live projectiles, each carrying an authored `Projectile.Configuration` (e.g. `Assets/Prefab/AK Tracer.prefab:105-119`). Their collider count is 0, so only the `Projectile`-component clause is violated — and that is the clause that matters, since reusing them would spawn damage-dealing rounds on the client. | 1 |
| **A-6** | E5 — HUD wiring for `ScoreUi.SetAuthoritativeState` (V10) | **CLOSED** (Phase 6 task 6.6, 2026-08-26) — **and clause (b) never needed Play Mode** | **Clause (a) — the dedicated human-count element now exists.** `ScoreUi.humanCountText` (`ScoreUi.cs:54`) is rendered on its own at `:221-229`; the concatenation into the phase label survives only as a fallback for a prefab predating the element (`:217`). Authored on the shipped prefab in the Editor (not by hand-editing YAML, per the asset gate's own warning about fileIDs): `Prefab/Ingame UI Container.prefab` gains `Score UI Canvas/Phase Row/Human Count`, and `humanCountText: {fileID: 3323220635708522552}` at `:7624` is distinct from `phaseText` and `phaseTimerText`. **Pinned by extending A8** (`AssetWiringDetectors.OwedPhaseLabels` + `RenderedLabels`), so the field must be assigned AND must not point at a label another field already drives — E5's whole point is three SEPARATE elements, and two aimed at one label would satisfy any per-field null check. **Mutation-proved 2/2:** unassigned → A8 exit 1; aimed at the phase label → A8 exit 1 twice, once from each side of the collision. Rendering is self-describing (`"8 players"` / `"1 player"`) so no companion static label has to be authored and kept in sync, and a count of 0 renders blank rather than a fabricated `"0 players"`. **Clause (b) — "the timer is hidden during `Playing`" is implemented, and the claim that it needs a Play Mode observation is wrong.** The chain is readable end to end: `MatchStateModel.HasTimer` (`MatchStateModel.cs:64`) gates on the PHASE, not on the value — `_hasAny && _current.Phase != MatchPhase.Playing` — so a `PhaseSecondsRemaining` of 0 during `Playing` (which is what the `ushort` wire field carries; it cannot express a negative) never reaches the HUD as a clock; `NetClientObjectivePresenter.cs:162-164` maps `!HasTimer` to the `-1` sentinel; `ScoreUi` blanks on a negative rather than rendering `"0:00"`. **The library half is already pinned** by `ClientEventConsumptionTests.ThePlayingPhaseRendersNoTimer:269`. **Named residue, stated rather than implied:** the two MAPPING steps (presenter `!HasTimer` → `-1`, and `ScoreUi` negative → blank) are `Assembly-CSharp`/Unity-tree code that no test assembly can reference (**E-11b**) and no gate rule grades today. Deleting either would restore a permanent `"0:00"` with every test green. That is a G9/G10-shaped gate clause and it is NOT in this commit. | closed (mapping steps ungated) |
| **A-7** | E6 — `NetClientExplosionPresenter._effectsByKind` `ParticleSystem[]` (V10) | **CLOSED** (Phase 1, 2026-08-19) | Presenter placed on the `NetClient` object, and indices 0 (`Grenade`) and 1 (`Rocket`) authored as two reusable scene FX groups — `Explosion FX (Grenade)` and `Explosion FX (Rocket)`, built from the `Explosion *` particle children of `Frag Grenade.prefab` and `pod rocket.prefab` so a replicated blast looks identical to a locally-simulated one. Everything that could act (the projectile script, mesh, audio, trail) is stripped; the group root is an emitter-less `ParticleSystem`, so the presenter's one `Play()` fires all of them. `playOnAwake` off throughout — Play Mode confirms 13 systems present, none playing. Vehicle (2) and Environment (3) left empty on purpose: neither kind has a producer (**C-10**, **C-11**), and `NetClientExplosionPresenter.cs:210-217` requires them not to throw. **Pinned by** `AssetWiringDetectors.ExplosionEffectsAreAuthored`, which demands 0 and 1 and permits 2 and 3. *Original finding:* component on no GameObject: guid `db9e52959104431aaaadf330b21686f8` has zero refs across `Assets/**`, so `_effectsByKind` (`NetClientExplosionPresenter.cs:61`) has zero authored entries where E6 requires indices 0 (`Grenade`) and 1 (`Rocket`). This is a blanket failure — see **X-1**. | 1 (see **X-1**) |
| **A-8** | `NetTurret` + `TurretAimLimits` on every turret prefab (V6) | **VOID** | The `NetTurret` MonoBehaviour was never built and was deliberately superseded during V6. `Assets/Scripts/Net/Shared/NetTurretAim.cs:70-79`: "**A static resolver, not a component on every turret prefab.** … it would ship a seam that stays inert until fourteen prefabs are re-saved, with nothing anywhere reporting the gap. … Every id this needs is reachable at runtime from `MountedWeapon.user.seat`, so no authoring is required." `TurretAimLimits` is a plain struct (`Ironfront.Net.Replication/Vehicles/TurretAimLimits.cs:27`), not a component, so it cannot be attached at all. Scope: `class NetTurret` → zero across `Ironfront_Reborn/Assets/**` and every `Ironfront.Net.*` project. | — |
| **A-9** | `ScoreUi.phaseText` / `phaseTimerText` (`ScoreUi.cs:23,25`) (V10 E5) | **CLOSED** (Phase 1, 2026-08-19) | Both keys authored, and authored as *new* elements rather than the labels the fallback already borrowed. A `Phase Row` was added to `Score UI Canvas` as a sibling of `Panel` (anchors x 0.3-0.7, y 0.855-0.900, so it sits directly under the ticket panel and clear of the bar, the score labels and both flag circles), carrying `Phase Label` (left half, `MiddleLeft`) and `Phase Timer` (right half, `MiddleRight`) — both styled from `blueFlagsText` so the row reads as part of the same HUD. Authored through the Editor over MCP (`PrefabUtility.LoadPrefabContents` / `SaveAsPrefabAsset`), never by hand. **Pinned by** `AssetWiringDetectors.ScoreUiTextRefsAreAssigned`, which checks four things, not one: the field is assigned; its fileID names an object that exists; that object is the **same component type** as the labels this `ScoreUi` already drives; and it is **distinct from every other `Text` `ScoreUi` drives** (`blueScoreText`, `redScoreText`, `blueFlagsText`, `redFlagsText`, `victoryText`, and the sibling owed field). **Every one of those clauses exists because review proved the previous draft green, by mutation, on an authoring it was written to forbid.** Three mutations, all reported `clean, exit 0` when found: (A) the assignments **cross-swapped** onto the flag labels — neither equals *its own* fallback, so the original pairwise comparison passed while the HUD rendered exactly what the null path rendered; (B) both fields aimed at a **fileID no object carries** — non-zero, so `IsNull` is false, while Unity still loads null; (C) `phaseText` aimed at an **anchor of the wrong type** (the `Phase Row` `RectTransform`, `!u!224`, which genuinely exists) — resolution alone accepted it, and Unity loads a type mismatch as null. Drag-and-drop cannot produce (C); a programmatic authoring pass, which is exactly how this row was closed, can. All three now exit **1**, re-verified against the real prefab, which was restored byte-identical (`1caf3516c83b8263e6d4f7d9fc917ed7`) after each run. The expected component type is read off a sibling label in the same document rather than pinned to a guid, because uGUI's `Text` carries one guid in the legacy DLL form and another in the package form and this tree is mid-migration. `RenderedLabels` carries its own staleness companion (`RenderedLabels_HasNoStaleEntries`): a listed field the block does not carry is a finding, because `Reference()` cannot tell a renamed field from an unassigned one and the distinctness loop skips both. **Observed RED first** against the pre-authoring tree — 2 findings, exit **1** — then exit **0** after the save. **Side effect, deliberate and verified:** saving migrated the prefab from the legacy `m_PrefabParentObject` format to V2, rewriting the whole file. All 462 pre-existing objects kept their fileIDs (the only removal is `!u!1001 &100100000`, the legacy container the V2 format drops); `GameManager.ingameUiPrefab` was re-loaded in the Editor and still resolves, and all nine previously-assigned `ScoreUi` refs re-read non-null. *Original finding:* The serialized `ScoreUi` block at `Assets/Prefab/Ingame UI Container.prefab:7226-7237` enumerates nine assigned fields and **omits both keys**. An absent key leaves the field `null`, so `SetAuthoritativeState` still takes the flag-text fallback and its `WarnOnce` naming E5 still fires. `ScoreUi` exists in no other asset. Scope: all `.prefab`/`.unity`/`.asset` under `Assets/**`. | 1 |
| **A-10** | authored `damageDropOff` curves sampled into `ProjectileConfig` (V7) | ALREADY-CLOSED | **Sampling step:** `Assets/Scripts/Assembly-CSharp/ProjectileCatalogBuilder.cs:84` passes `Sample(source.damageDropOff)` into `FromConfiguration`; `Sample` (`:95-107`) evaluates at `ProjectileConfig.DropoffSamples` points and returns an empty table for a keyless curve rather than 32 zeroes. **Source curves:** 16 projectile prefabs carry authored keyframes (e.g. `Tank Projectile.prefab:106-120` flat 1.0; `AK Tracer.prefab:105-119` 1.0 → 0.75 across range). No curve authoring is owed. **Dependency, not residual work:** the sampled table reaches a live `ProjectileConfig` only once **A-1** (client) and **X-2** (server) are wired. Do not schedule separate curve work. | — |
| **A-11** | `aimLimits` on turret prefabs (V0 § 7) | **VOID** | V0 closure § 3.4 still holds verbatim: `aimLimits` returns nothing across `Assets/**` `.prefab`+`.unity`, the same result recorded at `plans/replication/reports/2026-08-17-phase-v0-closure.md:116-133`. With the key absent Unity leaves the C# initializers standing, and both are already the intended values — `MountedTurret.cs:24-30` (600 °/s, pitch stops −40/+15), `TankTurret.cs:35-41` (300 °/s, `PitchMin`/`PitchMax` overwritten in `Awake` from the cannon joint per `:31-33`). Optional tuning, not work; it never gated anything. | — |
| **A-12** | `LobbyShellOverlay`'s three serialized fields (E9) | **VOID** | E9 is a scene-hygiene note, not an assignment (`integration-checklist.md:128-137`: any prefab or scene referencing the component will be marked dirty on next save). **No prefab or scene references it** — guid `3a9b866060cb47f1af22ca5125dd4d71` has zero refs across `Assets/**` — so no asset can be dirtied and there is nothing to re-save. All three fields carry the intended LAN/plaintext defaults as C# initializers (`LobbyShellOverlay.cs:62,66,70`). See **X-5** for the wiring gap this exposes. | — |
| **A-13** | A4 — `NetMovementAgent` + `NetPredictionClock` on the player prefab | ALREADY-CLOSED | Both on `Assets/Prefab/Player Fps Actor.prefab` in exactly the state A4 specifies. `NetMovementAgent` (guid `bb446c36efee1994485a87e8b7f17466`) at `:341`, `m_Enabled: 1` (`:340`); `NetPredictionClock` (guid `5d38a7c4162b48f09e73c0aa4b16de52`) at `:352`, **`m_Enabled: 0`** (`:351`), `MaxTicksPerFrame: 5` (`:355`). A4's pass condition — both components, clock **disabled** (`integration-checklist.md:156-162`) — met on both clauses. Landed in `e7f61e3` (PR #117). Confirmed at `plans/unity-client/reports/2026-08-17-round8.md:575`. | — |
| **A-14** | the `CAR HORN` row in `_Managers.prefab` so `SpecChecker` passes (V6) | **VOID — and inverted** | `WeaponIds.cs:96` declares `CAR_HORN = 18`; `WeaponIds.cs:144-145` `IsLoadoutRegistered => IsKnown(weaponId) && weaponId != CAR_HORN` **exempts** it. `SpecChecker` checks that exemption in both directions — the reverse sweep skips exempt ids (`Program.cs:321-333`) and the forward sweep **fails outright if one appears** (`Program.cs:291-301`). `_Managers.prefab` holds 17 `NetworkId:` rows and zero `car horn` matches, which is why the gate exits 0. Pinned by `MountedWeaponAndTurretTests.cs:694-721` (`AnExemptIdNeedsNoPrefabRow`, `TheExemptionHasNoStaleEntries`). **Authoring this row would turn `SpecChecker` RED.** Do not do it. | — |

**Group A: 3 open · 7 closed · 4 void.** (Phase 1 closed A-1, A-5, A-7 and closed A-2 down to its
`_actor` half. A-6 and A-9 wait on **C-4**.)

**Phase 1 re-verified the four rows its own phase file still listed as work, and confirmed all four
owe nothing.** The phase file predates this ledger, so it named them; the ledger's evidence stands:

| Phase-1 task | Row | Re-verified 2026-08-19 |
|---|---|---|
| 1.4 `NetTurret` + `TurretAimLimits` | **A-8**, **A-11** | Both VOID. `NetTurret` does not exist to attach; `TurretAimLimits` is a struct. No work. |
| 1.5 `CAR HORN` row in `_Managers.prefab` | **A-14** | VOID **and inverted**. `SpecChecker` re-run: exit **0**, 90 constants matched. Authoring the row would turn it RED. Not done, deliberately. |
| 1.7 `damageDropOff` curves | **A-10** | ALREADY-CLOSED, and now *reaching* something for the first time — see **X-2**. No curve authoring owed. |
| 1.9 A4 player prefab | **A-13** (gated on **E-5**, void) | ALREADY-CLOSED. Both components authored, clock disabled as A4 requires. No work. |

---

## 3. Group B — verification needing two real clients

Every row here shares one blocker, so the evidence is stated once. **No two-process game-server
harness exists.** Scope searched: `tools/` (only `ClientWiringGate`, `SpecChecker`,
`UnitySyntaxCheck` plus shell/ps1 scripts), `Ironfront.Tools.*/`, `Assets/Scripts/Net/`.
`Ironfront.Tools.LoadTest` targets the **master** server over MSP (`LoadTestOptions.cs:144`
`--master`), and `tools/run-integration.ps1:1-16` is a two-process integration script for the
**master**, not the game server — prior art of the right shape, wrong target. Reusable pieces:
`Assets/Scripts/Net/Headless/LocalClient.cs`,
`Assets/Scripts/Net/Diagnostics/{MovementShadowCompare,TransportDebugOverlay,VehicleReplicationOverlay}.cs`.

Round 9's two-client observations were produced through **one Editor** over the MCP `script-execute`
bridge (`plans/unity-client/reports/2026-08-18-round9.md:17-18`), driving the server directly. They
do not discharge any row below. See **X-3** for why an honest second client cannot be scripted yet.

| id | item (source) | status | evidence | closes in |
|---|---|---|---|---|
| **B-1** | E7 — two-client combat check (V10) | **RUN 2026-08-25 — FLAKY, 1 of 3, and both halves have a named owner: X-26 (the flake) and X-36 (the name)** | **The chain fire → hit → kill → killfeed resolves, for the first time in this investigation.** `artifacts/lane-b/x25-torso-aim-02/observer-{a,b}-checkpoints.jsonl`: 30 shots `rejection=None`, 4 with `hits=1`, OBS-A **100 → 21 → 0**, `alive:false`, and a killfeed line on **both** observers — `killerActorId 41 / victimActorId 42 / cause Bullet`. Configuration, because a verdict without one is not repeatable: `pwsh tools/run-lane-b.ps1 -Set combat -SpawnIndex 0`, seeds `UnityEngine.Random=20260821` and `NetworkSimulator=off`, spawn pinned index 0 of 6, weapon pinned RK-44 (X-27's `ILoadoutDirectory`). **Flaky 1 of 3, and the flake is not the aim.** The rate is measured on `artifacts/lane-b/x27-pinned-01/-02/-03`, the set in which weapon AND witness are both controlled — `x25-torso-aim-02..04` differ in weapon and in who shot whom, so a rate over those three would be a rate over three different experiments. With both controlled the rate did not move: min distance **3.30 m / 1.52 m / 1.93 m**, occluded **0 / 16 / 18**, hits **4 / 0 / 0**, and all **34** occlusions are `Bone_002 layer=8` at `frac≈0.94` — the victim's own rig bone. The only run that scored is the only one whose pair never got closer than 3.30 m: **X-26**, distance-dependent. **The "with a name" half is not closeable by any run.** The feed renders `killerName "#5001"` / `victimName "#5002"` — the transport player id — because the server never parses the join ticket; filed as **X-36**. Report: `reports/2026-08-25-phase-3d-lane-b-verdicts.md`, `reports/2026-08-25-x27-x28-and-what-the-flake-really-was.txt`. **X-36 IS CLOSED (2026-08-26, verdict-closure R2).** The server now reads the display name out of the signed join ticket it was already parsing, so `S_PLAYER_LIST` carries a real name and the feed no longer has only `#5001` to render. **The "with a name" half is therefore no longer unclosable-by-any-run** — it needs a lane-B run against a server whose clients join with named tickets, which the harness's `JoinTicketSource` can mint. The flake half (**X-26**) is untouched and still R4's. **R4 CLOSED THE FLAKE'S CAUSE (2026-08-26).** X-26 is fixed: the victim's own rig no longer occludes the shot that hit it, so the distance-dependence this row measured has no mechanism left. **This row still does not carry a verdict, and must not**: no lane-B run has been taken across the fix (V-D2), R1 owns the runs, and the crossover distance stays unmeasured. The 'with a name' half was closed separately by R2 (X-36).| [R4](../verdict-closure/phases/phase-r4-prediction-and-hitbox.md) + [R2](../verdict-closure/phases/phase-r2-seat-and-name.md) |
| **B-2** | E8 — two-client HUD check (V10) | **RUN 2026-08-25 — graded PASS with a caveat, and the caveat is a measurement nobody took: this row moves to X-29, it does not close** | `artifacts/lane-b/x27-pinned-01/observer-b-checkpoints.jsonl` (`hud`): the drawn scoreboard equals the **offline** `MatchScoreboard` at **0 of 7** checkpoints, and runs on a different clock — a round cycle `200 → 0 → 200` against an offline model creeping `0/0 → 2/20`. In `artifacts/lane-b/x25-torso-aim-02` it agrees at 1 of 7, and that one is a coincidence (`2/64` on both sides). Read against `ScoreUi`'s own remark — *"Two sources feed it and they never mix"* — a dead presenter would leave the OFFLINE model driving the text, so **not-offline is sufficient for not-dead**. **What no artifact here shows is that the drawn numbers equal the SERVER's numbers.** That needs the server process to record its own totals, which nothing does; it is **X-29**'s second half, and closing this row on the design argument would be exactly the "assumed passing" `phase-3e-run-and-ledger.md` ([spec deleted — index](phases/README.md)) § 5 forbids. | [R1](../verdict-closure/phases/phase-r1-programme-set.md) |
| **B-3** | E9 — two-client capture-point check (V10) | **CLOSED (2026-08-25) — the one row in this phase whose artifact supports the whole check** | `artifacts/lane-b/x25-torso-aim-02/observer-{a,b}-checkpoints.jsonl` (`capturePoints`): `Fortress Capture Point` transitions `owner 1 → -1` between the `approach` and `in-range` checkpoints, and **both observers agree on all six points at all seven checkpoints** — compared field by field, zero disagreements. E9 reads "capture point changes owner on both clients"; the owner changed and both clients agree, so the check is met. Configuration as B-1. **Stated rather than buried:** the transition observed is team → neutral, not team → team. A team → team transition is the stronger reading of E9 and this run does not contain one — that is a remark about strength, not a missing measurement, which is why it does not hold the row open. **Annotated 2026-08-27 (verdict-closure R3), and the annotation STRENGTHENS it.** This check's decoded-agreement half was graded by lane A's `AgreementBlock`, which **X-35** showed could not tell divergence from staleness — so its PASS was computed by a counter that would have reported a non-zero whenever interest management spread the clients out, and its zero proved less than it appeared to. Re-measured with the split in place: **0 substantive divergences and 0 quantizer-edge divergences over 15,987 same-tick comparisons** across three R3 runs. The verdict is not withdrawn — it is now supported by a measurement that could have failed. [`2026-08-27-phase-r3-wire-integrity.md`](reports/2026-08-27-phase-r3-wire-integrity.md) § 4.4. | — |
| **B-4** | E10 — two-client grenade check (V10) | **RUN 2026-08-25 — BLOCKED on X-31; the grenade is never equipped, so nothing detonates on either client** | `artifacts/lane-b/x-grenade-01`: 3 of 3 clients exit 0 with 5 of 5 checkpoints, spawn pinned to 0, `-Weapon "RK-44" -Gear "FRAG"`, server logs `primary='RK-44' gear1='FRAG'` and `explosionsAttached: true` on every client — and **`weaponId` reads 1 (the rifle) on all three clients at all five checkpoints**, `explosionsTotal: 0`. The harness works; the grenade never reaches the hand. Located to the client since: `artifacts/lane-b/x31-diag-04` shows the server receiving `buttons=0x0001 slot=-1` on **60 of 60** frames — `Fire` alone — from a programme whose same step object carries `"switchWeaponSlot": 2`. E10 needs a detonation to compare and there is none to compare.  **STILL BLOCKED 2026-08-27, and it moves off X-31 rather than staying on a closed row.** X-31 is fixed and proven: `artifacts/lane-b/r1-grenade-03` holds the FRAG (`weaponId` **7**) and fires it (**60 of 60** `[shot] weapon=7`), one shot passing `rejection=None fired=True hits=1`. **And nothing detonated** — `explosionsTotal 0` on all three clients with `explosionsAttached true`, because the grenade resolved as **hitscan** at 1.2 m. The carried-weapon fire path models no projectile and announces no explosion; that is **X-42**, a second defect X-31 was hiding, and E10 still has nothing to compare. | X-42 |
| **B-5** | E11 — A16 camera-hijack check (V10) | **RUN 2026-08-25 — NOT GRADED, and the reason is filed as X-37: no programme provokes the case** | `artifacts/lane-b/x25-torso-aim-02/observer-{a,b}-checkpoints.jsonl` (`activeCameras`): the field is recorded at all seven checkpoints on all three clients and carries **baseline only** — one active camera throughout, no hijack ever attempted. The instrumentation is present and the situation is not. This is not a failing check and it is not a passing one: **nothing in `tools/lane-b/` contains a step that would make a second camera claim the view**, so no run of the existing programme set can grade E11 either way. Programme work, phase 3D, filed as **X-37**. | [R1](../verdict-closure/phases/phase-r1-programme-set.md) |
| **B-6** | E12 — scene-ordering check (V10) | **CLOSED — RUN 2026-08-27, GRADED PASS on a named artifact and mutation-proved (verdict-closure R1)** | No artifact grades E12, because no programme provokes a scene-ordering case — the same gap as B-5, filed together as **X-37** and recorded here with its own line rather than folded into B-5's verdict (`phase-3e-run-and-ledger.md` ([spec deleted — index](phases/README.md)) § 5, "silently absorbed"). **The second half of this row remains untrue and is left standing as the correction it is:** it read "none of the nine presenter scripts is referenced anywhere (**X-1**)", but X-1 closed on 2026-08-19 and what actually blocked E12 was **X-9** — every presenter on the `NetClient` object disabling itself in `Awake` because the role still read `Server`, fixed at `dc4fd73`. Confirmed still fixed by the runs above: all three clients render and report. E12 has needed only its programme since, and still does.  **GRADED PASS 2026-08-27, verdict-closure R1 task R1.2 — and the plan that ordered the work had the case wrong.** This phase asked for *"a step that puts the scene into the order it names"*. V10 § 7 states E12's pass condition as *"no presenter logs its null-bootstrap warning on a **normal client start**"* — a property of an ordinary start, not of a provoked situation. **So every lane-B run ever taken has already exercised E12**; what no artifact carried was the outcome, and no programme was ever needed. `LaneBCheckpointRecorder.presentersWithNoBootstrap` reads it off the same `HashSet` `NetClientPresenterGuard.WarnOnce` writes to — not a counter beside it, so the artifact cannot disagree with the log it summarises — and ships a count beside the names so *the key was absent* and *the list was empty* are different readings. **Verdict:** `artifacts/lane-b/r1-grenade-03/{driver,observer-a,observer-b}-checkpoints.jsonl`, `presentersWithNoBootstrapCount: 0` and an empty name list on **3 of 3 clients at 5 of 5 checkpoints**. **Mutation-proved, because an empty set is exactly the shape a false green takes:** forcing `TryResolveClient` onto its not-found branch and taking a real three-client run took the field 0 → **6**, naming all six presenters on all three clients, and the count equalled `grep -c` of the warning in the same client's log (`artifacts/lane-b/r1-e12-mutant`, `reports/2026-08-27-r1-e12-mutation-proof.txt`). **Headless half not claimed:** E12's second sentence — *a headless build logs nothing from any presenter* — is not graded here; the server process writes no checkpoint record, and that is X-29's gap. | closed |
| **B-7** | V5 — two clients see the same vehicle in the same place while a third drives it, 100 ms RTT / 5 % loss | **RUN 2026-08-25 — BLOCKED on TWO rows, X-30 and X-32; the half that is measured is exact and it is not this check** | **Parity, and it is the strongest number lane B has:** `artifacts/lane-b/x25-torso-aim-02/observer-{a,b}-checkpoints.jsonl` (`vehicles`) — 14 vehicles present on both observers at four consecutive checkpoints, **maximum component position delta 0.00 m**. **Both of the check's own conditions are missing, and neither is programme work.** (1) `drivenVehicleId: 0` on all three clients: `SeatRequestMessage` has **zero production senders**, so no client can ask for a seat and no recorded programme can express one — it is a reliable opcode, not an `InputButtons` bit (**X-30**). (2) The run used `-Sim off`, and the named condition is 100 ms RTT / 5 % loss — which lane A has now measured for the first time and found **unsurvivable**: 4 of 8 clients dropped inside 120 s at `--sim typical` against 8 of 8 on a clean wire (**X-32**, `artifacts/lane-a/run-01-report.json` vs `run-02-clean-report.json`). Grading this check at its stated condition needs X-32 closed first. **X-30 IS CLOSED (2026-08-26, verdict-closure R2).** `ClientSeatRequester` sends `C_SEAT_REQUEST` and `ScriptedInputStep.seatToggle` lets a recorded programme raise the edge, so the capability this check was blocked on now exists. **That unblocks the check; it does not grade it** — what is still owed is a lane-B run with a programme that actually mounts, which is R1's. **X-32 IS CLOSED (2026-08-27, verdict-closure R3)** — `--sim typical` now holds **8 of 8 for 121.2 s** (`artifacts/lane-a/r3/r3-typical-report.json`) against the 4-of-8 and 0-of-8 runs this row was blocked on. **Both of this check's blockers are now gone** (X-30 closed by R2, X-32 by R3); what is still owed is the lane-B run itself, under `-Sim typical`, with a programme that actually mounts — R1's. [`2026-08-27-phase-r3-wire-integrity.md`](reports/2026-08-27-phase-r3-wire-integrity.md) § 2.4. | [R1](../verdict-closure/phases/phase-r1-programme-set.md) |
| **B-8** | V5 — no perceptible input lag; convergence without visible snapping | **RUN 2026-08-28 — PARTIAL, and now for a second reason. Numeric half: still a sample too quiet to mean anything (X-28 open, nobody moved). Human half: LOOKED AT 2026-08-28 and UNGRADEABLE — the frames render the deploy menu, not the game (X-48), and stills cannot show input lag at all. X-38 is closed; this row is not.** | `artifacts/lane-b/x25-torso-aim-02/*-checkpoints.jsonl`: `correctionSnaps 0`, `correctionBlends 0`, `lastPositionErrorM 0`, `lastAngleErrorDeg 0` at every checkpoint on all three clients. **Read that as "nothing snapped", not as "prediction is healthy."** X-28's pin puts every body on spawn point 0, inside the driver's 6 m hold distance, so the approach never runs and prediction had almost no work to do — the run cannot distinguish a healthy reconciler from an idle one. **X-21** — `PredictionReconciler.Reconcile` replays inputs without ever moving the predicted position — is still open and will resurface the moment this check gets a sample worth having. **The human half is unverdicted, not passed:** 21 frames captured, nobody has watched one (**X-38**). **R4 CLOSED THE NUMERIC HALF'S CAUSE (2026-08-26).** X-21 is fixed — the replay writes the position back — and X-24's 3 cm seam is closed, so a future run's `correctionSnaps`/`lastPositionErrorM` can mean something. **Still PARTIAL, and still ungraded:** this row's numbers came from a sample too quiet to distinguish a healthy reconciler from an idle one, and no run with a real approach exists yet (X-28 → R1). The human half is unwatched (X-38 → R6). R4 also found that `CorrectionCount` counts lag rather than misprediction — **X-41** — so a future artifact's `corrections: N` must not be read as a mispredict rate. **HUMAN PASS DONE 2026-08-28 (verdict-closure R6 task R6.1) — check 8 is UNGRADEABLE FROM THE ARTIFACT, and that is a finding about the capture, not a pass.** Fresh post-R1/post-R4/post-R5/post-C5 run: `artifacts/lane-b/r6-combat-01`, `-Set combat -SpawnIndex 0 -Weapon "RK-44"`, 3/3 clients exit 0 with 7/7 checkpoints. **The frames were watched, all 21, and they do not show the game.** Every one renders the deploy/loadout menu — `driver-01-spawned.png` through `driver-07-respawn-window.png` and both observers' sevens — so there is no player body in frame whose convergence could be watched. **Two independent reasons it cannot be graded**, either alone sufficient: (1) no body is rendered (**X-48**); (2) *input lag* and *visible snapping* are temporal properties, and seven stills taken 20-30 s apart cannot exhibit either even from correct frames — this check needs video or a per-frame trace, which the harness does not capture. **The numeric half is unchanged and still cannot mean anything:** `correctionSnaps 0`, `correctionBlends 0`, `lastPositionErrorM 0`, `lastAngleErrorDeg 0` on all three clients at all seven checkpoints — identical to the 2026-08-25 reading, because X-28 is still open and nobody moved (DRIVER's position is the same at all seven; OBS-A's too until respawn). **And the counters disagree with each other, which is X-41 on a live run:** `localActor.corrections` reads 1 / 3 / **2320** on DRIVER / OBS-B / OBS-A while `correctionSnaps` stays 0 everywhere, with OBS-A's count jumping 136 → 2015 across the one interval its RTT spiked to 106 ms. Read `corrections` as lag, never as a mispredict rate. | [R4](../verdict-closure/phases/phase-r4-prediction-and-hitbox.md) + [R6](../verdict-closure/phases/phase-r6-human-pass-and-decisions.md) |
| **B-9** | V5 — the kinematic remote path does not break an unlisted cosmetic | **RUN 2026-08-28 — UNGRADEABLE, which replaces UNVERDICTED and is still not a pass. The artifact has now been read, so X-38 no longer holds this row: all 21 frames render the deploy/loadout menu, the kinematic remote path is never in shot, and the check cannot be answered either way (X-48). Two unlisted cosmetics were found while looking and are filed rather than folded in (X-49, and the dead-plus-undeployed frame). This row stays open on X-48.** | `artifacts/lane-b/x25-torso-aim-02/*.png` — **7 frames per client, 21 per run**, captured at all seven checkpoints on all three clients, across three runs. This check is human judgment by construction: no counter in the checkpoint record can say whether a cosmetic outside Task 3's enumerated six broke. **A captured frame is evidence only once somebody has looked at it**, and nobody has — so this reads UNVERDICTED rather than PASS, which is the distinction [`phase-3d-lane-b.md`](phases/phase-3d-lane-b.md) § 5 asks for. The human pass is a deliverable that nothing currently schedules; filed as **X-38**. **HUMAN PASS DONE 2026-08-28 (verdict-closure R6 task R6.1) — check 9 is UNGRADEABLE, and two unlisted cosmetics are recorded as findings.** All 21 frames of `artifacts/lane-b/r6-combat-01` were watched. **The kinematic remote path is not in frame:** every PNG renders the deploy/loadout menu, so no remote body, muzzle flash or ragdoll appears in any of them, and a check about that path cannot be graded either way from them (**X-48**). This is not new and not a regression — the `< DEPLOY >` button region is **pixel-identical across all 90 frames of five runs** spanning 2026-08-25 to 2026-08-28 (`x25-torso-aim-02`, `x25-torso-aim-04`, `r1-grenade-03`, `r5-x47-smoke`, `r6-combat-01`), so the answer was available on the day X-38 was filed. **What the frames DO show, both outside Task 3's enumerated six and neither attributable to the remote path:** (i) a **Development Console overlay covering roughly a quarter of every frame on every client at every checkpoint** — `Hidden/CubeCopy` / `CubeBlur` / `CubeBlend` shader-not-found at `*-01-spawned.png`, then a `NullReferenceException` cascade from `*-03-in-range.png` onward (**X-49**); (ii) **two mutually exclusive UI states drawn at once** — `observer-a-05-killed.png` renders "You are dead. Press Space to respawn." on top of the loadout tiles and the `< DEPLOY >` button, i.e. a client simultaneously un-deployed and dead. **UNVERDICTED became UNGRADEABLE, not PASS** — which is the distinction the row was filed to protect. | [R1](../verdict-closure/phases/phase-r1-programme-set.md) + [R6](../verdict-closure/phases/phase-r6-human-pass-and-decisions.md) |
| **B-10** | V5 — Profiler: the client vehicle stage adds no per-frame allocation | **RUN 2026-08-27 — UNGRADEABLE, and the blocker has changed: no longer X-33 (instrument), now **X-44** (no driving window to measure)** | **The instrument gap is closed.** X-33 shipped `LaneBAllocationSampler` and check 10 was reassigned from lane A to lane **B** in [`phase-3-harness.md`](phases/phase-3-harness.md) § 2 itself. Every lane-B checkpoint now carries an `allocation` window, and the instrument was observed recovering an injected 1,048,576 B/frame to within frame noise — see X-33. **What blocks the grade now is the RUN, not the meter.** Check 10 asks whether the vehicle stage *adds* allocation, and "adds" is a difference: the per-frame figure while driving against the figure on foot, from one run. Every checkpoint of the 2026-08-27 combat set reads `drivenVehicleId: 0` on all three clients — the on-foot programme — so the run holds one side of the subtraction and not the other. **Numbers it does hold**, for the record and as the on-foot baseline the eventual difference is taken against: 24,626–29,812 B/frame steady state, 53,374–63,372 B/frame across the scene-load window, `valid: true` and 95–5,726 frames on every window (`artifacts/lane-b/*-checkpoints.jsonl`). **The vehicle programme does not exist and is not R5's** — § 3 of the phase file forbids it touching the programme set (R1 owns programmes; this phase owned an instrument). R1 could not write one either, and said so: **X-44**, *"no scripted client can walk to a vehicle"*. **And even with a programme this would not grade today**, because **X-46** means a networked player's vehicle does not respond to them at all — a driving window would be a seated client beside a stationary hull. | [R5](../verdict-closure/phases/phase-r5-lane-a-verbs.md) |
| **B-11** | V5 — a headless server survives drive → damage → burn → death with a networked driver | **RUN 2026-08-27 — PARTIAL, and for the first time on a run that TRIED: 2 of 4 verbs fire; the other 2 are blocked by X-46, and two throwing paths were fixed on the way** | **The harness can now provoke the sequence** (**X-34** closed): `--behavior combat` sends the seat, vehicle-input, fire and respawn opcodes and records each verb with the tick, the observing client and the evidence. That is the whole difference from the 2026-08-25 grade, which was PARTIAL because every frame of every run carried `InputButtons.None`. **The run:** `artifacts/lane-a/r5/r5-combat-06`, 8 clients / 150 s / clean wire, seed 12345. 8/8 held, 24,100 snapshots applied, `malformed/unknown 0/0`, decoded divergence **0** over 48,733 same-tick comparisons, and **zero exceptions in the whole server log**. **DAMAGE — yes.** Decoded tick 1307 by client 7, `S_HIT_CONFIRM target=47 hitbox=Limb`, 22 sightings across 2,298 trigger ticks. **DEATH — yes.** Same tick and client, `S_DEATH victim=47 killer=48 cause=Bullet`, 8 sightings, and a `C_SPAWN_REQUEST` after the respawn gate. **DRIVE — no, and the server says why in the same 150 s.** 1,142 `C_VEHICLE_INPUT` messages accepted against a hull that never moved, beside the log line *"actor 48 took a driver seat and no driver input sink could be attached to `Ai Character Optimizations(Clone)`"* (**X-46**). **BURN — no, and it is downstream of DRIVE.** The only route open to a client with no explosive is `Vehicle.AutoDamage`, which decays an ABANDONED vehicle 7% every 2 s from 50 s after it empties — so it needs a vehicle to have been driven and left. No vehicle health moved by a point and no `Burning` flag was ever set, across all eight clients' captures. **Two throwing paths were fixed to get this far**, both found by this capability and both closed: **X-45** (the seat entry threw) and **X-47** (the helicopter input threw 309 times in 150 s). **Graded PARTIAL rather than FAIL, per V-D2:** the "survives" half is stronger than it has ever been — this is the first execution of the path at all, and it is now clean — and the two missing verbs are named rows rather than unmeasured claims. **Variance is recorded, not averaged:** six Combat runs produced four different verb sets, governed by spawn distance to the nearest vehicle (the X-22 class, one lane over). Report § 2.5. | [R5](../verdict-closure/phases/phase-r5-lane-a-verbs.md) |
| **B-12** | V5 — prefab authoring | **VOID** | `2026-08-19-phase-v5-closure.md:141` — "**Not needed.** … the phase ships with no authored-asset change." | — |
| **B-13** | V6 — two-client turret parity | **RUN 2026-08-25 — BLOCKED on X-30: no client can man a turret, so parity has nothing to compare** | `artifacts/lane-b/x25-torso-aim-02/observer-{a,b}-checkpoints.jsonl` (`vehicles[].turretYaw`): the field is recorded on both observers at all seven checkpoints and every turret is **unmanned** — `drivenVehicleId: 0` on all three clients. The yaws agree, and they agree because nothing is moving them, which grades nothing. Same root as B-7: `SeatRequestMessage` has zero production senders (**X-30**), and manning a turret is a reliable opcode rather than an `InputButtons` bit, so the programme set cannot express it. Recorded with its own artifact rather than folded into B-7. **X-30 IS CLOSED (2026-08-26, verdict-closure R2).** `ClientSeatRequester` sends `C_SEAT_REQUEST` and `ScriptedInputStep.seatToggle` lets a recorded programme raise the edge, so the capability this check was blocked on now exists. **That unblocks the check; it does not grade it** — what is still owed is a lane-B run with a programme that actually mounts, which is R1's. | [R1](../verdict-closure/phases/phase-r1-programme-set.md) |
| **B-14** | V7 — two-client grenade parity | **RUN 2026-08-25 — BLOCKED on X-31, same root as B-4 and recorded separately** | `artifacts/lane-b/x-grenade-01`: `explosionsAttached: true` on all three clients, `explosionsTotal: 0`, `weaponId` pinned at 1 (the rifle) across all five checkpoints. Parity between two clients needs one detonation on either; there are none, so this check has nothing to compare rather than a disagreement to report. The recorder is proven live by the `explosionsAttached` flag, so a detonation that HAD happened would be in the artifact — which is why this reads BLOCKED and not "instrumentation missing". **X-31** owns it.  **STILL BLOCKED 2026-08-27, same root as B-4 and recorded separately.** X-31 closed; the grenade is equipped and fired in `artifacts/lane-b/r1-grenade-03` and still produces no detonation, because the shot resolves as hitscan. Parity between two clients needs one detonation on either and there are none. Moves from X-31 to **X-42**. | X-42 |
| **B-15** | V7 — Profiler run behind criteria 8 and 9 | **RUN 2026-08-27 — UNGRADEABLE: it now has a harness, and neither of its two criteria has a run through it** | **The half this row named is answered.** `phase-v7-projectiles.md:555` recorded "no harness"; lane B now samples the allocator every frame and reports it per checkpoint (**X-33**), so a profiler run behind V7's criteria 8 and 9 has somewhere to land. **What is still missing is the run, and specifically a PROJECTILE one.** Criteria 8 and 9 are about projectile flight and its cost; the only lane-B sets on disk are `combat-*`, `grenade-*` and `smoke` (`tools/lane-b/`), and the 2026-08-27 combat runs carry no projectile load at all. The allocation instrument is also whole-frame, so it can say what a frame cost and not what a projectile cost — a projectile figure is a difference between a run with them and a run without, which is two runs nobody has taken. **Not graded from the R5 runs on purpose (V-D2).** Quoting 24,626–29,812 B/frame from an on-foot combat run against a projectile criterion would be a number attached to the wrong question. | [R5](../verdict-closure/phases/phase-r5-lane-a-verbs.md) |
| **B-16** | bandwidth per client (V7 criterion 8) | **CLOSED at 8 clients** (Phase 4, 2026-08-26) — re-opens as V9's row at 16 | **Measured, decomposed, and cross-checked.** `artifacts/lane-a/p4-clean-report.json` (schema `ironfront.loadharness/2`), 8 clients / 120 s / clean wire / seed 12345, 56 actors + 14 vehicles on Dustbowl, 8 of 8 held: **mean 2.53 KB/s, worst client 3.02 KB/s** at the DATAGRAM level — inside the 5, 8 and 12 KB/s budgets all three of which are in circulation (`2026-08-26-phase-4-measure.md` § 3.1). Split by opcode: `Snapshot` 0.89 KB/s, `VehicleSnapshot` 0.62 KB/s, **transport framing 0.88 KB/s — 34.68%, more than the whole vehicle stream and untouchable by any rung of the D5 ladder**. Vehicles cost **0.63 KB/s against a ~1.6 KB/s projection, 2.5x high**. Server-vs-client cross-check agrees at **-0.17%**. Two of the phase's five rows describe worlds this tree cannot build and are reported unrunnable rather than approximated; the projectile row is a **measured zero** because `HarnessBehavior` has no fire. The old 1.67 KB/s figure is **not comparable** — 16 players + 32 bots, and measured at the snapshot level, above the 34.68% of framing this includes. | 4 |
| **B-17** | server tick p99 (V7 criterion 9) | **CLOSED at 8 clients** (Phase 4, 2026-08-26) — re-opens as V9's row at 16 | **Measured from the server's own per-tick JSONL, sample size beside every percentile.** `artifacts/lane-a/p4-clean-ticks.jsonl`, same run and configuration as B-16. Loaded sample (>=1 connection), **n = 3,637**: p50 881 us, p95 1,259 us, **p99 1,502 us**, max 44,527 us, mean 903 us. All 3,960 ticks: p50 861, p99 1,497. **Against the 33,333 us budget at 30 Hz, p99 is 4.5% of it**, and **1 loaded tick of 3,637 (0.03%)** exceeded budget. Entry-size head-room measured alongside: mean **14.29 B** per actor entry against `InterestManager.MaxEntrySize` 23 B (**37.9% under**), 2.09 entries per snapshot, and **`entriesShed` 0 across the entire run**. Under projectile load it is still untaken — no synthetic client fires. `2026-08-26-phase-4-measure.md` §§ 4-5. | 4 |

**Group B: 16 open · 1 void.**

---

## 4. Group C — code follow-ups

| id | item (source) | status | evidence | closes in |
|---|---|---|---|---|
| **C-1** | `ServerProjectileBridge.AuthoritativeFlight` cutover | **CLOSED — DECIDED OFF** (Phase 5, 2026-08-26) | **The decision the row was open for has been taken, and the answer is off.** Not on a bad number: **two of the three required inputs could not be produced at all**, because `Ironfront.Net.LoadHarness`'s synthetic client cannot fire (**X-34** — `HarnessBehavior` is `Idle`/`Move`, every frame carries `InputButtons.None`). Phase 5 § 3 pre-committed that a missing input is a "no", not a judgment call. **The three inputs, with their numbers** (`reports/2026-08-26-phase-5-cutover-gate.md` § 3): damage accounting **0 hits producible** against a requirement of exactly 1 application per hit; tick p99 **1,502 µs** against 33,333 µs (n = 3,637) but measured with the stepper **inactive**, so it is the baseline the stepper's cost would be added to rather than a measurement of it; bandwidth **0 B/s** of projectile traffic against an 8,192 B/s budget, on a 2,590 B/s per-client baseline. 0 of 3 answer the question § 3 asks; generously, 1 of 3. **The prepared patch is NOT landed** — Phase 2 task 2e's delete-the-engine-call patch stays written, guarded and inert on the branch, so the next attempt starts from a patch rather than from a decision. **The default is now asserted, not described** (AC-2, which bound on either branch and was unmet): `Assets/Tests/EditMode/ProjectileAuthorityDefaultTests.cs` pins that the flag is off at subsystem registration **and** that at that default a dedicated server's engine still owns projectile damage while the library stepper does not — inverting `NetProjectileAuthority.Clear()` turns **both** RED, Unity exit 2, restored suite 76/76 (`reports/2026-08-26-phase-5-ac2-mutation-proof.txt`). Before this, `ProjectileDamageOwnershipTests` took `authoritativeFlight` as a *parameter* and so proved the partition function while saying nothing about which value ships. **Reopening condition — all four, any one failing keeps it off:** (1) X-34 closed, ≥ 100 recorded hits across ≥ 2 seeds; (2) with the flag on, **0** hits with 2 applications and **0** with 0; (3) tick p99 **< 33,333 µs** with the stepper genuinely active at 16 clients / 12 vehicles, n ≥ 3,000 loaded ticks (head-room from baseline: 31,831 µs); (4) per-client bandwidth **< 8,192 B/s** mean and worst, naming which of the three circulating budgets it graded against (head-room: 5,602 B/s mean, 5,098 B/s worst). **V7 § 6.1's table row is amended and § 6.1.2 records this**, per AC-5. Still guarded meanwhile: `ProjectileDamageOwnershipTests` proves engine and library are never both the owner in either configuration, and `ClientWiringGate` **G7** fails the build if any of the three engine-side sites loses its guard. *Original finding:* Bare auto-property at `Assets/Scripts/Net/Server/ServerProjectileBridge.cs:116` — no initializer, so default `false`; engine-side damage still runs at `Assembly-CSharp/Projectile.cs:203`, `ExplodingProjectile.cs:69` and `GrenadeProjectile.cs:127`. | closed |
| **C-2** | `ClientCombatState` instantiated by nothing | **CLOSED** (Phase 2, 2026-08-19) | `NetClientLocalCombatDriver` (`Assets/Scripts/Net/Client/`) is the one production owner: it constructs `ClientCombatState`, folds the local snapshot entry and `S_DEATH` into it, takes input away on death, draws the respawn countdown and sends `C_SPAWN_REQUEST` - a message the client had never sent, so `ServerMessageRouter.SpawnRequestsReceived` could only read zero. **Wired, not merely present:** `NetClientBootstrap.EnsureLocalCombatDriver` adds it in code (the `EnsureVehicleStage` precedent), because Phase 2 authors no scenes. *Original finding:* Declared `Ironfront.Net.Replication/Client/ClientCombatState.cs:34`. Every `new ClientCombatState` is a test (`ClientCombatTests.cs:85,119,…,409`; `ClientEventConsumptionTests.cs:153`; `ServerCombatAuthorityTests.cs:491`); every non-test mention is a doc comment (`ProtocolConstants.cs:127`, `ServerReloadPolicy.cs:20,82`, `ServerRespawnGate.cs:7`, `NetClientCombatPresenter.cs:185`). Corroborated by the live gate run in § 1. Scope: zero production instantiations across `Ironfront.Net.*/`, `Assets/Scripts/`, `tools/`. | 2 |
| **C-3** | `PlayerList` (0x4B) → named killfeed | **CLOSED** (Phase 2, 2026-08-19) | **Sender - closed:** `ServerTickLoop.EmitPlayerList()` broadcasts on join and on leave. **Subscriber - closed:** `NetClientCombatPresenter` folds `OnPlayerList` into the new `PlayerNameTable`, and its killfeed renders names. **Gate - added:** `WriterCoverageRunner` **G6** now grades every `ServerEventWriter.Write*` for a production caller, closing the asymmetry V3 recorded (the client half was reported every run; the server half had no gate). Observed RED on `WritePlayerList` before the fix. `OnPlayerList` retired from `KnownUnwiredEvents`, which is empty again. **Names are not usernames** - the game server never parses a `JoinTicket`, so the table carries `#<playerId>` from the transport; see `ServerPlayer.DisplayName`. *Original finding:* **Struct — closed:** `Ironfront.Net.Protocol/Messages/PlayerListMessage.cs:15` `PlayerListEntry`, `:48` codec (`SizeFor:70`/`Write:87`/`TryParse:124`/`NameOf:169`). **Router case — closed:** `Ironfront.Net.Replication/Client/ClientMessageRouter.cs:353`. **Sender — open:** `Ironfront.Net.Replication/Server/ServerEventWriter.cs:156` is the *only* `WritePlayerList` occurrence in the repo; zero callers across `Ironfront.Net.*/`, `Assets/Scripts/`, `tools/`, tests included. **The design over-states this row** ("no message struct, no router case, no caller"). Phase 2's planned shared-file PR against `Ironfront.Net.Protocol` + protocol-spec § 5 is mostly already done, and P-D8's `PROTOCOL_VERSION` question is likely moot since the opcode and codec already ship. | 2 |
| **C-4** | `ScoreUi` holds match state that does not run headless (V8 D9) | **CLOSED** (Phase 2, 2026-08-19) | State moved to the new `MatchScoreboard` (a plain class, so it exists headless); `ScoreUi` renders it and holds no match state. Its class doc no longer claims a D9 divergence. The two production callers moved with it (`Actor.Die`, `CapturePoint.SetOwner`). *Original finding:* Still owns `blueScore`/`redScore`/`blueFlags`/`redFlags`/`gameEnded` (`Assembly-CSharp/ScoreUi.cs:36-52`), `AddScore`→`Win` (`:83-113`), `AddFlag`→spawn-point-loss win (`:116-133`), `Win` (`:136-160`). Its own remark: "V10 D12 closes only the rendering half … This class still holds match state that does not run headless." **Ordering constraint holds:** this lands before **A-6**/**A-9** author `ScoreUi`'s text refs. | 2 |
| **C-5** | `GameManager`'s five loose booleans | VERIFIED-OPEN — **PARKED, re-affirmed 2026-08-28 (verdict-closure R6 task R6.4, decision V-D4). Nothing is owed: **P-D10** excludes it by name at `plan.md:41`, no phase in any track claims it, and no check is blocked on it — it is a tidiness item that was never in scope, not a deferral.** | `Assembly-CSharp/GameManager.cs:23 reverseMode`, `:25 assaultMode`, `:27 nightMode`, `:29 noVehicles`, plus `victoryPoints` — the set named at `2026-08-18-phase-v8-closure.md:68`. Excluded by `plan.md:41` (P-D10). Recorded; no phase owns it. | PARKED (P-D10) — re-affirmed in [R6](../verdict-closure/phases/phase-r6-human-pass-and-decisions.md) § 4 |
| **C-6** | capture-point minimap marker | **CLOSED** (Phase 2, 2026-08-19) | `MinimapUi.SetMarker(Transform, Color)` / `RemoveMarker(Transform)` plus the new `MinimapMarker` component; `CapturePoint.SetOwner` registers and recolours on every flip (idempotent by subject, so a capture does not stack icons). Falls back to `minimapSpawnPointPrefab` when `capturePointMarkerPrefab` is unauthored, since Phase 2 writes no prefabs. *Original finding:* `Assembly-CSharp/MinimapUi.cs` exposes exactly six public statics — `SelectedSpawnPoint():107`, `UpdateSpawnPointButtons():131,152`, `PinToLoadoutScreen():193`, `PinToIngameScreen():202`, `AddActorBlip(Actor):211` — none a capture-point marker; serialized prefabs are `minimapSpawnPointPrefab:26` and `actorBlipPrefab:28` only. Scope: zero marker API across `MinimapUi.cs`, `NetClientObjectivePresenter.cs`, `CapturePoint.cs`. | 2 |
| **C-7** | scorch `DecalType` | **CLOSED** (Phase 2, 2026-08-19) | `DecalManager.DecalType.Scorch = 3`, appended not inserted (the value indexes `decalDrawers`). `NetClientExplosionPresenter` draws blasts with it. `InitMeshes` and `AddDecal` tolerate an unauthored fourth drawer - warn once, fall back to `Impact` - rather than throwing `IndexOutOfRange` out of `StartGame`. *Original finding:* `DecalManager.DecalType` still has exactly three members — `Impact = 0, BloodBlue = 1, BloodRed = 2` (`Assembly-CSharp/DecalManager.cs:8-13`). `NetClientExplosionPresenter.cs:183` still records "No scorch DecalType exists"; `:189` still draws the blast with `DecalType.Impact`. | 2 |
| **C-8** | per-bone ragdoll force hardcoded to `MainRigidbody()` | **CLOSED** (Phase 2, 2026-08-19) | `ActiveRaggy.RigidbodyForBone(HumanBodyBones)` and `Actor.ApplyRigidbodyForce(Vector3, HumanBodyBones)` / `KnockOver(Vector3, HumanBodyBones)`. Wired to the real signal: `NetClientCombatPresenter` maps the `S_DEATH` hitbox byte to a bone, so a headshot and a leg shot no longer throw the corpse identically. **V10 stated blocker was wrong** - it assumed the bone map depends on Editor rig naming, but resolving through the animator humanoid map normalises that. *Original finding:* `Assembly-CSharp/Actor.cs:677` is the only force application; `ActiveRaggy.MainRigidbody()` (`ActiveRaggy.cs:305`) the sole accessor with no per-bone overload; the other consumer `RemoteActorView.cs:102` also goes through it. Scope: `MainRigidbody` across all `*.cs`. | 2 |
| **C-9** | `World/VehicleLifecycle.cs` carries rotation as euler degrees | ALREADY-CLOSED | `Ironfront.Net.Replication/World/VehicleLifecycle.cs:57-58` now declares `public readonly float RotationX, RotationY, RotationZ, RotationW;` threaded through the ctor at `:60-72`, with the reason at `:32-36` ("Euler degrees, **which the seam carried before**, cannot be packed at all"). Feeds `Ironfront.Net.Protocol/Quantize.cs:231 PackQuat`. Zero `euler` remain in the file. | — |
| **C-10** | `Vehicle.Explode()` does no blast damage — `ExplosionKind.Vehicle` (V1-D5 → V4) | **CLOSED** (Phase 2, 2026-08-19) | **Decided: a wreck damages.** `Vehicle.Explode()` calls `ActorManager.Explode(..., ExplosionKind.Vehicle)` at its own configured radius (`wreckExplosion`, with code defaults so no prefab authoring is required). **Balance consequence, intended:** taking cover behind a burning vehicle is now dangerous. **A latent bug was fixed alongside it** - `ActorManager.Explode` vehicle loop index-walked the live `instance.vehicles` while `Vehicle.Damage` -> `Die()` -> `DropVehicle` can remove from it, skipping the next vehicle; it now iterates a snapshot. Not re-entrant: `Die()` reaches `Explode` through `Invoke(..., 0.3f)`, so a chain is sequential rather than nested. *Original finding:* `Assembly-CSharp/Vehicle.cs:796` `protected virtual void Explode()` is `WakeUp()` + `AddForce(…2000f)` + `AddTorque(…500f)` + three cosmetic calls. No damage call. `ActorManager.cs:379-381` still documents that it "**does not route back here** (D5)". `ExplosionKind.Vehicle` has zero producers: the three construction sites are `ExplodingProjectile.cs:70` (Rocket), `GrenadeProjectile.cs:129` (Grenade), `ServerProjectileBridge.cs:347-348` (Grenade\|Rocket only). **V4 did not take it** — `git show --stat ce69391` carries no `Vehicle.cs` blast change, and `ExplosionEventTests.cs:257` still reads `// V4 owns the caller` (see **X-4**). | 2 (P-D11) |
| **C-11** | `ExplosionKind.Environment` has no source (V1 → V7) | **CLOSED** (Phase 2, 2026-08-19) | **Decided: give it a source.** New `ExplosiveProp` component (fuel drum / gas cylinder) emits `ExplosionKind.Environment` through the same server-authoritative `ActorManager.Explode` path as every other blast. Wired at both ends: a bullet sets one off (`Projectile.Hit`, behind the same ownership guard as hitbox damage) and a blast chains into neighbours (`ActorManager.Explode`), with the prop fuse deferring the detonation so a chain never recurses. **Placing drums in a map is content work and no scene carries one yet** - the component is wired to its callers, not to a level. *Original finding:* Member at `Ironfront.Net.Protocol/Enums/GameplayEnums.cs:161`. Zero production producers — same three construction sites as C-10, and `ServerProjectileBridge.ExplosionKindFor` is `Grenade : Rocket`, V7's own mapper, so V7 added no Environment path. Only `Environment` references are tests: `ActorLifecycleMessageTests.cs:182`, `ExplosionEventTests.cs:258` (`// V7 owns the caller`, see **X-4**). | 2 (P-D12) |
| **C-12** | grenades and deployables never ballistically stepped | VERIFIED-OPEN — **PARKED, re-affirmed 2026-08-28 (verdict-closure R6 task R6.4, decision V-D4). Nothing is owed: the behaviour is deliberate and **pinned in both directions** — `ABouncingOrRigidbodyProjectileIsNotBallisticallyStepped` asserts Grenade/AmmoBag/Medipack are not stepped *and* that Rocket/Shell/GuidedMissile are, so a silent flip either way goes red. **P-D10** excludes it, and the pin is live.** | `ProjectileReviewFixTests.cs:85` `ABouncingOrRigidbodyProjectileIsNotBallisticallyStepped()` asserts `StepsKind(Grenade/AmmoBag/Medipack) == false` (`:92-94`), `Launch(Grenade,…) == 0` (`:96-99`), `LiveCount == 0` (`:100`), **and the companion direction** that `Rocket`/`Shell`/`GuidedMissile` are stepped (`:104-106`). Production matches at `ServerProjectileBridge.cs:180`. Out of scope by `plan.md:41` (P-D10). | PARKED (P-D10) — re-affirmed in [R6](../verdict-closure/phases/phase-r6-human-pass-and-decisions.md) § 4 |
| **C-13** | V3's `SeatInfo` shedding cost | ALREADY-CLOSED | Both preconditions from `2026-08-18-phase-v3-closure.md:254-256` are met. (a) `Ironfront.Net.Replication/Server/VehicleIdPool.cs:42`, ctor `:56`, wired at `ServerVehicleLifecycleSink.cs:37,45,49,53` and `ServerStateAudit.cs:189,206`. (b) `Assets/Scripts/Net/Server/NetServerActor.cs:312-314` `TryFindSeatOf(_actorId, out ushort vehicleId, out byte seatIndex)` feeding `SnapshotBuilder.Capture(…, vehicleId, seatIndex)` at `:316-328`. The benefit the 58→50 shedding ceiling was paid for is live. | — |
| **C-14** | phase-05 Task 6's `Actor.Damage` balance-parameter guard | ALREADY-CLOSED | Guard present and labelled: `Assembly-CSharp/Actor.cs:797` comment, `:813` signature, `:824` `bool ownsHealth = !NetContext.IsClient;`, gating health subtraction `:826-829` and the death branch `:842-846`. `balanceDamage` threads to `controller.ReceivedDamage(…)` `:825` and `:830-833`, gated on `!flag` rather than `ownsHealth` — local hit feedback by design per `:806-812`. Closes `2026-08-18-phase-v1-closure.md:57`. **Carry this caveat:** the guard is a two-way `IsClient` check writing `health` directly, **not** the three-way `NetRole` + `IActorDamageSink` route `phase-v1-explosions.md:53` describes — `NetRole` occurs zero times in `Actor.cs`. The effect holds; that sentence in V1-D2 is inaccurate and should be corrected rather than propagated. It also owes a pin (**X-6**). | — |
| **C-15** | `MatchController._capturePoints` | **VOID** | The item was "rebind after the type changes `Transform[]` → `CapturePoint[]`" (`phase-v8-objectives.md:62,109,381`). The type change never happened and cannot: `Net/Server/MatchController.cs:40` still reads `[SerializeField] private Transform[] _capturePoints`, and `Bindings/ICapturePointDirectory.cs:51-58` states why — `CapturePoint` compiles into `Assembly-CSharp`, which no asmdef may reference. D6's content ships through the seam. The field is alive and used (`MatchController.cs:189,198,213`); the *rebind task* is void, as recorded at `phase-v8-objectives.md:428` and `2026-08-18-phase-v8-closure.md:65`. | — |
| **C-16** | documentation drift in four places | **CLOSED** (Phase 2, 2026-08-19) | **And the drift was far wider than 2 of 8.** Every `#L` anchor in `docs/codebase-map.md` was re-resolved against the tree and most `Actor.cs` and `Weapon.cs` anchors were stale, not two - `Weapon.CanFire` 306->362, `Shoot` 321->377, `SpawnProjectile` 388->495, `Projectile.Travel` 92->153, `Actor.Damage` 813->861, `Actor.Die` 725->770, and the whole headless-call table. `phase-v7-projectiles.md:211` re-resolved too, and its `Invoke("Cleanup", 10f)` no longer exists in that shape at all (V7 task 8 replaced it). **Handoff:** nothing re-checks these, so they will drift again - an anchor-verification gate is the automation this row owes. *Original finding:* The four are named at `2026-08-18-phase-v10-closure.md:57`. **(1) `plans/00-shared/architecture.md:314` — closed:** `:310-318` now shows `IngameUi.Hit()`; `ShowHitmarker` has zero occurrences across `plans/`+`docs/` except the four that describe the drift. **(2) `phase-v7-projectiles.md:211` — still drifts:** cites `Explode()` at `:56-80` and `IngameUi.Hit()` at `:66`; actual `GrenadeProjectile.cs` has `Explode()` at `:108` and its only `IngameUi` call at `:134`. **(3) `phase-v8-objectives.md:91` — closed:** every anchor resolves. **(4) `docs/codebase-map.md` — still drifts, two of eight:** `:149` claims `ActorManager.Register(this)` at `Actor.cs:186` (blank line) and `:150` claims `IngameUi.instance.Show()` at `:225` (`}`); `:53-54` imprecise. **The design over-states this row.** | 2 |

**Group C: 12 open · 3 closed · 1 void.** → **After Phase 2 (2026-08-19): 2 open · 12 closed · 1 void · 1 prepared.** → **After Phase 5 (2026-08-26): 2 open · 13 closed · 1 void.** Still open: **C-5** and **C-12**, both excluded by P-D10. **C-1** closed as a DECISION — the flag was decided OFF because two of its three evidence inputs could not be produced at all (X-34); the prepared patch stays unlanded and the reopening condition is four numbers in the row.

---

## 5. Group D — claims made but never verified

| id | item (source) | status | evidence | closes in |
|---|---|---|---|---|
| **D-1** | `Weapon.Configuration.releaseDelay = 0.6f` is a guess never read from the throw clip | **CLOSED** (Phase 6 task 6.1, 2026-08-26) — **and the guess was wrong for both clips, not drifted** | **Authored per weapon, from each weapon's own clip.** `frag.prefab:71` and `spearhead.prefab:71` carry `releaseDelay: 0.952444`; `ammobox.prefab:88` and `medipack.prefab:88` carry `releaseDelay: 0.3186882`. **The last unknown resolved from YAML, not the Editor:** the owner decision of 2026-08-19 left "whether an Animator state `speed` multiplier rescales the raw clip time" to Phase 4 — it is `m_Speed: 1.3` on BOTH `Throw` states (`Old Frag.controller:249`, `Old Ammobox.controller:149`) with `m_SpeedParameterActive: 0`, so the multiplier is static and the authored value is the event time divided by it: `1.2381772 / 1.3 = 0.952444` (`frag_throw.anim:2249`) and `0.4142947 / 1.3 = 0.3186882` (`Ammobox Throw.anim:1430`). **Pinned by `ClientWiringGate` A9** (`AssetWiringDetectors.cs`, `ThrowReleaseDelayMatchesTheThrowClip`, registered at `AssetGateRunner.cs` in `Checks`), which walks prefab → Animator → controller → `Throw` state → clip and fails above 0.1 ms of divergence. **Mutation proof, 5 mutations / 5 REDs / 0 false greens** (`reports/2026-08-26-d1-mutation-proof.txt`): A — frag re-pinned to `0.6` → exit 1 expecting 0.9524440; B — ammobox re-pinned to the SAME `0.6` → exit 1 expecting 0.3186882, which is the pair that proves the rule reads each weapon's own clip rather than one hardcoded number; C — the line deleted → exit 1 on the absence clause, the state all four prefabs were in before this task; D — the state's speed made parameter-driven → **exit 2**; E — the clip's `SpawnThrowable` renamed → **exit 2**, because a chain it cannot resolve must not report either verdict. **The true-by-construction test is gone:** `OfflineBehaviourChangeTests.cs` fed `0.6f` to both sides of its own comparison; the replacement pins what a gate cannot see — the conversion is role-independent and the two weapons land on different ticks, 29 and 10 — and asserts `NotEqual` between them, the clause the old form could not express. **D7's divergence changed shape rather than closing**, from client-vs-server to offline-vs-server, and is now bounded by one sim tick in one direction (server later by 14.22 ms / 14.65 ms, never earlier, because `ThrowableWeapon.cs:42` uses `Mathf.Ceil`) — written into `plans/replication/phases/phase-v7-projectiles.md` § "D7 amendment" in the same commit, per AC-2. *Original finding:* a bare literal at `Weapon.cs:74`, consumed at `ThrowableWeapon.cs:42`, with nothing detecting the drift. | closed |
| **D-2** | ~~ten~~ **five** plan-named Unity tests for V7 are unwritten | **DECIDED 2026-08-28 (verdict-closure R6 task R6.3) — won't-do, five named, count corrected at source.** The V7 record's arithmetic now matches the repo and its reopening condition is keyed to the subject types. Nothing is owed unless that condition fires. | `phase-v7-projectiles.md:394-416` names **22** tests. Checked each by name repo-wide: **15 present verbatim** (`ProjectileTests.cs:58,94,118,155,255,306,349,419,476,500,563`; `DeployableTests.cs:44,87,255,309`); **2 present renamed** (`AProjectileSpawnRoundTripsAtTwentyBytes` → `PacketHexSampleTests.cs:472,486`; `OfflineProjectileBehaviourIsUnchanged…` → `OfflineBehaviourChangeTests.cs:40`, renamed because there were three changes not two). **5 genuinely unwritten**, all grenade/throw: `AGrenadeDetonatesOnTheSameTickOnBothSides`, `AGrenadeDetonationPositionComesFromTheServerNotThePrediction`, `AGrenadeAppliesItsBlastDamageExactlyOnce`, `AThrowReleasesOnTheSameTickOnServerAndClient`, `AClientSpawnThrowableSpawnsNothing` — zero coverage under any other name. **The V7 record was wrong in both directions and is corrected in place (2026-08-28):** its own arithmetic summed to **nine**, not the ten it claimed, and the *guided-missile pair* it named as missing does not exist to be missing — Task 10 names exactly **one** guided test and it is written, `AGuidedMissileReParameterizesWithTheSameId` at `ProjectileTests.cs:500`. Both the § 6.1 table row and § 6.1.1 now read five, name the five, and list the seventeen that exist so the 15 + 2 + 5 = 22 can be checked rather than taken. **Re-verified against the repo on 2026-08-28**, not inherited: each of the five returns zero hits repo-wide under any name; the two renamed are `PacketHexSampleTests.cs:472` + `:486` (split serialize/parse against one hex constant) and `OfflineBehaviourChangeTests.cs:42` — *`:42`, not the `:40` this row previously carried.* **The reopening condition needs no further work:** § 6.1.1's was already restated on 2026-08-26 against `Weapon` / `ThrowableWeapon` / `GrenadeProjectile` / `Projectile` leaving `Assembly-CSharp`, after the folder-keyed version fired on asmdef-seam C4 and its conclusion turned out false. R6 confirmed it still names the subjects; all four remain under `Assets/Scripts/Assembly-CSharp/`. **P-D9** stands. | [R6](../verdict-closure/phases/phase-r6-human-pass-and-decisions.md) |
| **D-3** | phase-05 Task 6's `Actor.Damage` guard was assumed, not re-verified | **CLOSED** (Phase 4 task 4.4, 2026-08-26) — re-verified against the file, not inherited | `Assembly-CSharp/Actor.cs:934` `bool ownsHealth = !Ironfront.Net.Unity.NetContext.IsClient;`, gating `health -= healthDamage` at `:936` and `Die()` + `ServerCombatEvents.ReportDeath` at `:952`. First re-verification since `2026-08-18-phase-v1-closure.md:57` recorded "assumed closed, not re-verified here". `2026-08-26-phase-4-measure.md` § 10. | — |
| **D-4** | V8's A5 CI-gate fix was recorded, not re-run | ALREADY-CLOSED | `tools/ci.ps1:130` `Start-Process -FilePath $env:UNITY_PATH -Wait -PassThru -NoNewWindow`; exit code read at `:136`; throw at `:143`. The gate can now go red; before the fix it structurally could not. Closes the "did not re-inspect `tools/ci.ps1`" caveat at `2026-08-18-phase-v8-closure.md:70`. | — |
| **D-5** | V8's elimination-by-spawn-point-loss was recorded, not re-run | ALREADY-CLOSED | Code present: `Ironfront.Net.Replication/Match/MatchStateMachine.cs:204` `SetSpawnPointCounts`, grace gate at `:403`, rule at `MatchRules.cs:78`, Unity caller at `MatchController.cs:259-266`. **Now pinned by five tests the V8 closure did not know about:** `ObjectiveAuthorityTests.cs:135,163,183,206,228`. Upgrades `2026-08-18-phase-v8-closure.md:69` from "CLOSED, not re-verified live" to "pinned at the library level". No Unity-level run exists; that is a Phase 3 item. | — |

**Group D: 2 open · 3 closed.**

---

## 6. Group E — ops checklist, round 8

Round 8 of `integration-checklist.md` is dated 2026-08-16. **`plans/unity-client/reports/2026-08-17-round8.md`
answers seven of its rows one day later** (summary table at `:573-584`), and the checklist was never
updated. That report, not the checklist, is the current state for A3, A4, A7, A11, A12, A13 and D2.

| id | item (source) | status | evidence | closes in |
|---|---|---|---|---|
| **E-1** | D1.1 — fix the build target | ALREADY-CLOSED — **and not by `c80c09e`** | All three sub-points closed. (a) `Assets/Editor/EditorBuild.cs:123-131` calls `SwitchActiveBuildTarget(BuildTargetGroup.Standalone, BuildTarget.StandaloneLinux64)` and fails loudly if it cannot, **before** the subtarget assignment at `:136`. (b) `-buildTarget Linux64` is passed at `tools/build-server.ps1:118`, carried unchanged through `c80c09e` and predating it. (c) "never restores it" is closed by `EditorBuild.cs:311-323 RestoreBuildTarget`, invoked at `:83` inside the `finally`. **Closing commit is `e7f61e3` (#117):** `git log -S SwitchActiveBuildTarget -- Assets/Editor/EditorBuild.cs` returns `e7f61e3` alone, and the file's whole history is `e7f61e3` + `355d994`. `c80c09e` fixed two *different* defects — the withheld `Microsoft.Bcl.AsyncInterfaces.dll` and `Start-Process -Wait`. | — |
| **E-2** | D1.2a — build the tarball and publish the release | ALREADY-CLOSED | Release `gameserver-v0.1.0`, published 2026-08-18T06:06:21Z, one asset named exactly `gameserver-linux.tar.gz`, 47,295,141 bytes, `sha256:fc438f23590d13221094520c8fd84c64464e67168c9ca98ff90382afec4dda71`. Filename matches what `.github/workflows/images.yml:153` globs on. The round-8 packaging complaint is also closed: `tools/build-server.ps1:44-46` declares `[string]$TarballPath = "build/gameserver-linux.tar.gz"`. | — |
| **E-3** | D1.2b — push the game-server image and report the digest | **CLOSED** (Phase 7, 2026-08-26) — closed on a digest, **not** on the correction below | **The digest, which is what closes this row:** `ghcr.io/nghaiz/ironfront-game-server@sha256:c310da19a065c3dd3a39417d8ede252aa0f9e3c03cabf0cdc3a4a494f9627a40` (tags `gameserver-v0.3.0`, `2026-08-26T09-22-15VN`; public; pulled anonymously to confirm), pushed by `images.yml` run `32922452961` off `develop` at `fa275d5`. **The gate ran BEFORE the push and passed:** `PASS: 27015/udp bound after ~6s` against the real ELF (BuildID `2bbf639e92710735622ddf4a977f318b4121593f`) on a Linux kernel. **And the digest was proved to hold that same ELF rather than assumed to** — the image was pulled by digest and the executable inside compared byte-for-byte (`sha256:61c388a1cd661a6230c857fd66de0cec86a1ec497d5f6df1b7f1d585263be19f`, identical), then RUN, logging `[net] server up on UDP :27015, 16 connections` with `00000000:6987` bound in `/proc/net/udp`. *Chain of custody:* local tarball `sha256:92743cb2…` == the release asset digest GitHub stored == what the workflow consumed. **— THE ORIGINAL EVIDENCE OF THIS ROW WAS WRONG, and it is kept here rather than deleted. —** The row concluded *"no game-server image exists"* from `gh api users/Sagitoaz/packages/container/ironfront-gameserver/versions` → 404. **It queried the wrong account.** An image existed under `nghaiz` the whole time (`sha256:31215cfe…`, tag `gameserver-v0.1.0`, private) — the 404 was a real absence *in the namespace searched* and said nothing about the one that mattered. The lesson is not "that image was fine": it was built from a server that binds no UDP port (F2, closed by `2b7ac41`), so pushing it would have produced a deployment that could never have worked. The lesson is that **a negative result is a claim about the search, not about the world** — query `user/packages` as the authenticated user. Three later claims on this row also died and are recorded as dead: *blocked on CI billing* (public repo, free minutes), *`permission_denied` on the GHCR namespace* (resolved by renaming the target to `ironfront-game-server`, which the workflow then created and therefore owns — `repository: Nghaiz/LTM`), and *no repo admin* (`ADMIN` since the 2026-08-21 transfer). The abandoned hand-created `ironfront-gameserver` (no hyphen, `repository: null`, private) still exists and is still referenced by nothing. Report: `reports/2026-08-26-phase-7-ops-to-digest.md` | closed |
| **E-3a** | 2 of 18 weapon configs are class-derived placeholders — `WRENCH(16)`, `SUPER WRENCH(17)` | **WON'T-DO** (Phase 7, 2026-08-26) — reasoned, not deferred | Surfaced in every shipped build's startup log now that `NetLog` has a subscriber: `[weapons] 2 of 18 weapon configs are class-derived PLACEHOLDERS, not registry values: WRENCH(16) SUPER WRENCH(17)`. **Phase 7 § 6 called this "a content gap, not a netcode one". Both halves are wrong.** (a) *The content is not missing* — `Ironfront.Net.Replication/Combat/WeaponCatalog.cs:262-266` carries the real numbers: WRENCH 60 damage / 150 balance / 300 force, SUPER_WRENCH 200 / 200 / 2000, both 3 m over a 0.15 s swing. (b) *It is a netcode gap* — the catalog models a **hitscan shot**, and a swing is not one. Writing 60 damage at 3 m range into it would let `ServerFireResolver` resolve a wrench as a very short rifle, which is a worse defect than the one it would close. So both entries stay `Inert` and stay UNAUTHORED **on purpose**, so that `DescribeUnauthored` keeps naming them at every startup — the warning is the designed mechanism, not noise. **What was actually missing** is that this decision lived only in a code comment, where no ledger reader would ever meet it; this row is that fix. **Reopen if** the catalog gains a melee/swing model — at that point the numbers above are ready and the two entries become authorable in one edit. Report: `reports/2026-08-26-phase-7-ops-to-digest.md` § 4 | closed |
| **E-4** | D2 — sign off on `Assets/Editor/EditorBuild.cs` | **DECIDED** | **Keep it where it is; signed off.** `2026-08-17-round8.md:578`. The gating condition is satisfied in today's tree: `EditorBuild.cs:70-84` is the `finally` — `RestoreDefines(previousDefines)` at `:76` and `RestoreBuildTarget(...)` at `:83` under `if (!Application.isBatchMode)`; `EditorApplication.Exit` moved outside the try to `:91`. Confirmed by the owner 2026-08-19. | — |
| **E-5** | A3 — re-run the shadow comparison | **VOID** | Closed 2026-08-14, three days *before* the round-8 checklist that still calls it "🔴 blocks A4". `plans/unity-client/reports/2026-08-14-a3-shadow-rerun.md:8` — "**Passed — A3 closed; A4 unblocked**", harness line `worstH=0.0076m … threshold=0.010m`, zero grounded warnings across the requested walk↔sprint transitions (`:20-30`). Round 8 caught its own staleness: `2026-08-17-round8.md:23,574` — "Strike it from round 9." The only thing A3 gated was A4, itself closed (**A-13**) in the clock-disabled state A3 existed to protect. **Phase 0 Task 0.2's question is answered: no re-run is required.** | — |
| **E-6** | A7 — can a player reach past ±2048 m? | **CLOSED** (Phase 6 task 6.2, 2026-08-26) — **and the mechanism was not the one the row names** | **The ±2048 m is the WIRE's range, not a wall.** `Quantize.PackPos` (`Ironfront.Net.Protocol/Quantize.cs:88`) clamps with `Clamp01((v - POS_MIN) / POS_RANGE)` before it packs, so a body past it is still simulated by the server at its true position while every snapshot pins it to the boundary — no exception, no counter, nothing in a log. That is the "silent permanent rubber-band", and it is an encoder behaviour rather than a physics one. Pinned directly by `PlayVolumeTests.PositionQuantizationClampsSilentlyBeyondTheWireRange`, which asserts two positions 500 m apart pack to the same code. **Production caller:** `Vehicle.cs:278` calls `KeepInsideLevelBounds()` from `FixedUpdate`, which at `:303` guards on `NetContext.IsServer` and at `:305` calls `LevelBounds.ClampInside` (`LevelBounds.cs:81`, delegating to `PlayVolume.TryClamp`). **The written decision — clamp, server-side, counted.** *Damage* kills a vehicle for crossing a line the player cannot see and turns a replication fault into a combat outcome; *authoritative teleport* is a discontinuity every observer renders as a warp and needs a wire teleport bit so interpolation does not sweep the body across the map — a protocol change for a boundary case. *Clamping* is the least intervention that makes the wire honest, and `LevelBounds.ClampCount` (`LevelBounds.cs:56`) turns the crossing into a counted event. The outward velocity component is zeroed with the position (`Vehicle.cs:310-314`), per axis, so a body at the edge does not re-cross every physics step and still slides ALONG the face. **Stated cost, because it is a gameplay change:** Dustbowl's box is 700 m tall centred at y = 207.6, so helicopters now have a ceiling near y = 557.6 and invisible walls near ±850/±800 m. That was always true of the wire; it now happens where the server can see it. **The clamp is only sufficient because the authored box fits the wire, and that is now asserted too:** Dustbowl's `LevelBounds` sits at `Scenes/Dustbowl.unity:65260-65261` — centre `{-70.78, 207.57, -88.63}`, `m_LocalScale: {1700, 700, 1600}` — roughly ±920 m at its widest, comfortably inside ±2048 — checked by `PlayVolume.FitsOnTheWire` at `LevelBounds.cs:111` (a `Debug.LogError` at scene load) and by `PlayVolumeTests.DustbowlsAuthoredVolumeFitsOnTheWire`. Widening the volume past 2048 would otherwise reintroduce the exact silent divergence with every other check still green. **Arithmetic moved to where tests reach it:** `Ironfront.Net.Replication/World/PlayVolume.cs`, 8 tests, because `LevelBounds` is `Assembly-CSharp` and untestable (**E-11b**). **The call itself is pinned by `ClientWiringGate` G9** (`ClientWiringDetectors.cs`, `FindUnpinnedLevelBoundsCall`), **mutation-proved 4 mutations / 4 REDs / 0 false greens** (`reports/2026-08-26-e6-mutation-proof.txt`): A — the call removed → exit 1 at `:301`, which is the state the tree was in for the whole of V5; B — the server guard removed → exit 1 at `:305`, a clause A stays green through; C — the method renamed → exit 1; D — the rule's scope re-pointed at a non-existent file → **exit 2**, the completeness guard in `Program.cs`, because an absence rule that scanned nothing has not passed. *Original finding:* `2026-08-17-round8.md:579` — "**YES** — two respawning helicopters, `LevelBounds.IsInside` has **zero callers**, no altitude clamp." | closed |
| **E-7** | A11 — master-link plugin DLLs | ALREADY-CLOSED | Both DLLs are **git-tracked**, not merely on disk: `git ls-files Ironfront_Reborn/Assets/Plugins/` lists `Ironfront.MasterClient.dll`(+`.meta`) and `Ironfront.Net.MasterLink.dll`(+`.meta`), beside the other four libraries, last rebuilt into `538c4c7`. Run confirmed at `2026-08-17-round8.md:577` — "**Done.** 0 warnings / 0 errors, 6+6 copied, 1064/1064 tests green." The gotcha the checklist flags at `:226-231` is closed too: the drift check's glob is widened to `Ironfront.*.dll` at `.github/workflows/ci.yml:216`, which matches `Ironfront.MasterClient.dll` (the old `Ironfront.Net.*.dll` never did). | — |
| **E-8** | A12 — server CPU percentage | **DECIDED** | **Leave `cpuPercent` at −1.** `2026-08-17-round8.md:580` — "Checked the alternatives; a container reads the host's CPU." Rationale on record at `integration-checklist.md:239-243`: Unity exposes no portable process-CPU counter, and a fabricated number on a matchmaking input is worse than an absent one because the master acts on it. Confirmed by the owner 2026-08-19. **Exposes a live follow-up this decision does not cover** → **X-7**. | — |
| **E-9** | A13 — who owns the kill/death tally | **DECIDED** | **The server/protocol track owns it.** `2026-08-17-round8.md:581` answers the checklist's "yours or mine" with "**Yours.**", and names the resolve point: `ServerTickLoop.cs:497` `EmitDeath`. Confirmed by the owner 2026-08-19. **Two facts the implementation still needs**, recorded here so Phase 2 does not rediscover them: (1) the actorId→playerId mapping is unreachable — `IMatchReporter.cs:33` wants a `playerId`, `EmitDeath` holds an `actorId`, and the dictionary is `private` at `NetServerBootstrap.cs:79` with no accessor; (2) **bots have no `playerId` at all**, so whether bot kills are tallied, dropped, or given synthetic ids determines the accessor's signature. Until then `ServerMasterReporter.CollectScores` keeps returning an empty list, the deliberate choice over all-zero rows (`integration-checklist.md:251-254`). | 2 |
| **E-10** | S5 / A9 — bot LOD profiling | ALREADY-CLOSED | Measured with exactly the pair the amended checklist asked for: `plans/unity-client/reports/2026-08-18-round9.md:245-270,484` — AI cost above the `AlwaysOff` floor **0.951 ms/frame (`AlwaysOn`) vs 0.775 (`Scheduler`)** = 18.5 % saved while skipping 38.6 % of bot-ticks (`granted=14370 skipped=9030`), at 40 bots. The seam it needed exists: `Assets/Scripts/Net/Server/BotLodGate.cs`. Tick p99 is flat across arms by construction, because `AiActorController` runs in `Update` and no `ServerTickLoop` stage contains it. | — |
| **E-11** | F — the asmdef split | **SPLIT, and the layering half is CLOSED** (2026-08-21) — the remainder is renamed **E-11b** below | The row asked for asmdefs to stop client code calling server code. Two things were wrong with that. **(a) It is not a risk, it is fifteen files.** A grep for `Ironfront.Net.Unity.Server` outside comments under `Assets/Scripts/` returns fifteen: two `NetBindings/` (by design), eleven legacy gameplay, one diagnostics harness — and `Net/Client/NetClientObjectivePresenter.cs:135`, shipped client code reading `NetServerBindings.CapturePoints` since V8. **(b) An asmdef could not have fixed it.** `NetBindings` must live in `Assembly-CSharp` (it is the only assembly that sees both halves), so `Assembly-CSharp` must reference the server assembly, so all 333 legacy files may call in; `autoReferenced: false` closes that and kills `NetBindings` with it. **Closed by a gate instead:** `tools/check-net-layering.ps1`, wired into `tools/ci.ps1` and `.github/workflows/ci.yml` in the same commit, with a named baseline carrying the companion assertions `pinned-baseline-test-companion.md` requires, and `Net/Client` / `Net/Input` given no baseline at all. The client presenter was **fixed, not baselined**: `ICapturePointDirectory` moved to `Ironfront.Net.Unity.Shared` as `NetSceneBindings`. Mutation proof: `2026-08-21-layering-gate-mutation-proof.txt`. | closed |
| **E-11b** | the asmdef split itself, for TESTABILITY | **CLOSED** (2026-08-26, asmdef-seam C4c) — **and it still does NOT unlock P-D6 or P-D9** | What an asmdef still buys is an EditMode test assembly that can reference `Net/Client`'s 25 files (`Reconciler`, `RemoteActorRegistry`, the killfeed model), which today has no test path at all. **That cost is now dated and concrete:** the X-17 fix on 2026-08-22 landed in `RemoteActorRegistry.OnSpawn` with no unit test possible, and had to be verified by a full three-client lane-B run instead — minutes per verification where a test would have been seconds. It buys nothing for **P-D6** (`plan.md:37` — the gate must read prefab YAML and legacy source) or **P-D9** (`plan.md:40` — ten V7 tests exercise `Weapon` / `ThrowableWeapon`, legacy MonoBehaviours no asmdef move touches). Both stay closed regardless. Measured cost, 2026-08-21, as legacy types each folder names: `Net/Input` **9**, `Net/Diagnostics` **11**, `Net/Client` **26** — each one needs the seam extraction `Net/Server` already had, so this is a strangler in that order, not four JSON files. `Net/Headless` was **0** and was folded into Shared rather than given an asmdef. **CLOSED 2026-08-26 by [`plans/asmdef-seam/phases/phase-c4-net-client.md`](../asmdef-seam/phases/phase-c4-net-client.md) C4c.** The thing this row asked for exists and is checkable by path, not by claim: `Ironfront_Reborn/Assets/Scripts/Net/Client/Ironfront.Net.Unity.Client.asmdef` (the assembly) and `Ironfront_Reborn/Assets/Tests/EditMode/Client/Ironfront.Net.Unity.Client.Tests.asmdef`, whose `references` name `Ironfront.Net.Unity.Client` — the reference that could not be written before C4 — carrying **6** `[Test]` methods in `NetClientSeamTests.cs`. The load-bearing one pins finding **A16**. **The measured-cost sentence above is now paid rather than merely dated:** an `RemoteActorRegistry`-shaped fix has a test path that does not require a three-client lane-B run. **Two things this closure deliberately does not claim.** (a) `Net/Client` still ships `autoReferenced: true`, so `Assembly-CSharp` may still reach into it; flipping it is [`phase-c5-autoreferenced.md`](../asmdef-seam/phases/phase-c5-autoreferenced.md) and is a separate row-free step. (b) P-D9's ten V7 tests are **still** unwritable and the reopening condition recorded for them in `phase-v7-projectiles.md` § 6.1.1 fired on this asmdef while its conclusion is false — corrected in the same commit as this row. | closed |

**Group E: 3 open · 4 closed · 1 void · 3 decided.**

---

## 7. Found during Phase 0 — items no group covers

These came out of the verification and are on no list today. They are recorded here rather than
left for a later phase to rediscover.

| id | item | status | evidence | closes in |
|---|---|---|---|---|
| **X-1** | **No client presenter is instantiated anywhere.** Rows A-1, A-5, A-7 and A-12 are four symptoms of one missing scene pass | **CLOSED** (Phase 1, 2026-08-19) — **and the row's own count was wrong** | Closed as one scene pass: `NetClientProjectilePresenter`, `NetClientExplosionPresenter`, `NetClientCombatPresenter`, `NetClientObjectivePresenter` and `CosmeticTracerPool` added to the `NetClient` object (&629676520) in `Dustbowl.unity`, beside the bootstrap they resolve through. All five instantiate in Play Mode. **Correction — five of the nine guids were not authoring debt.** The row was built from "zero guid references across `Assets/**`", which is the right query for a `MonoBehaviour` and meaningless for anything else. `NetClientPresenterGuard` is a `public static class` and `ClientTurretDirectory` an `internal sealed class` constructed in code at `ClientVehicleStage.cs:143` — neither can sit on a GameObject, so zero references was their **correct** state, and a detector demanding otherwise could only ever be red. `RemoteActorView` and `LobbyShellOverlay` are components but belong to the remote-actor prefab (**A-2**) and the lobby scene (**X-5**), not here. X-1's real content was **four presenters plus the tracer pool**. **Pinned by** `AssetWiringDetectors.PresentersAreOnTheClientObject`, which checks same-GameObject rather than same-scene — a presenter parked on a sibling satisfies a scene-wide check and resolves nothing. *Original finding:* nine scripts return **zero** references across `Ironfront_Reborn/Assets/**`, `ProjectSettings/**`, `Packages/**`: `NetClientCombatPresenter` (`bc6c11e3…`), `NetClientExplosionPresenter` (`db9e5295…`), `NetClientObjectivePresenter` (`b05689a5…`), `NetClientProjectilePresenter` (`feedb881…`), `NetClientPresenterGuard` (`cb7a6682…`), `CosmeticTracerPool` (`188a2915…`), `ClientTurretDirectory` (`1b93d860…`), `RemoteActorView` (`076337bd…`), `LobbyShellOverlay` (`3a9b8660…`). **Phase 1 should treat "put the presenters on the client bootstrap object in `Dustbowl.unity`" as one task, not four.** | 1 |
| **X-2** | The **server** projectile catalog is unwired too | **CLOSED** (Phase 1, 2026-08-19) | `ProjectileCatalogInstaller` added to the `NetServerBootstrap` object in `Dustbowl.unity` and its `_prefabsByKind` authored with the same seven prefabs as **A-1**, so both roles simulate from one authored source. With A-1 and this closed, **A-10**'s sampled `damageDropOff` curves finally reach a live `ProjectileConfig` — verified: Shell/Rocket/GuidedMissile/Bullet each carry a 32-sample dropoff table, and Grenade/AmmoBag/Medipack an empty one (the keyless-curve path `ProjectileCatalogBuilder.Sample` takes by design, not 32 zeroes). **Pinned by** `AssetWiringDetectors.ProjectileCatalogInstallerIsWired`, which fires on any scene carrying a server bootstrap. *Original finding:* `Assets/Scripts/Assembly-CSharp/ProjectileCatalogInstaller.cs:35`, guid `1e1d8de547d73f847a33a9a802368cbe`, has zero refs across `Assets/**`. Group A names only the client array (**A-1**); this is its server sibling, with the same fix shape, and appears on no list. Together with **A-1** it is why **A-10**'s sampled curves reach nothing. | 1 |
| **X-3** | **An honest second client cannot be scripted yet** — this blocks Phase 3 | **CLOSED** (Phase 3C, 2026-08-20) | **Not a wire-format change: `PROTOCOL_VERSION` is still 3 and `PacketHexSampleTests` is untouched.** `InputButtons` has declared `Fire`/`Aim`/`Reload` since phase-01 and `ServerCombatAuthority.cs:170,178` plus `MountedWeaponAuthority.cs:127,132` have read them since phase-05; what was missing was a client that could set them. `MoveInput` now carries the three bits and `MoveInput.ToButtons()` is the ONE mask builder — `ClientPredictionStage`'s private duplicate, which knew only Jump/Sprint/Crouch, is deleted. The intent is installed into the tick loop by `FpsActorController.InstallNetworkCombatIntent()`, pushed down rather than pulled up because `Ironfront.Net.Unity.Shared` declares no references and is what the dedicated server builds on. `C_ACK_BASELINE` gains its first sender (`BaselineAckPolicy` + the subscription in `NetClientBootstrap.OnSnapshotApplied`), which means the delta encoder produces a delta against a real client for the first time — before this, `DeltaEncoder.TryFindBaseline` returned false on every call and every snapshot every client ever received was FULL. **One thing the phase file did not ask for and the phase did anyway:** the sender hard-coded `0f` pitch, which `ServerCombatAuthority.AimDirection`/`ShotOrigin` both read — so a working trigger would still have sent every shot out perfectly level. The pitch now comes off `NetPredictionClock.AimPitchDegrees`, sampled with the tick. **Pinned by** `ClientInputSenderTests` (22 assertions; the Unity half by Roslyn source scan, since no gate here compiles Unity code), each of the four §5 pins proved red by mutating the shipped artifact — 19 mutations, all red, recorded in `reports/2026-08-20-phase-3c-client-input.md`. **Two of the original three claims were false on the day the row was written:** the server COULD act on Fire/Reload, and `NetClientLocalCombatDriver.cs:147` DID send `C_SPAWN_REQUEST`. Only the ack gap was real. *Original finding:* The Unity client sends exactly two message types: `ClientMessageType.Input` (`Assets/Scripts/Net/Client/ClientPredictionStage.cs:120`) and `ClientMessageType.VehicleInput` (`ClientVehicleStage.cs:337`). (1) `MoveInput` carries `MoveX`, `MoveZ`, `YawDegrees`, `Jump`, `Sprint`, `Crouch` and **no Fire or Reload** (`Ironfront.Net.Replication/Movement/MoveInput.cs:16-40`). (2) Nothing sends `C_SPAWN_REQUEST` — every `SpawnRequest` hit is the server receive side (`ServerMessageRouter.cs:150-157`, `ISpawnRequestHandler.cs:20`, `ServerTickLoop.cs:1084`). (3) Nothing sends `C_ACK_BASELINE` — `AckBaselineMessage` has exactly one non-test caller, the server's parse at `ServerMessageRouter.cs:127`. This is why round 9 drove the server directly instead of following the E-row instructions literally (`2026-08-18-round9.md:39-47`). **Owner decision (2026-08-19): a first-class Phase 2 item with an explicit blocks-Phase-3 edge.** | 3C |
| **X-8** | `Chat`, `LoadoutSelect` and `Ping` have no client sender either | **CLOSED as a gate** (Phase 6 task 6.5, 2026-08-26) — **and the row's premise was wrong** | **The server does NOT route these three.** `ServerMessageRouter.Route` (`Ironfront.Net.Replication/Server/ServerMessageRouter.cs:119-196`) has cases for `Input`, `AckBaseline`, `SpawnRequest`, `SeatRequest` and `VehicleInput` only; `Chat`, `LoadoutSelect` and `Ping` fall to `default: UnknownMessages++`. So the phase plan's framing ("a `ClientMessageType` the server routes and nothing on the client ever writes") does not describe them, and shipping the senders AC-6 asks for would have added three **write-only** paths the server counts as corruption — strictly worse than the current state. **Scope confirmed with the owner: gate only, senders deferred.** **What shipped is `ClientWiringGate` G10** (`tools/ClientWiringGate/ClientSenderCoverageRunner.cs`), the third direction — G1 grades the client's inbound half, G6 the server's outbound half, and nothing graded the client's OUTBOUND half, which is where opcodes have actually been going missing. **Population is every declared enum member via `Enum.GetNames`, not the routed subset** — grading only the routed set would have dropped all three of these rows silently, and it is what surfaced **X-30**. The three are named exemptions in `KnownUnsentMessages` with the reason and the work that retires each, reported as a `KNOWN GAP` line on every run rather than being silent. **Scanned roots stop at `Assets/Scripts`**, so `Assets/Editor/NetVerificationHarness.cs:372` — which does send `SpawnRequest` — cannot mark it covered; that is the **X-10** lesson in gate form, and it is proved rather than asserted (mutation E). **Mutation-proved 5 mutations / 5 REDs / 0 false greens** (`reports/2026-08-26-x8-mutation-proof.txt`): A — a covered opcode loses its sender; B — SeatRequest's exemption deleted, which is the proof the gate discriminates on it TODAY; C — an exempted opcode gains a sender → hard fail on the stale exemption, so the list cannot become a graveyard; D — a new `ClientMessageType` added → graded automatically, the "stops the fourth one sitting uncalled for four phases" property § 6 asked for; E — the Editor harness cannot substitute for the shipped client. Red paths also run on every `dotnet test` (`ClientSenderCoverageGateTests`, 7 tests). **Live state: 4 of 8 covered** — `Input`, `VehicleInput`, `SpawnRequest`, `AckBaseline`. *Original finding:* split out of **X-3** by Phase 3C rather than silently absorbed into its closure. | senders: unrouted, deferred |
| **X-14** | **A networked human cannot change weapon server-side** | VERIFIED-OPEN — **PARKED, re-affirmed 2026-08-28 (verdict-closure R6 task R6.4, decision V-D4). Nothing is owed here because neither half is expressible as a `.cs` change: closing it needs client-side prediction of the switch or it lags a round trip, and a UI story for the rejected case — two product decisions. Its wire half is not unguarded: `ClientMessageType.LoadoutSelect` is a named exemption in `ClientSenderCoverageRunner.KnownUnsentMessages` citing this row, so the opcode is reported on every CI run and the entry hard-fails if a sender lands without this row being reconsidered.** Previously reviewed by Phase 6 task 6.7 and deliberately left open, with the reason recorded. | **Not closed, and not for want of an owner.** Phase 6 § 7a claimed this row so it would stop being ownerless debt; having read it, the deferral in the original finding is correct and is re-affirmed rather than overridden. Closing it means routing a human's weapon switch through the server, which needs (a) client-side prediction or the switch lags a round trip, and (b) a UI story for the rejected case — both product decisions, neither expressible as a `.cs` change this phase could make honestly. **What phase 6 did add is its other half's gate.** `C_LOADOUT_SELECT` is **X-8**, and `ClientMessageType.LoadoutSelect` is now a named exemption in `ClientSenderCoverageRunner.KnownUnsentMessages` citing THIS row — so the opcode is reported on every CI run instead of being invisible, and the entry hard-fails the build if a sender ever lands without this row being reconsidered. **Next step, named:** decide the prediction and rejection story; the wire bits already exist (`InputButtons.SwitchWeapon0..3`, bits 11-14, with a producer and a consumer). *Original finding:* the producer is the scripted source only; `LocalInputSource` returns nothing for those bits. | PARKED — re-affirmed in [R6](../verdict-closure/phases/phase-r6-human-pass-and-decisions.md) § 4 |
| **X-15** | Every networked player integrated **DETACHED** and free-fell out of the world, so every shot originated from a ghost hundreds of metres below the map | **CLOSED** (2026-08-22) | Root cause: `ServerPlayer.Tick` branches on `Actor.Movement`, `agent == null` takes `MoveDetached` (`State.Position + motion`, **no collision**) — a branch documented as covering a body that has not spawned *yet*, which for a networked player never ended. `NetMovementAgent` is authored on `Prefab/Player Fps Actor.prefab` and nowhere else; a claimed body is `ActorManager.actorPrefab`. **Fix:** `NetServerActor.AttachMovementAgent()`, called from `IronfrontNetBindings.CreatePlayerBody` — the narrowest of the three candidates. **Not** authored onto the AI prefab, because every bot uses it and is driven by `AiActorController`; that would put a second driver on every character in the game. `ServerPlayer` also warns once after 30 detached ticks, so the branch's "yet" is now checked. **Green:** shot origin `y = -353 → -448 falling` became `y = 33.6` on the ground, nearest target `704 m` became `37.7 m` (`reports/2026-08-22-x15-fixed-and-the-geometry.txt`). | closed |
| **X-17** | A remote player's PROXY is spawned and never positioned, so every client draws other players at the object pool's parking spot | **CLOSED** (2026-08-22) — and the previous reading of this row, *"a harness problem, not an engine one"*, was **wrong** | The 2139 m in the old title is not a distance between two players. It is the distance from the shooter to **(0, 2000, 0)**, where `RemoteActorRegistry` parks a pooled body — measured at all seven checkpoints of `artifacts/lane-b/x17-measure-01` while the victim's own record put it at (1088.1, 103.1, 954.3). Reading it off `pitch` alone misleads: pitch is POSITIVE-IS-DOWN (`ScriptedAim`), so -42.6 deg reads as *below* the map under the ordinary sign and *above* it under the real one. **Nothing was wrong with the wire.** When the snapshot carried actor 42 it carried (1088.11, 103.41, 954.30) — the truth to the centimetre. **Root cause:** `SnapshotInterpolator.TryLerpPosition` needs the actor in BOTH endpoints, and interest culling REMOVES a body past `InterestManager.CullRadius` (500 m) from the accumulated world, while `ServerTickLoop.AnnounceNewActors` announces EVERY actor to every client regardless of interest. An actor that is announced but never replicated therefore has exactly one position ever sent to it — the one in `S_SPAWN_ACTOR` — and `RemoteActorRegistry.OnSpawn` **threw it away**. **Fix:** `OnSpawn` places the transform from `message.PosX/PosY/PosZ` and `message.Yaw`. **No unit test exists for it**: `Net/Client` has no asmdef, so `RemoteActorRegistry` compiles into `Assembly-CSharp`, which no test assembly can reference — a concrete, dated cost of **E-11b**. Evidence is the run: `reports/2026-08-22-x17-root-cause-and-fix.txt`. | closed |
| **X-18** | Every client was told the WRONG position for every pre-filled player slot body, and was never told again | **CLOSED** (2026-08-22) — found BY the X-17 fix, filed and fixed in its own commit per phase-3 §7 | `IronfrontNetBindings.CreatePlayerBody` does `Instantiate(manager.actorPrefab)` with **no position**, so all sixteen slot bodies (`ServerPlayerSlotPool`, 3A) sat on the prefab's authored spot near (0, 1000, 0) from startup. `AnnounceNewActors` announced them THERE — before any was claimed — and `MarkSpawnSent` fires once, so the real spawn point was never sent. Harmless inside interest range, where snapshots overwrite it every frame; outside `CullRadius` it is the only position the client has, which is the second half of X-17. **Fix:** `ServerTickLoop.IsAnnounceable` — an unclaimed player slot is not announced. Release-then-reclaim still re-announces (`ForgetActor` → `SpawnAckTracker.Forget` drops both directions). Bots unaffected: `AvailableForPlayers` is set only by the slot pool. **Mutation-proved**, two faults, two mutants, two reds, each caught by its own test and by no other — dropping the `AvailableForPlayers` qualifier hides all 55 bots while the skip test stays green (`reports/2026-08-22-x18-mutation-proof.txt`). **Green that means something:** at 518.9 m in `artifacts/lane-b/x18-fixed` the target is still culled (`inSnapshot: false`) and the proxy holds (1206.17, 31.84, 1762.31) — actor 42's real spawn point to the centimetre, where it would have read (0.03, 999.98, 0.03). | closed |
| **X-19** | Every shot passed OVER the target, because the client's body was moving with its collision turned OFF | **CLOSED** (2026-08-23) — and the 0.332 m is derived, not guessed | **The verdict first.** The shot log now prints `present.torso` (the pose the server holds) beside `frame.torso` (the pose the resolver judged against), plus the rewound tick and the shooter's `Movement.State.Position` beside its `transform.position`. Identical on all 481 shots of two runs, `frame.tick == wanted.tick` throughout: **the pose was not recorded low**. The disagreement is on the client and purely vertical — dX and dZ are zero to seven significant digits while dY is a third of a metre. **Root cause:** `NetMovementAgent.CharacterMove` writes the motion straight onto the transform when the CharacterController is disabled — no sweep, no floor, no flags — and then returns `transform.position` as though the move had resolved. On a client that branch was taken on **11,785 of 11,785 ticks**, all three clients, whole run (`artifacts/lane-b/x19-move`), because `FpsActorController.Start` calls `DisableInput()` (which clears `characterController.enabled`) and the only caller that sets it back is `FpsActorController.SpawnAt` — the gameplay spawn a networked body deliberately never runs (`Actor.cs`). **The magnitude, derived:** the server's grounded velocity −10 m/s packs to i8 −19 and unpacks to −9.5748; the reconciler's replay adds one airborne gravity tick to −9.9672; one client tick at 1/30 s asks for **−0.33224010 m** against a measured −0.33224110, and two ticks give −0.67756 — the −0.678 the 2026-08-22 report saw once and could not account for. **Fix:** `ClientPredictionStage` re-asserts the collision capsule ahead of the tick it protects, leaving a seated body alone; the legacy `FirstPersonController` is deliberately NOT disabled because its `Update` runs `RotateView()` and switching it off would take mouse look from the player. **Detector:** `CharacterMove` counts every uncollided move, clears the stale collision flags and names the first one; **mutation-proved**, three faults, three mutants, three reds, each caught by the test written for it — and a straight revert is caught by two of the three but NOT by the healthy-body test, which is why that test alone would have graded the original bug green (`reports/2026-08-23-x19-mutation-proof.txt`). **Green:** `CollisionBypassedMoves` 0 on every process of every post-fix run; dY from −0.332/−0.678 to +0.010..+0.046 (the 0.0625 m position quantum); driver corrections 2208 → 0. **Shots now enter hitboxes for the first time** — `occluded=20` of `resolved=30` in `x19-fixed-04`, against `occluded=0` across 260 pre-fix shots. The victim still survives, which is **X-20**. `reports/2026-08-23-x19-drawn-not-recorded.txt`, `-x19-mutation-proof.txt`, `-x19-lane-b-rerun.txt` | closed |
| **X-20** | Twenty shots that DID enter a hitbox were rejected by the world linecast | **CLOSED** (2026-08-25) — the occlusion question is answered; what it was hiding is filed as **X-24** and **X-25** | First run in which any shot reached a box at all: `artifacts/lane-b/x19-fixed-04`, weapon 1, 30 fired, `resolved=30 occluded=20 hits=0`, victim on 100 health. Pre-fix the same counter read `occluded=0` across 260 shots — which was never "no walls", because `ShotsOccluded` only increments AFTER the slab test has picked a winner. **Two readings and this run cannot separate them:** (1) the linecast is correct and there is geometry between the pair — they held at **10.1 m** against a programmed `holdDistanceMeters: 6.0`, and stopping 4 m short is what an obstacle looks like; (2) the linecast is wrong in the way the 2026-08-22 report hypothesised — the endpoint lands inside the victim's own capsule and mask `-2049` excludes only layer 11, so the victim's own collider can block the shot that hit it. Reading 2 was killed by `occluded=0` and is no longer killed; it is also not proven. **Next measurement, same shape as 3F.1:** print what the linecast actually hit. `reports/2026-08-23-x19-lane-b-rerun.txt`  **INSTRUMENTED 2026-08-25 — the measurement this row asked for now exists; the ANSWER still needs the run.** The row said "next measurement, same shape as 3F.1: print what the linecast actually hit", and nothing could, because `ServerTickLoop.IsOccluded` used `Physics.Linecast`'s **bool** overload and threw the hit away. It now uses the `out RaycastHit` overload — same query, same cost — and records `collider=<name> layer=<n> d=<hit>m of <ray>m frac=<f>`, which the shot log prints as `occlusionHit[...]` under the existing `IRONFRONT_LOG_SHOTS=1` gate. **How to read it:** the collider NAME is the discriminator. Terrain or a building names reading 1 (real cover, consistent with holding at 10.1 m against a programmed 6.0 m); the victim's own body collider names reading 2 (mask -2049 excludes only layer 11, so the victim can block the shot that hit it). `frac` corroborates — near 1.0 puts the blocker at the endpoint. The line states facts and no verdict, deliberately. **A defect found while building it, not shipped:** `LastOcclusion` is last-write-wins and written only on a HIT, so an unblocked shot would have reprinted the PREVIOUS shot's collider — a wall blocking a shot nothing blocked, in the one artifact meant to settle this. `OcclusionFor` dates the description against `ShotsOccluded` and prints `none-this-shot` instead. **Green:** 64 of 64 EditMode tests, 10 of them new. **Mutation-proved, three mutants, three reds:** weakening the freshness compare reprinted a stale collider; removing the zero-length guard put `frac=NaN` in the artifact; swapping the fraction operands printed 2.857 and 1.053 where 0.350 and 0.950 belong. **Still owed:** a lane-B run. Both readings remain alive and this changes neither — it only makes the next run able to tell them apart.  **THE RUN, 2026-08-25 — `artifacts/lane-b/x20-occlusion-01`, ~~spawn pinned to slot 0~~ (CORRECTED 2026-08-25: it was NOT pinned — its own server log carries `SPAWN_INDEX=0 is outside the scene's 0 spawn point(s)` and its actors landed on points 2, 2, 1. See X-22. The 4.7 m pairing was luck; nothing else in the report depends on the pin and its seam arithmetic stands), 3 of 3 clients exit 0.** The collider name decides nothing, because **there was no occlusion to name**: 240 trigger frames, every one `occlusionHit[none-this-shot]`, `occluded=0 presentFallbacks=0 resolved=6 hits=0`, victim on 100 health at all 7 checkpoints. `none-this-shot` is the freshness path, not a default — the instrument worked and reported that the thing it was built to describe did not happen. **Both readings are dead for this run rather than decided between**, and this does NOT retroactively settle the baseline: `x19-fixed-04` held at 10.1 m unpinned, this run stands at 4.7 m pinned, so it is different geometry and the question is retired, not won. **What the run exposed instead:** everything upstream is now correct — `present.torso == frame.torso` to seven digits, `frame.tick == wanted.tick`, `aim.resolved=true` on actor 42 at 4.65 m, nothing in the way — and the shot still misses. `HitboxSet.FromSize` stops torso AND arms at 1.550 and starts head at 1.580, so **nothing covers 1.550..1.580, a 3 cm seam across a standing body**; a ray through it returns Miss before the occlusion test runs, which is precisely why `occluded` stays 0. The logged ray arrives 1.5684 m above the target's feet — inside the seam. `ScriptedTargetSolver` raises BOTH endpoints by `EYE_HEIGHT` 1.6, which is 0.02 m inside the head box's lower edge rather than centre of mass, so the aim point is 1.6 m at every range and the margin is two centimetres everywhere. **Stated honestly:** the seam is a fact of the source; that THIS ray threaded it is arithmetic over an `aim` rounded to two decimals, and ±0.005 in y is ±0.024 m at 4.74 m — wider than the seam. Consistent, not proven. **Next measurement, one line:** `LagCompensator.Resolve` already loops all four boxes — on a miss, record the nearest box and its signed miss distance instead of discarding them. Fix nothing until that line exists. `reports/2026-08-25-x20-the-linecast-blocked-nothing.txt` | closed |
| **X-21** | `PredictionReconciler.Reconcile` replays unacknowledged inputs without ever moving the predicted position | **CLOSED** (2026-08-26, verdict-closure R4 task R4.1) | The replay loop calls `MovementCore.Step(ref predicted, in _inputs[slot], dt)` and **discards the return value**, while `Step`'s own remark says `MoveState.Position` is "not" written — "only the collision system knows where the actor really ended up, so the caller writes it back after moving". So replay advances velocity and stance and never the position: every correction lands the client on the server's STALE position with the unacknowledged motion thrown away. Corroborated by the pre-fix record — `corrections: 2208` in a 136 s run that never converged, with `pendingInputs` pinned at `Capacity`. X-19's fix dropped the driver to `corrections: 0` by removing what was being corrected, so the fault is now **quiet rather than gone** and will resurface the moment prediction has real work to do. `reports/2026-08-23-x19-lane-b-rerun.txt` **FIXED, and the test that should have caught it was green on the broken code.** `Reconcile`'s replay loop now writes `MovementCore.Step`'s return value back into `MoveState.Position`, which is what `Step`'s own remark says the caller must do. The pre-existing pin, `ClientReplicationTests.ACorrectionReplaysTheUnacknowledgedInputsOverAuthority`, built its expected position by calling `Step` and reading `.Position` — the same method under test, the one that deliberately does not write that field — so both sides sat on the authoritative 3.000 m and compared equal through 2,208 corrections. It has been repaired to accumulate the returned deltas, and `PredictionReplayTests` computes every expectation from the movement CONSTANTS instead. **The load-bearing pin asserts DISPLACEMENT, not a correction count** (a count would have passed on the post-X-19 idle path): with client and server running the same `MovementCore` over the same inputs, a correction must move a correctly-predicting client by nothing. **Mutation-proved** — reverting the one line reports *"a correction moved a correctly-predicting client by 0.5013 m"* and takes 6 tests red (`reports/2026-08-26-r4-x21-observed-red.txt`). Two facts stated rather than left to be rediscovered: a grounded replay carries the stick-to-ground force the floor normally refuses (N x 0.333 m, absorbed by `ApplyCorrectedState`'s `CharacterMove` path, pinned); and `CorrectionCount` counts snapshots taken at more than 0.25 m of lag rather than mispredictions — filed as **X-41**. Report: `../verdict-closure/reports/2026-08-26-r4-prediction-and-hitbox.md`.| [R4](../verdict-closure/phases/phase-r4-prediction-and-hitbox.md) |
| **X-22** | The lane-B combat set only produced an engagement when the two spawn points happened to be close | **CLOSED** (2026-08-25) — the selection is pinnable; the first pinned RUN is 3D's | **Why the seed did not already fix this**, which is the part that had to be understood first: `LaneBHarness.Awake` has always called `UnityEngine.Random.InitState(IRONFRONT_LANEB_UNITY_SEED)`. A seed pins the draw SEQUENCE, not which draw a given player lands on — three clients join over a real socket at times nobody controls, so `MoveToSpawnPoint` is reached at a different offset in that sequence every run. **Fix:** `PinnedSpawnPointDirectory`, a decorator over the existing `ISpawnPointDirectory` seam that reports exactly ONE slot eligible. `ChooseSpawnIndex`'s reservoir sampling then has nothing to sample between: with one candidate `Random.Range(0, 1)` has a single value, so the pinned slot wins whatever the RNG is doing, and the draw is still consumed so nothing else the seed governs is perturbed. Installed by `LaneBHarness.OnSceneLoaded` on the server role only, behind `IRONFRONT_LANEB_SPAWN_INDEX`, validated against the scene's count — and every failure path logs an error and leaves the run UNPINNED rather than throwing, because a harness that quietly stopped being deterministic IS this row. **It narrows and never widens:** eligibility is still the inner directory's answer, so pinning a team-owned point starves the other team into the existing loud `MoveToSpawnPoint` warning rather than silently admitting it. **Green:** 54 of 54 EditMode tests, including 5 new ones in `SpawnPointSelectionTests`. **Mutation-proved, both halves of the `&&`:** dropping the index test → 3 reds (the pin escaped, 300 draws scattered); dropping the inner team call → 2 reds (a team-owned point admitted the wrong team). **Which index:** Dustbowl has six slots and all six have placed a real player across `artifacts/lane-b` (`grep "placed at spawn point"` → 0..5 all present), so any is valid; every player lands on the chosen one, so the pair is co-located either way. **NOT claimed:** no lane-B run has been executed with the pin — that run is phase 3D itself, and it is what will show the approach collapsing from 95 s to nothing  **RE-OPENED AND RE-CLOSED 2026-08-25 — the fix above never took, and the row's own caution is what caught it.** Phase 3D ran it and every run logged `IRONFRONT_LANEB_SPAWN_INDEX=0 is outside the scene's 0 spawn point(s)` on a six-point map. **Root cause:** `PinSpawnPointIfRequested` ran from `LaneBHarness.OnSceneLoaded` and validated the index against `ISpawnPointDirectory.Count`, which reads `ActorManager.instance.spawnPoints` — an array filled by `ActorManager.StartGame()`, reached from `GameManager.OnLevelLoaded`, **another subscriber to the same `sceneLoaded` event**. The harness asked before the array existed, read 0, and rejected every index. **Two runs reported a pinned spawn and were not pinned:** `x20-occlusion-01` placed its actors on points 2, 2, 1 and `x25-torso-aim-01` on 5, 2, 0 with the pair 483 m apart — see X-20's correction. **Fix:** a count of zero is a RETRY, not an answer, until the deadline, and the deadline is the `server ready` line — nothing can join before it, so nothing can spawn before it. An index outside a NON-EMPTY directory is still answered at once; retrying that would turn a typo into the same quiet. The decision moved to `LaneBSpawnPin`, engine-free and `<Compile Include>`-linked into the dotnet suite for the reason `ScriptedAim` states. **Mutation-proved, three mutants, three reds, each caught by the test written for it:** restoring `empty is Failed regardless of final` (the shipped bug) reds `AnEmptyDirectoryBeforeTheDeadlineIsRetriedSilently`; retrying an out-of-range index reds `AnIndexOutsideANonEmptyDirectoryFailsImmediately`; never reaching the deadline with no directory reds `NoDirectoryAtTheDeadlineFailsAndNamesTheRow`. **Green:** `spawn pinned to index 0 of 6` and all three actors on point 0 across `x25-torso-aim-02`, `-03`, `-04` — 3 of 3. **What the pin does NOT buy:** an isolated engagement. All three bodies on one point put the witness in the line of fire and a third party in range — X-28. `reports/2026-08-25-phase-3d-lane-b-verdicts.md` | closed |
| **X-23** | `develop` had been RED for eleven consecutive CI runs and nothing required anyone to notice | **CLOSED** (2026-08-25) — scoped, not silenced | **The run record:** last green `b14ef41c` (2026-08-21T16:28); red from `26a3f33a` (16:58, #166 — the commit that last touched the flagged file) through `29b44bf`. Eleven merges landed on a red branch because require-status-check is off, and `docs/branch-protection.md` declined to turn it on *because* `build-test` was red. **Diagnosis:** G4 scopes by FILE — `NetClientLocalCombatDriver` is governed because `OnSpawnActor` is a per-actor path — but the flagged read is not on it. `ScriptedRespawnPressed()` reaches `FpsActorController.instance` from `Update()`, reading THIS client's own input, resolved per frame rather than cached precisely because the body is spawned and killed independently of the component and a cached reference would go stale at a death. **Fix:** one entry in the `PerActorGuardExemptions` array that already existed for exactly this shape, with its reason and its residual risk written out — G4 cannot see call paths, so a future per-actor caller of that helper would not be caught, and the entry says to delete rather than widen if one appears. **Companion, which no exemption had before today:** `PerActorGuardExemptions_HasNoStaleEntries` re-checks all six entries by IDENTITY against the tree — the path still matches a file, the file is still in scope without the exemption, and the named member still reaches a local-only singleton — and its failure text says DELETE, never re-point or re-pin. **Mutation-proved, five mutants, five reds:** renamed file → RED; member that exists but touches nothing (`RequestRespawn`) → RED; out-of-scope file (`/GameManager.cs`) → RED; whole-file entry on an in-scope file with no touch (`/BaselineAckPolicy.cs`) → RED; and removing the new entry outright → the **gate** returns to exit 1 on the same line, so the exemption is load-bearing rather than decoration. Plus a red/green twin proving the exemption is per-MEMBER and does not hand the file a pass. **Green at this commit:** `ClientWiringGate` **exit 0** (was 1), `SpecChecker` exit 0, `dotnet test` **1,703 / 0 / 0** (was 1,700 — the three new tests). B-5 still owns the observational A16 check; this row was the gate failure | closed |
| **X-24** | The hitbox stack has a **3 cm vertical seam**: torso and arms both stop at 1.550 m, the head starts at 1.580 m, and a ray through the gap hits a live standing player for nothing | **CLOSED** (2026-08-26, verdict-closure R4 task R4.2) — **measured first, then fixed** | `HitboxSet.FromSize` builds four boxes off the feet as (centre, full size): legs 0.45/0.90 → 0.000..0.900, torso 1.20/0.70 → 0.850..**1.550**, arms 1.25/0.60 → 0.950..**1.550**, head 1.70/0.24 → **1.580**..1.820. Nothing covers 1.550..1.580. `LagCompensator.Resolve` loops all four and returns `HitResult.Miss` when none is struck — **before** the occlusion test, which is why the X-20 run reads `occluded=0` rather than blaming a wall. This is a GAME defect, not a harness one: a human aiming at the same band gets the same nothing, and the band sits at chest-to-chin height on a standing body. **Not yet measured:** which box the ray missed and by how much — the shot log prints the torso centre only. That measurement is X-20's stated next line and should land before any fix, because widening a box is a balance change and guessing at 0.03 m is how a hitbox stops matching the mesh. `reports/2026-08-25-x20-the-linecast-blocked-nothing.txt` **The measurement shipped in its own commit, before the fix, exactly as this row required.** `LagCompensator` records, on a shot that struck no box, which box the ray came closest to and the **signed** vertical offset to its nearest edge — positive above the top, negative below the bottom — and the shot log prints it as `nearestMiss[...]`, dated against a counter the way the X-20 occlusion line is. Observed against the pre-fix geometry: a level ray at 1.560 m through a live standing body reports `box=torso vertical=+0.010m`; at 1.570 m it reports `box=head vertical=-0.010m`. Both signs, from a run, not from the arithmetic that produced the constants. **Then the fix, sized from that:** the torso's top edge IS the head's bottom edge, derived rather than written down, so moving the head moves the torso with it and the seam cannot reopen. **Balance consequence, stated:** the head box is byte-for-byte unchanged (1.580..1.820, extents 0.12) so where a headshot starts has not moved and the boundary still resolves head-first on a tie; the 3 cm band now resolves as `Body`; the torso is 0.73 m rather than 0.70 m tall and gains the neck, which is where a player aiming at a neck expects a body hit. Lowering the head's lower edge instead would have made the one box carrying a damage multiplier 12.5% taller; a fifth neck box needs a wire enum value it cannot have (three values, protocol-spec.md §4.5). `HumanoidTorsoCenterHeight` is now derived from the box's edges (1.215 m; it was a literal 1.20 that matched by coincidence), so `ScriptedAim`'s aim point follows the box. **Pin observed RED first** — *"vertical seam of 0.0300 m below head: nothing covers 1.5500..1.5800 m on a standing body at scale 1"*, and again at scale 0.85 (`reports/2026-08-26-r4-x24-observed-red.txt`); it asserts over the UNION of the boxes, not against these constants. Report: `../verdict-closure/reports/2026-08-26-r4-prediction-and-hitbox.md`.| [R4](../verdict-closure/phases/phase-r4-prediction-and-hitbox.md) |
| **X-25** | `ScriptedTargetSolver` aims at feet + `EYE_HEIGHT`, which is 2 cm inside the head box's edge rather than centre of mass — so every lane-B combat shot is a coin toss against **X-24**'s seam | **CLOSED** (2026-08-25) — **status cell corrected 2026-08-26**: the fix, its three mutants and its run are all recorded in this row and were recorded on the day; the cell was never moved off `VERIFIED-OPEN`, which is the drift phase 8 finding 6 named and declined to touch | `Pitch = ScriptedAim.PitchDegrees(from.y + DefaultAimHeight, …, to.y + DefaultAimHeight)` raises BOTH endpoints by `ProtocolConstants.EYE_HEIGHT` = 1.6. The remark explains the intent correctly — aiming at the target's ORIGIN from 1.6 m up is a downward shot into the feet — but feet + 1.6 is not centre of mass: the head box spans 1.580..1.820, so the aim point sits 0.020 m inside its lower edge, and the torso's 0.35 m of margin is never used. Because both endpoints move together the aim point is 1.6 m at EVERY range, so this is not a close-range artefact. **Why it matters beyond aesthetics:** it is why no lane-B combat run has ever scored a hit — the harness aims at the one height where X-24's seam lives. **Fix is a harness change and belongs to the harness** (phase-3 §6 forbids patching a game defect inside it, and this is not one): aim at the torso centre, feet + 1.20, where the nearest edge is 0.35 m away. Doing so will also stop masking X-24, which must keep its own row and its own measurement. `reports/2026-08-25-x20-the-linecast-blocked-nothing.txt`  **CLOSED 2026-08-25.** `ScriptedAim.PitchAtBody` raises the shooter by `ShooterEyeHeight` and the target to `TargetAimHeight`, read from the new `HitboxSet.HumanoidTorsoCenterHeight` rather than restated at the aim site — move the box and the shooter follows it. The two ends being raised by DIFFERENT heights is the whole of the method, and calling `PitchDegrees` with the same height on both is the bug this row names. **Mutation-proved, three mutants, three reds, all against the new `AimingAtAStandingBodyEntersTheTorsoWithMargin`:** restoring the X-25 aim point verbatim misses the torso at all seven ranges (`the shot missed the torso entirely at 120 m` … `at 1.5 m`); aiming at the target's origin misses; swapping the two heights misses. The test asserts the ray enters the torso with ≥ 0.30 m to the nearest edge, so an aim point merely inside the box by a hair still fails it. **It does NOT close X-24** — a shooter aiming at centre of mass stops being a coin toss against the seam; the seam is still there. **The run:** `artifacts/lane-b/x25-torso-aim-02` is the first lane-B run to resolve a trigger into a death — 30 fired, 4 hits, OBS-A 100 → 21 → 0, killfeed on BOTH observers (`killer 41 / victim 42 / Bullet`). It took X-22's re-fix as well; neither alone produced a kill. `reports/2026-08-25-phase-3d-lane-b-verdicts.md` | closed |
| **X-26** | The victim's own rig bone rejects the shot that hit it — X-20's reading 2, no longer a story | **CLOSED** (2026-08-26, verdict-closure R4 task R4.3) | Reading 2 was written down on 2026-08-22, killed by `occluded=0`, and could not be tested until a shot actually reached a hitbox. `artifacts/lane-b/x25-torso-aim-03` is that run: the pair at **0.96 m**, 30 shots fired, and 12 of them blocked by `occlusionHit[collider=Bone_002 layer=8 d=0.96m of 1.06m frac=0.911]` / `frac=0.941`. **`frac` near 1.0 puts the blocker at the endpoint**, the collider is a rig bone rather than terrain or a building, and the occlusion mask `-2049` excludes only layer 11 — so layer 8 blocks and the body occludes itself. This is the discriminator X-20's row asked for, answered from the collider NAME exactly as that row said it would be. **What it does NOT say:** the other 18 shots of that run missed the boxes outright (`hits=0`, `occluded=12`), so this is not the whole of run `-03`, and it is not why `x20-occlusion-01` read `occluded=0` (that run's rays never reached a box — X-24). **Next measurement, unchanged from X-24's:** `LagCompensator.Resolve` should record the nearest box and its signed miss distance on a miss. `reports/2026-08-25-phase-3d-lane-b-verdicts.md`  **ISOLATED 2026-08-25, and it is check 1's flake.** The original observation could not rule out the weapon or the witness, because neither was controlled. With the weapon pinned (X-27) and the witness walked aside (X-28), three runs of one programme on one spawn slot say it plainly: `x27-pinned-01` min distance **3.30 m**, `occluded=0`, 4 hits, **kill**; `-02` at **1.52 m**, `occluded=16`; `-03` at **1.93 m**, `occluded=18`. **All 34 occlusions across the three runs are `collider=Bone_002 layer=8` at `frac=0.938..0.960`** — not one is terrain and not one is a building. So the defect is **distance-dependent**, and the only run that scored is the only run where the pair never got inside 3.3 m. **Not the aim (X-25, closed), not the loadout (X-27), not the witness (X-28), not the spawn (X-22).** **What three runs do NOT give:** the crossover. 3.30 m is one clean sample above it and 1.52/1.93 m two below; where it sits is unmeasured. **And it does not say the bone SHOULD be excluded** — widening mask `-2049` changes whether anyone can shoot through a body, which is a game decision and is why this row is phase 6's rather than the harness's. `reports/2026-08-25-x27-x28-and-what-the-flake-really-was.txt` **FIXED in the game, not in the harness (V-D7).** The occlusion seam now carries the victim's actor id — whose collider a linecast found is a question only the engine can answer — and `ServerTickLoop.IsOccluded` skips any collider under the victim's own transform hierarchy. `Physics.RaycastNonAlloc` over a reused 32-slot buffer replaces `Physics.Linecast`, because the nearest hit may BE the victim and a query that returns only the nearest cannot look past it. `IsChildOf`, not a root comparison: the blockers are bones several levels down an imported rig, and a root-only check would have excluded nothing while reading as a fix. **Route chosen and stated: an ignore-list per query, not a layer move.** Re-layering bone colliders is cheaper and riskier — layer assignment is authored, so a new rig silently re-opens it, and it would have owed an asset gate (P-D5); a per-query exclusion cannot be un-authored, and needs no gate. **This row's own caution is honoured: the mask is NOT widened.** Another player standing between shooter and victim still blocks the shot. Two counters so a run is gradeable without a log: `SelfOcclusionsIgnored` rises exactly where the pre-fix build reported an occlusion, and `OcclusionBufferSaturations` says outright when a truncated query might have missed a blocker. **Mutation-proved 3/3** — root-only comparison, exclude-everything, and a seam that never carries the id — each observed red (`reports/2026-08-26-r4-x26-mutation-proof.txt`); EditMode `SelfOcclusionTests` 5/5 green on the real tree. **What is still owed and is NOT this row:** the crossover distance is still unmeasured, and no lane-B run has yet been taken across this fix — R1 owns the runs.| [R4](../verdict-closure/phases/phase-r4-prediction-and-hitbox.md) |
| **X-27** | The lane-B shooter's loadout is not pinned, so two combat runs are not comparable shot-for-shot | **CLOSED** (2026-08-25) — **status cell corrected 2026-08-26**, same drift as X-25: `ILoadoutDirectory` shipped, four mutants red, `weapon=1` on every shot line of all three `x27-pinned-*` runs | Nothing in the recorded programme selects a weapon, so the loadout follows whichever slot the server hands out. Measured across three runs of the SAME programme with the spawn pinned: weapon **1** (RK44, clip 30) in `x25-torso-aim-02` and `-03`, weapon **15** in `-04` — 30 shots against 14. X-20's report noted this on 2026-08-25 and declined to file it ("a property of the harness's inputs"); three runs later it is a measured cause of non-comparability, and a flake rate quoted over runs that differ in weapon is a rate over three different experiments. ~~**Fix is a programme step, not a code change**~~ — **that reading was wrong, and the correction is the row's content.** Nothing in a recorded programme can select a weapon (X-8: no `LoadoutSelect` client sender), and the draw is not the client's at all: `AiActorController.GetLoadout` picks each slot with `UnityEngine.Random.Range` over a **private static** name array, and a networked player's SERVER-side body goes through it. **CLOSED 2026-08-25 with a seam, not a seed** — `ILoadoutDirectory` on `NetServerBindings` (null by default, so every shipped configuration keeps today's exact draw) and `PinnedLoadoutDirectory`, which forces one name per slot and **defers on every slot it was not given**, so pinning a primary cannot silently disarm a gear slot. **The draw is always consumed, structurally:** the drawn name is an ARGUMENT to `AiActorController.PinnedOr`, so C# evaluates `Random.Range` before the method is entered and no edit inside it can skip one — a pinned run's RNG sequence is identical to an unpinned one's, the same argument X-22 makes for the reservoir draw. **This edits shipped gameplay code, which phase-3d §6 otherwise forbids**, taken as an explicit decision rather than assumed; the alternative was reflection into a private static from the harness, which breaks silently on a rename. **Mutation-proved, four mutants, four reds**, each caught by the test written for it. **Green:** `weapon=1` on every shot line of all three `x27-pinned-*` runs; EditMode 74 passed (was 64). Runner flag `-Weapon <name>`, empty by default so every existing invocation is unchanged. `reports/2026-08-25-x27-x28-and-what-the-flake-really-was.txt` | closed |
| **X-28** | Pinning all three players to ONE spawn point puts them in each other's fire, and in someone else's | VERIFIED-OPEN — harness defect, the cost of X-22's fix rather than a fault in it | X-22 narrows the directory to one slot, so every player lands on it and the pair is adjacent — which is what it was for. It also co-locates the WITNESS: in `x25-torso-aim-02` the resolver's nearest target is actor **43** (OBS-B) at 2.7 m while the shooter aims at 42. And it does not isolate the point: in `x25-torso-aim-04` the killfeed reads `killerActorId 65535, victimActorId 41, Bullet` — the DRIVER was shot by a party with no actor id, respawned **1.6 km** from the pinned point (`min dist 1604 m`), and the run graded nothing. **The pin bought repeatable geometry, not a repeatable engagement**, and check 1's 1-in-3 is partly this. Candidate shapes: separate near-adjacent pins per role rather than one shared slot; a programme step that walks the witness out of the line; or a spawn point the bots do not contest. **FIRST HALF ADDRESSED 2026-08-25, second half still open.** `combat-observer-b.json` strafes for its first seven seconds, before anything fires — pure programme data, no code, nothing under test changed. Measured over three runs: the resolver's nearest target is the TARGET in `x27-pinned-01` and `-02` (239 of 239 and 240 of 240 shot lines) and the WITNESS again in `-03` (241 of 241). **Two of three** — seven seconds of walk against a spawn point all three still share buys separation rather than guaranteeing it. **Still open:** the third party (in `x25-torso-aim-04` the driver was shot by actor 65535 and respawned 1.6 km away); it did not recur in these three, which is not evidence it is fixed. **And a consequence found while measuring, which raises this row's importance:** with the spawn pinned, all three bodies start INSIDE the driver's `holdDistanceMeters: 6.0`, so `ApproachMoveZ` returns 0 from the first frame and the driver never moves — the 1.5..3.3 m spread is spawn jitter, not an approach. The pin does not merely co-locate the witness, it parks the SHOOTER inside the range where **X-26** fires. `reports/2026-08-25-x27-x28-and-what-the-flake-really-was.txt` | [R1](../verdict-closure/phases/phase-r1-programme-set.md) |
| **X-29** | Two lane-B checks have no measurement in the checkpoint record at all | VERIFIED-OPEN — harness gap, found while grading | **Check 13's middle term.** `combat.driverEnabled` records whether `NetClientLocalCombatDriver` is RUNNING — its own remark says so, and it must keep running to accept a respawn request — so its staying `true` after death is correct and is NOT the measurement. Nothing in the record says whether a dead player's movement and fire input are suppressed, so "death → **input disable** → respawn screen" is graded on two of its three terms. The victim's death (`alive false`, `health 0`) and respawn window (`canRespawn false → true`, `secondsUntilRespawn 0.94 → 0`) ARE recorded. Related: the victim's programme sets `respawn: true` on its last step and the last capture is at that step's START, so no artifact shows the respawn landing. **Check 2's authoritative half.** The record holds the drawn scoreboard text beside the OFFLINE model (`blueScoreText "0"` vs `offlineBlueScore 1`; `redScoreText "64"` vs `offlineRedScore 64`), which shows the HUD is not drawing the offline scoreboard and says nothing about whether it matches the SERVER's score. E8 reads "reflects authoritative state"; the artifact cannot currently answer it either way.  **HALF WITHDRAWN 2026-08-25 — the measurement was already there and I mis-read it.** `ScoreUi`'s own remark settles the logic: *"Two sources feed it and they never mix."* Offline, `MatchScoreboard` raises `Changed` and `UpdateUi` redraws; on a networked client `SetAuthoritativeState` writes the server's totals **straight to the text fields and never touches the offline scoreboard** (V10 D11). So drawn-vs-offline IS the discriminator — if the presenter were dead the offline model would drive the text and the two would agree. **Measured over the existing artifacts:** `x27-pinned-01` drawn == offline at **0 of 7** checkpoints; `x25-torso-aim-02` at 1 of 7, and that one is a coincidence (`2/64` on both sides). The drawn values also follow a ROUND CYCLE the offline model does not — `200 → 0 → 200` against a monotonic `0/0 → 2/20`, two different clocks. Check 2 is graded PASS-with-caveat on that. **The caveat, stated:** this proves *not offline-driven*, and authoritative-driven only by way of `ScoreUi`'s two-source design; nothing here independently checks that the drawn numbers equal the SERVER's numbers. Closing that needs the server process to record its own totals into its artifact — in scope for this phase (Diagnostics, server role) and not yet done. **Noticed while grading, not filed:** the offline `MatchScoreboard` is ADVANCING on a networked client (`0/0 → 2/20`). Per the same remark the two never mix, so that is a second scoreboard nobody reads rather than a wrong one — consistent with local bot scoring, and recorded here so the next reader is not surprised. **Check 13's half of this row stands unchanged and still has no measurement:** `FpsActorController.inputEnabled` is a `private bool`, `DisableInput` also clears `characterController.enabled` but X-19's fix has `ClientPredictionStage` RE-ASSERTING that capsule, so it is not a usable proxy. That term needs a read-only accessor on shipped gameplay code — a §6 decision.  **ACCESSOR ADDED 2026-08-25 UNDER THE NEW §6 POLICY, AND IT DOES NOT GRADE THE CHECK.** `FpsActorController.IsInputEnabled` now exposes the private flag, the recorder writes it as `localInputEnabled`, and it reads **False on every client at every checkpoint, alive or dead** (`artifacts/lane-b/x31-diag-01`, all three clients, 7 of 7). **Why:** `FpsActorController.Start` calls `DisableInput()` and the only caller that re-enables is `SpawnAt`, the gameplay spawn a networked body deliberately never runs — the same fact X-19's root cause rests on. The flag gates `UpdateInput()`, the LOCAL keyboard-and-mouse read, and a scripted client reaches the actor through `FpsActorController.SetInputSource` + `NetPredictionClock.CombatButtonSource` instead. So the flag is pinned False for the whole life of a lane-B client and **cannot distinguish alive from dead**. Reporting "input disabled after death" from it would have been true, constant, and meaningless. **Recorded as a spent exception rather than quietly dropped**, because the next person will otherwise reach for the same accessor. **What would actually grade it, and needs no accessor at all:** the SERVER's treatment of a dead player's input — the shot log already carries `rejection=`, so whether fire and movement are refused after death is measurable on the authoritative side, which is where the check's content lives. `reports/2026-08-25-phase-3d-lane-b-verdicts.md` | [R1](../verdict-closure/phases/phase-r1-programme-set.md) |
| **X-30** | **`SeatRequestMessage` has zero production senders — a real client cannot ask to enter a vehicle at all**, which blocks lane-B checks 7 and 12 outright | **CLOSED** (verdict-closure R2 task R2.1, 2026-08-26) — **and its gate exemption was deleted rather than re-pinned** | **The sender is `ClientSeatRequester`** (`Ironfront_Reborn/Assets/Scripts/Net/Client/ClientSeatRequester.cs`): it reads a local edge, picks the nearest vehicle within `SeatArbiter.MaxSeatReachMetres`, and writes `ClientMessageType.SeatRequest` reliably on channel 2. **Wired, not merely present:** `NetClientBootstrap.EnsureSeatRequester` adds it in code (the `EnsureVehicleStage` / `EnsureLocalCombatDriver` precedent), so there is no scene it can be missing from — a component authored nowhere would have retired the exemption on paper while every player still stood outside the vehicle. **Gate moved 4 of 8 → 5 of 8** and the `KNOWN GAP - ClientMessageType.SeatRequest` line is gone; mutation-proved by deleting the sender and observing the gap return. **The intent is a new local-only edge, `IInputSource.SeatTogglePressed`, NOT `InputButtons.Use`** — `Use` is a level bit, so driving a reliable message off it sends one request per tick for as long as the key is held; this is `RespawnPressed`'s argument for `C_SPAWN_REQUEST`, applied to the one other edge-triggered vehicle action. `ScriptedInputStep.seatToggle` gives a recorded programme the same edge, which is what B-7 and B-13 were actually blocked on. **NOT PREDICTED, stated as a decision:** nothing here touches occupancy or moves the local body — a mispredicted ENTRY leaves the player driving a vehicle the server refused, with no correction that unwinds it invisibly. **Rejection path:** `RejectedOccupied` walks to the next seat index (bounded by `NetClientVehicle.SeatCount`, now kept from `S_VEHICLE_SPAWN`); `RejectedLockedOut` schedules ONE retry after `SeatArbiter.ReentryLockoutTicks`, which is what that enum member's own remark asks for; every other refusal is terminal and is recorded in `LastRefusalText` rather than swallowed. **A collision was found and fixed beside it:** `FpsActorController.Update` acted on the same `GetButtonDown("Use")` edge with a local `actor.EnterSeat`, unguarded — harmless while nothing sent the opcode, and a double-entry the moment something did. Guarded with `NetContext.IsClient` rather than deleted, because offline still runs it. *Original finding:* found while authoring the vehicle programme set. | R2 — closed |
| **X-31** | **A scripted client cannot equip its grenade**, so check 4 stays blocked even with the gear slot pinned and the throw path wired | **CLOSED** (2026-08-27, verdict-closure R1 task R1.1) | **The run:** `artifacts/lane-b/x-grenade-01`, spawn pinned to 0, `-Weapon "RK-44" -Gear "FRAG"`, 3 of 3 clients exit 0 with 5 of 5 checkpoints. The server logged `primary='RK-44' gear1='FRAG'`. And **`weaponId` reads 1 (the rifle) on all three clients at all five checkpoints**, with `explosionsTotal: 0` — the grenade was never equipped and nothing ever detonated. **The wire path is intact, checked rather than assumed:** `ScriptedInputSource:140` forwards `step.switchWeaponSlot`; `InputButtonPacker:76` sets `InputButtons.SwitchWeapon2`; `ServerCombatBridge:118` calls `ApplyWeaponSwitchIntent(frame.WeaponSlot)`; and `NetServerActor.WeaponId` reads the LIVE gameplay actor through `IGameplayActorSource.TryGetActiveWeaponNetworkId`, falling back to its serialized field only when there is no `Actor` — so a switch that DID take would replicate and the artifact would show it. That last one was checked specifically because the opposite (a `WeaponId` cached at spawn) would have made the artifact blind and this row wrong. **Two candidates survive, and this run cannot separate them:** (1) `weapons[2]` is empty — `WeaponManager.EntryNamed("FRAG")` resolved to nothing, or the pinned name never reached `SpawnLoadoutWeapons` for a CLAIMED body (`ServerCombatBridge:307` calls `actor.EquipLoadout()`, a path X-11 already found broken once); (2) `Actor.SwitchWeapon:1169` took the `IsToggleable()` branch and called `Toggle()` instead of unholstering, which leaves `activeWeaponSlot` on the rifle and `WeaponId` unchanged — and a toggled grenade is not a thrown one. **Next measurement, one line, same shape as 3F.1:** log `weapons[slot] != null` and `IsToggleable()` per slot at `EquipLoadout` time. Do not fix before it exists. **A doc claim this falsifies:** `ScriptedInputProgramme.switchWeaponSlot`'s remark states "This is how a grenade is thrown, and the reason check 4 needs no new wire bit". The wire bit is indeed enough to REQUEST a slot; it is not enough to end up holding a grenade, and the remark reads as though it were.  **NARROWED 2026-08-25, and BOTH filed candidates are dead.** Two gated diagnostics were added under the new §6 policy — `Actor.LogLoadoutSlots` at equip time and `NetServerActor.LogSwitchIntent` at the switch edge, both behind `IRONFRONT_LOG_LOADOUT=1`. **The slots are armed correctly:** `slot0[RK-44 toggleable=False netId=1] slot1[S-IND7 …] slot2[FRAG toggleable=False netId=7] slot3[empty] slot4[empty]`, `active=0`. So candidate (1) *empty `weapons[2]`* is dead, and candidate (2) *the `IsToggleable()` branch* is dead — the FRAG is present and is **not** toggleable, so `SwitchWeapon` would take the unholster path. **And the switch never reaches the server:** `artifacts/lane-b/x31-diag-02` (grenade set, slot 2 held for 11 s across three steps) logs **zero** `[switch]` lines. That is a real negative rather than a silent gate — the `[loadout]` lines appear in the SAME server log from the SAME env var, so the gate demonstrably works in that process. Read against the method: a first frame carrying slot 2 would pass both early returns and log `forwarded`, so zero lines means slot 2 never arrived. **Everything upstream still reads correct in source:** `ScriptedInputSource.Buttons` passes `weaponSlot: step.switchWeaponSlot` to the one shared packer; `InputButtonPacker:76` sets `SwitchWeapon2`; `InputFrame.WeaponSlot` decodes it back to 2; `FpsActorController:152` binds `clock.CombatButtonSource = () => inputSource.Buttons`, a closure over the FIELD, so a later `SetInputSource` is picked up. **So the break is at runtime between the scripted source and the sent frame, and static reading is out of road.** **LOCATED TO THE CLIENT 2026-08-25 — the server receives no slot bit at all, and `fire` from the same step object does arrive.** The `[shot]` line now prints the raw word it received (`buttons=0x{...:X4} slot={...}`, under the existing `IRONFRONT_LOG_SHOTS` gate). `artifacts/lane-b/x31-diag-04`: **60 of 60 frames read `buttons=0x0001 slot=-1`** — `Fire` alone, bit 13 clear. **Three things that ruled themselves out on the way:** the programme is NOT at fault (`grenade-driver.json` carries `"switchWeaponSlot": 2` on `armed`, `thrown` and `detonated`, and `"fire": true` on `thrown` — read back from the file); the WIRE is not truncating (`ClientInputMessage` writes `WriteU16((ushort)f.Buttons)`, full width, and `SwitchWeapon2` is `1 << 13`); and the DECODE is not wrong (`slot=-1` is the correct reading of `0x0001`). The call site also demonstrably runs — `ServerCombatBridge` calls `ApplyWeaponSwitchIntent(frame.WeaponSlot)` on the same per-frame path that emitted those 60 lines, before the mounted-weapon return. **So the loss is between the parsed step and `ScriptedInputSource.Buttons`, on the client**, and it is specific: one `InputButtonPacker.Pack(...)` call receives `fire: step.fire` and `weaponSlot: step.switchWeaponSlot` from the SAME step object, and only the first survives. **Next measurement, one line and no §6 decision needed** (it is Diagnostics): log `step.switchWeaponSlot` beside the packed `Buttons` inside `ScriptedInputSource`. That separates "the step the source is reading does not hold 2" from "it holds 2 and the packed word loses it". `reports/2026-08-25-x27-x28-and-what-the-flake-really-was.txt`  **CLOSED 2026-08-27, and the answer was in the sentence this row already contained.** The row's last narrowing reads *"one `InputButtonPacker.Pack(...)` call receives `fire: step.fire` and `weaponSlot: step.switchWeaponSlot` from the SAME step object, and only the first survives"* — true, and it named the wrong call. **That packer's answer never reaches the wire on a lane-B client at all.** `ScriptedInputSource.Buttons` is consumed by `NetPredictionClock.DefaultInput`, and `LaneBHarness` assigns `clock.InputSource = BuildMoveInput`, replacing `DefaultInput` **wholesale** — so the packer was computed and discarded every tick, and the frame that actually went out was built by `BuildMoveInput` from a `MoveInput` **with no field for a slot**. `fire` survived because `MoveInput` carries `Fire`; the slot did not because `MoveInput.ToButtons` — its own remark calls it *"the one place a `MoveInput` becomes buttons"* — had never heard of `SwitchWeapon0..3` or `Use`. **This is row X-3 a second time, to the same struct, one bit-group over**, and X-3's own remark predicted it: *"a mask built in two places is a mask that disagrees with itself the first time a bit is added."* **Fix:** `MoveInput` grew `WeaponSlot` and `Use` and carries them through `ToButtons`, `FromFrame` and `WithAxes`; the slot codec moved to `InputFrame.SlotBit` / `InputFrame.SlotOf` so the two producers — `InputButtonPacker` in `Ironfront.Net.Unity` and `MoveInput.ToButtons` in `Ironfront.Net.Replication`, assemblies that may not reference each other — share one transcription instead of keeping two. **Green:** `WeaponSlotOnTheWireTests`, 20 assertions through the real `C_INPUT` codec. **Mutation-proved 8/8**, each red — M1 and M6 are the shipped bug put back deliberately, because every assertion in `ClientInputSenderTests` passed on the day this row was filed (`reports/2026-08-27-r1-x31-mutation-proof.txt`). **RUN:** `artifacts/lane-b/r1-grenade-03` — `[switch] actor=41 slot=2 outcome=forwarded weaponId=7`, once and edged, `weaponId` reading **7** on the driver, and **60 of 60** shots resolving `weapon=7` against 60 of 60 `weapon=1` in `-02`. **What this row does NOT close, and B-4/B-14 move rather than stay:** the grenade is held and fired and still does not detonate — it resolves as hitscan, `hits=1` at 1.2 m, `explosionsTotal 0`. That is **X-42**, a different defect this one was hiding. | closed |
| **X-32** | **The reliable channel abandons packets under the exact network condition check 7 names, and nobody survives two minutes** | **CLOSED 2026-08-27 (verdict-closure R3)** | **Root cause, one line of arithmetic.** `Connection.Resend` shipped the stored datagram verbatim, so a retransmission kept its ORIGINAL sequence — and the ack bitfield is addressed by *distance behind the receiver's newest sequence* and holds exactly `ACK_BITFIELD_BITS` = 32 of them. At the ~50 packets/s a loaded client sends (30 Hz input plus a reliable baseline ack per applied snapshot) that window is worth about **0.64 s**, less than the second retransmission's backoff on a 100 ms link. Past it the copy still ARRIVED and the receiver had nowhere to say so, so the sender spent its whole 10 s `AbandonAfterMs` budget on a packet the peer already held, latched `HasAbandonedReliable`, and `Connection.Update` ended the connection at its single `Fail(DisconnectReason.TransportError)` call site. **That is why the clean control held 8 of 8 with the same client count, duration, behaviour and seed: on a clean wire nothing is ever retransmitted, so no sequence ever leaves the window.** **Fix:** `ReliabilityLayer.Update` assigns each retransmission a fresh sequence and relocates the record onto it; `Connection.Resend` re-stamps the header with that sequence and a current ack window. Wire format untouched, so **not a version event** — `protocol-spec.md` § 2.2's premise that an ack is only lost if 33 consecutive packets are lost was always a requirement on the sender, and now says so. Cost, stated: a late ack naming the original sequence is ignored, so the packet is retransmitted once more and acked on that copy. **Measured:** `artifacts/lane-a/r3/r3-typical-report.json` — 8 clients, 120 s, `--sim typical` (50 ms ± 20 ms, 5 % loss, 2 % reorder, seed 12345), **8 of 8 held for 121.2 s**, 19,308 snapshots applied, `TransportWarnings` empty. Against `run-01` **4 of 8** and `p4-typical` **0 of 8** on identical configuration. The clients that survived retransmitted MORE than the ones that died — 103–142 reliable packets each reached their first RTO, ~5 % of ~2,400 baseline acks, exactly the configured loss rate — and not one was abandoned. **Five tests, three observed RED first**, and all three go RED again when `Relocate` is mutated to reuse the sequence. The row's own instruction was honoured: `NetLog` is now routed into the harness console and `HarnessReport.TransportWarnings`. [`2026-08-27-phase-r3-wire-integrity.md`](reports/2026-08-27-phase-r3-wire-integrity.md) § 2. | [R3](../verdict-closure/phases/phase-r3-wire-integrity.md) |
| **X-33** | **Check 10 has no instrument: nothing in the repository measures per-frame allocation** | **CLOSED 2026-08-27 (verdict-closure R5 task R5.2)** — the instrument exists and has been SEEN rising | **The row had two halves and both are answered.** The first was the lane assignment, and it was wrong: [`phase-3-harness.md`](phases/phase-3-harness.md) § 2 put check 10 on lane **A**, which is engine-free on purpose (§ 4: *"a harness with its own decoder would grade the harness"*), never loads Unity, and holds no reference to `ClientVehicleStage`. **The assignment is now changed in § 2 itself** — the scope-lock row reads **B**, with the move and its reasoning recorded beneath it, so the list and the instrument agree rather than a report saying they should. **The second half was the instrument.** `LaneBAllocationSampler` (`Assets/Scripts/Net/Diagnostics/LaneBAllocationSampler.cs`) holds a `ProfilerRecorder` on `GC Allocated In Frame`, is sampled every frame from `LaneBHarness.Update`, and is drained into every checkpoint as a window — `frames`, `totalBytes`, `maxBytesInAFrame`, `bytesPerFrame`, `valid`, `probeBytesPerFrame`. It lives in `Net/Diagnostics`, which the asmdef's `defineConstraints` keeps out of a player build (asmdef-seam C4d), so a shipping build pays nothing. **Observed rising, not assumed to** (acceptance criterion 5): two lane-B smoke runs identical but for `IRONFRONT_LANEB_ALLOC_PROBE=1048576`. Steady-state windows read **24,672–24,871 B/frame** on the control and **1,073,248–1,073,382 B/frame** with the probe armed; every steady-state delta lands on **1,048,5xx** against an injected 1,048,576 B/frame — the quantity recovered to within frame noise. `artifacts/lane-b/r5-alloc-control`, `artifacts/lane-b/r5-alloc-probe`, tabulated in `artifacts/lane-b/r5/r5-x33-instrument-falsification.txt`. **What it deliberately does NOT claim.** `GC Allocated In Frame` is a WHOLE-FRAME counter and cannot attribute a byte to `ClientVehicleStage`; attribution needs a sampled call tree, which is a capture and not a counter. Check 10 is therefore graded as a DIFFERENCE between checkpoint windows — on foot against driving — and `bytesPerFrame` reports **-1**, never 0, when there is no answer, so an unavailable counter cannot render as a flawless zero. **B-10 is still not graded**, and by a different blocker: see that row. | closed |
| **X-34** | **Lane A's synthetic client sends `InputButtons.None` and nothing else, so check 11's sequence cannot be provoked** | **CLOSED 2026-08-27 (verdict-closure R5 task R5.1)** — the capability exists and two of the four verbs are now recorded from real runs | **`HarnessBehavior.Combat`** is the third behaviour, driven by `CombatDrill` (`Ironfront.Net.LoadHarness/CombatDrill.cs`): approach a vehicle, ask for its driver seat, drive it, get out, close on the nearest live body, hold the trigger, die, wait out `RESPAWN_SECONDS` and ask for a body back. Every opcode it sends is a SHIPPED one — `C_INPUT`, `C_VEHICLE_INPUT`, `C_SEAT_REQUEST`, `C_SPAWN_REQUEST` — and every fact it reads comes from the shipped decoders through a `DrillWorld` struct, so `tools/check-harness-no-decoder.ps1` stays green (10 files scanned, PASS). Where to point is `ScriptedAim`, **linked into the harness rather than transcribed**, for X-25's reason: that row was one aim convention written down twice and drifting. `VerbLog` records each verb's first sighting with the tick, the evidence and a count; `StateCapture` gained the vehicle `flags` column, without which a burn is unobservable — there is no `S_VEHICLE_BURNING`. **35 tests** in `Ironfront.Net.LoadHarness.Tests` cover the drill and the log with no socket. **Two of four verbs fire, from a real run:** `artifacts/lane-a/r5/r5-combat-06` (8 clients / 150 s / clean wire, 8/8 held, zero server exceptions) records **Damage** at decoded tick 1307 by client 7 (`S_HIT_CONFIRM target=47 hitbox=Limb`, 22 sightings) and **Death** at the same tick (`S_DEATH victim=47 killer=48 cause=Bullet`, 8 sightings), against 2,298 trigger ticks. **Drive and Burn do not**, and the reason is not the harness: **X-45**, **X-46** and **X-47** are three server defects this capability was the first thing ever to reach. See **B-11** for the grade and [`2026-08-27-r5-lane-a-verbs.md`](../verdict-closure/reports/2026-08-27-r5-lane-a-verbs.md) § 3. **Phase 5 reopening trigger, stated rather than left to be noticed:** **C-1** closed OFF partly because *"the load harness cannot fire"*. It can now. The other input C-1 names — a tick p99 measured with the projectile stepper genuinely ACTIVE — is still not produced, so the condition is not met; this row exists so the next reader knows one of its four terms has moved. | closed |
| **X-35** | **Lane A's cross-client agreement number cannot tell divergence from staleness, so neither its zero nor its non-zero means what it says** | **CLOSED 2026-08-27 (verdict-closure R3)** | **Fixed at the source of the number rather than at the number.** `DeltaDecoder` and `VehicleDeltaDecoder` now carry a `PositionProvenance` side table recording, per entry slot, the server tick that entry's position arrived on — the snapshot's own tick when the delta carried `Position`, the BASELINE's recorded tick when it did not, filed into history alongside the snapshot so an inherited value keeps the tick it last moved on however many deltas pass over it. Additive; 1,214 existing replication tests untouched. Deriving it from *when the value last changed* was considered and rejected in the type's own remarks: right almost always, and wrong in exactly the case that matters — a full snapshot re-sending an unchanged value for one client while the other's differs files a real divergence as staleness, and a false negative on divergence is the one error **X-40** cannot afford. **`HarnessReport.AgreementBlock` now carries `Divergences` (differ, same update tick), `StaleComparisons` (differ, different update tick) and `UnclassifiedComparisons`** — a side with no update tick at all, unreachable if the provenance is right and therefore surfaced rather than absorbed; **it is 0 on every run**, which is the self-check passing. Rates are taken over `SameTickComparisons`, never over every comparison attempted. **Mutation-proved as the phase required:** force both entries onto one update tick, change nothing else, and the count moves out of staleness into divergence; reverting the split turns that test red. **`Ironfront.Net.LoadHarness.Tests` now exists** (9 tests) — the harness had no test project, which is how a counter quoted in every run banner spent two phases reporting interest management as replication divergence. **Vehicle 4's settling sequence now scores as staleness:** `r3-clean-2` `FirstStale` = `tick 2135 vehicle 2: client 0 (17179,1690,15349) updated at 2135 vs client 1 (17179,1689,15349) updated at 2130`. [`2026-08-27-phase-r3-wire-integrity.md`](reports/2026-08-27-phase-r3-wire-integrity.md) § 3. | [R3](../verdict-closure/phases/phase-r3-wire-integrity.md) |
| **X-36** | **The killfeed renders a transport player id, so E7's "with a name" half cannot be satisfied by any run** | **CLOSED** (verdict-closure R2 task R2.2, 2026-08-26) — **and the row's premise was wrong: it needed no opcode and no version move** | **The name was already inside the signed ticket, and the server was already parsing it.** protocol-spec § 12 has carried `u8[16] displayNameUtf8` since the freeze; `UdpTransportServer.HandleConnectResponse` verifies the HMAC and calls `JoinTicket.TryReadFields` to bind `PlayerId` — and discarded the name field of that same call with an `out string _`. The whole change was to stop discarding it: `Connection.DisplayName` → `ConnectionInfo.DisplayName` → `ServerTickLoop.DisplayNameFor` → the `S_PLAYER_LIST` rows that were already being broadcast. **Not one byte on the wire moved**, `PROTOCOL_VERSION` is still 3, and `protocol-spec.md` needed no edit — so this row's stated reason for being unclosable inside phase 3 ("a real username needs a new opcode, and phase-3 AC-2 forbids one") was never true, and neither was `ServerPlayer.DisplayName`'s "the game server never parses a `JoinTicket`", which has been corrected in place. **The client half was already closed** by debt-closure phase 2 (**C-3**): `ClientMessageRouter.OnPlayerList` has had a production subscriber since `NetClientCombatPresenter.cs:104` wired `PlayerNameTable.Apply`, and `ClientWiringGate` reports 15 of 15 events subscribed — the KNOWN GAP the R2 phase plan expected to remove had already gone. **Sanitized at both ingresses** (`PlayerNameSanitizer`, `Ironfront.Net.Protocol/Security/`): the server's pass protects the server from a ticket whose signature proves only that the master issued it, and `PlayerNameTable.Apply`'s pass protects a client from a game server it cannot verify. It drops angle brackets (Unity rich text — `<color=#00000000>` is an invisible killfeed line), control characters, bidi overrides (U+202E re-orders the text AROUND it, so "A killed B" can be made to read as "B killed A"), unpaired surrogates and whitespace runs. **An empty result falls through rather than rendering blank** — server side to `#5001` then `Player 3`, client side to `actor 7`. **Evidence:** `Ironfront.Net.Transport.Tests/JoinTicketDisplayNameTests.cs` drives a real handshake and asserts the name reaches `GetInfo(id).DisplayName`; `PlayerNameSanitizerTests` (16 tests) and two new `PlayerNameTableTests` pin the hostile cases. Holds **B-1**'s second half, which now needs a lane-B run rather than a capability. | R2 — closed |
| **X-37** | **Checks 5 and 6 have no programme that provokes their case, so neither can be graded either way** | VERIFIED-OPEN — filed 2026-08-25 after the run recorded the instrumentation and no situation | Check 5 is E11's A16 camera hijack; check 6 is E12's scene ordering. **The instrumentation is present and reports baseline.** `artifacts/lane-b/x25-torso-aim-02/*-checkpoints.jsonl` carries `activeCameras` at all seven checkpoints on all three clients, showing one active camera throughout — no hijack is ever attempted, so the field records the absence of the case rather than the outcome of it. **Nothing in `tools/lane-b/` contains a step that would provoke either case**, so no run of the existing programme set can grade E11 or E12, and a re-run buys nothing. **Two cases, one row, and the reason is stated rather than assumed:** the missing artefact is the same *kind* of thing — an unwritten programme — but they are different situations needing different steps, and **B-5** and **B-6** each carry their own verdict and their own artifact line rather than one being folded into the other (`phase-3e-run-and-ledger.md` ([spec deleted — index](phases/README.md)) § 5). Programme work, phase 3D, already named in that phase's § 5 item 3. Holds **B-5** and **B-6**.  **HALF CLOSED 2026-08-27, verdict-closure R1 task R1.2.** **Check 6 (E12) is graded PASS** — see B-6. The row's framing of it was wrong in the same way this phase's was: E12's case is an ordinary client start, so no programme was ever missing, only the reading. **Check 5 (E11) stays open and its blocker is now named.** E11 is *"B enters a mounted turret and takes damage while A watches"* (V10 § 7), so provoking it needs a client that can **reach** a turret — and `ClientSeatRequester.TryFindNearestSeat` only sees seats within `SeatArbiter.MaxSeatReachMetres`, while the programme vocabulary has no verb that walks to one (`approach` resolves a **player display name**). That is **X-44**, and it holds B-5 for the same reason it holds R1.3.| [R1](../verdict-closure/phases/phase-r1-programme-set.md) |
| **X-38** | **The human half of checks 8 and 9 is a deliverable that nothing schedules, and 21 frames per run have sat unread since 2026-08-21** | **CLOSED 2026-08-28 (verdict-closure R6 task R6.1) — the frames were watched and the verdicts are written against named paths.** The row asked for a human pass, not for a pass verdict, and it got one: B-8 and B-9 now read UNGRADEABLE against `artifacts/lane-b/r6-combat-01/*.png` with the reason stated, neither on the strength of a counter. **What the pass found is worse than the row feared** — not frames that were fine and assumed, but frames that never showed the game at all, on every run since 2026-08-21 (**X-48**). The scheduling gap this row named is what kept that invisible for a week. | [`phase-3d-lane-b.md`](phases/phase-3d-lane-b.md) § 5 is explicit that an unartifacted green is a failed row, and its own § 5 item 5 asks for *"a human pass over the captured frames … recorded as human verdicts against named artifacts, which is a deliverable and not a disclaimer"*. **The frames exist and nobody has looked at one.** `artifacts/lane-b/x25-torso-aim-02/*.png` and its two siblings hold **7 frames per client, 21 per run**, captured at every checkpoint. Check 8's *"no perceptible input lag; convergence without visible snapping"* and check 9's *"breaks no unlisted cosmetic"* are both human judgment by construction — no counter in the checkpoint record can answer either. **Why a row rather than a to-do:** the failure mode this guards against is a verdict quietly upgrading from UNVERDICTED to PASS because the numeric half was green and the frames were assumed fine, which is the "assumed passing" shape one document over. Until somebody watches them and writes down what they saw against named paths, checks 8 and 9 read UNVERDICTED. Holds **B-8**'s and **B-9**'s human halves. | [R6](../verdict-closure/phases/phase-r6-human-pass-and-decisions.md) |
| **X-39** | **Dustbowl extends past the position quantizer's range, so every entity east of x = +2048 m replicates at exactly the same coordinate** | VERIFIED-OPEN — **reachability answered and severity settled 2026-08-27 (verdict-closure R3); decision recorded, execution owed** | **The open question is answered from the Editor, and the answer raises the severity: the region past x = +2048 m is not merely reachable, it is AUTHORED.** `Assets/Scenes/Dustbowl.unity` — terrain origin (0,0,0) size 3000 × 500 × 3000, so x and z run 0 → 3000 m; the `Level Bounds` volume is centre (1500, 300, 1420) size 1700 × 700 × 1600, i.e. **(650, −50, 620) .. (2350, 650, 2220)** — **302 m of x and 172 m of z past the quantizer's range**. Inside that strip: the **Oasis capture point** at (2085.6, 8.9, 1139.4) (37.6 m past), its **Flag**, its **Flag Parent**, **five vehicle spawners** from 2062.8 to 2097.4 m, and **182 of the map's 1,856 cover points** reaching x = 2200 m — **190 of 1,902 named points in all**. Vehicles spawn out there and one of seven objectives is contested out there, and everything in it replicates at exactly 2,048.00 m to every other client. **The check for this already existed and already fired.** `LevelBounds.SetupBounds` calls `PlayVolume.FitsOnTheWire` and logs `[bounds] the authored LevelBounds volume ((650.000, -50.000, 620.000) .. (2350.000, 650.000, 2220.000)) reaches past the wire's +/-2048 m position range` — into **every lane-B server log since E-6 landed**, three lines below a comment reading *"Today Dustbowl's is, by a wide margin"*. Nobody read either. A `Debug.LogError` in a 300 KB log is not a gate, so R3 ships one: `DustbowlFitsOnTheWireTests` parses the scene off disk, walks the transform chain to a world position (failing loudly rather than guessing on a rotated or scaled ancestor), and pins the overrun **by identity**. It is a **known-gap pin that INVERTS**: when the volume fits it goes RED announcing the fix and is replaced by a positive assertion — never re-pinned to new out-of-range corners. **Saturation is no longer silent, and that half is DONE.** `Quantize.PositionSaturates` names the condition; `PositionSaturationLog` counts it on the two paths carrying replicated entity positions and remembers the first offender by name; `LaneBHarness` puts the count on the per-second server line and the name in an error the first time it happens. Live evidence: `r3-typical-server.log` reads `posSaturated=95/5ent` at t=2 s and names `vehicle 1 at (2097.43,12.88,1151.01)` — which is the Editor's **Vehicle Spawner (1)** at (2097.4, 12.9, 1151.0), agreeing to 3 cm from an independent source. **13,208 saturation events across 9 distinct entities** in 121 s; `r3-clean-2` reached 15,803 across 10. **DECISION: re-origin the map** — shift Dustbowl by −1500 x / −1420 z, landing the play box at ±850 / ±800 with ~1,200 m of headroom, at **no protocol cost, no precision cost and no gameplay cost**. Its price: a bulk scene edit across 2,184 renderers, the terrain and 1,902 markers, plus NavMesh and occlusion rebakes and a verification pass across both lanes. Rejected, with costs: **widening the range** takes resolution 6.25 cm → 12.5 cm on every position of every map, moves `PROTOCOL_VERSION` and forces every hitbox/lag-comp tolerance to be re-read (keeping 6.25 cm instead needs i32 positions, +6 bytes per entity per update); **fencing play inside the range** is cheapest but is a gameplay change that moves or deletes Oasis and its five spawners. **WHAT THIS ROW IS NOW WAITING ON: the re-origin itself, which R3 deliberately did not execute** — a map-wide shift needs its own verification pass across both lanes, and half-doing it inside this phase would be worse than doing it deliberately. The gate is already in the suite and will announce the fix. [`2026-08-27-phase-r3-wire-integrity.md`](reports/2026-08-27-phase-r3-wire-integrity.md) § 5. | [R3](../verdict-closure/phases/phase-r3-wire-integrity.md) |
| **X-40** | **Two clients decode different world state on a clean wire, at a rate that varies six-fold between runs** | **CLOSED 2026-08-27 (verdict-closure R3) — the population was staleness, and the variation was the denominator** | **Answer: zero.** With **X-35**'s split in place, **0 substantive divergences and 0 quantizer-edge divergences over 15,987 same-tick comparisons** across three R3 runs (`r3-clean` 1,581 · `r3-clean-2` 13,207 · `r3-typical` 1,199). Every event lane A has ever reported under this name is one client holding an older copy of a world that is still moving. **Stated honestly rather than rounded into a verdict:** those comparisons are NOT independent — the same entities recur across ticks — so this is *no divergence observed in 15,987 comparisons*, not a confidence bound. It does not prove the rate is zero; it establishes that the population the old counter reported was not divergence, which is the question this row asked. **The shape split found nothing to split:** the 1-unit-on-one-axis population this row predicted (`26150,689` vs `26150,688`) is scored as STALENESS, because those two entries were last updated at different ticks — the benign shape was a symptom of the staleness, not a second population. **The six-fold variation was the denominator, and it is worse than six-fold.** `r3-clean` and `r3-clean-2` are the same command with the same two seeds 40 minutes apart, and their entity-comparison counts differ **4.5×** (4,310 vs 19,530). The old-style rate across five clean runs now reads 0.095 % · 0.534 % · 0.554 % · **1.392 %** · **0.261 %** — a **fourteen-fold** spread produced by nothing but which clients spawned where (`IRONFRONT_LOAD_SEED` pins the draw SEQUENCE, not which client consumes which draw — **X-22**, reappearing). **What moves it, checked rather than assumed:** per-client vehicle snapshot counts vary 567 against 2,018 on a clean wire (and 449 against 2,022 as far back as `run-02-clean`, so it is not new). The harness report now carries per-client `ActorUnknownBaselines` / `VehicleUnknownBaselines` / stale counts, and they are **zero on every client of every run** — the spread is interest management culling, working. No defect filed there. **Check 3's PASS is not withdrawn and not merely annotated — it is now supported by a measurement that could have failed.** [`2026-08-27-phase-r3-wire-integrity.md`](reports/2026-08-27-phase-r3-wire-integrity.md) § 4. | [R3](../verdict-closure/phases/phase-r3-wire-integrity.md) |
| **X-41** | `PredictionReconciler.CorrectionCount` counts snapshots taken at more than 0.25 m of lag, not mispredictions | VERIFIED-OPEN — filed 2026-08-26 by verdict-closure R4 while fixing X-21; **pre-existing, not introduced** | `Reconcile` compares the client's **current** position against an authoritative state for a tick `lag` in the past, so a client predicting perfectly is compared against a position it has legitimately left. Once `lag x speed` exceeds `PositionToleranceMetres` (0.25 m — **2.1 ticks at a walk, 1.2 at a sprint**) every snapshot returns `Corrected` even though the replay then moves the client by nothing at all. Two consequences. (1) **`corrections: N` in any artifact is a lag metric, not a mispredict rate** — B-8 must not read it as one. (2) `ClientPredictionStage` calls `ApplyCorrectedState(hardSnap:false)` on every `Corrected`, which pushes the CharacterController through a `CharacterMove` of ~zero every snapshot — the exact redundant move that method's own remark says it avoids by only writing back on a change. **Pinned, not just filed:** `PredictionReplayTests.TheCorrectionCounterMeasuresLagNotMisprediction` asserts 16 corrections on a client that mispredicted nothing, with a guard that fails loudly if the premise stops holding. **Not fixed here because it is not X-21:** comparing at the acknowledged tick needs a position history beside the input ring, which changes what that class stores. `../verdict-closure/reports/2026-08-26-r4-prediction-and-hitbox.md` | unowned |
| **X-42** | **The server's carried-weapon fire path is hitscan-only, so a thrown grenade resolves as a bullet and never detonates** | VERIFIED-OPEN — filed 2026-08-27 by verdict-closure R1 task R1.1, on the first run in which a grenade was actually held and actually fired | This is what was behind X-31 all along, and it could not be seen until X-31 was fixed, because until then nothing ever reached it. `artifacts/lane-b/r1-grenade-03`: the FRAG is equipped (`[switch] actor=41 slot=2 outcome=forwarded weaponId=7`, once, edged; `weaponId` reads **7** on the driver at three of five checkpoints), the session now fires it (**60 of 60** `[shot] actor=41 weapon=7`, against 60 of 60 `weapon=1` in `-02` before the session re-point), and **one shot passes** — `rejection=None fired=True hits=1 targets=56 nearest[actor=42 alive=True d=1.2m]`, followed by 21 `NoAmmo` (a grenade holds one) and 38 `OnCooldown`. **The grenade hit like a bullet.** `explosionsTotal` is **0** on all three clients at all five checkpoints while `explosionsAttached` is true, so the recorder was live and there was nothing to record. **Why:** `ServerCombatAuthority.Step` models hitscan only — `WeaponConfig.ProjectilesPerShot` is a shotgun pellet count, not a weapon kind, and there is no branch that launches a ballistic grenade. Explosions are announced by `ServerProjectileBridge.AnnounceExplosion` and `ServerCombatEvents.ReportExplosion`; nothing on the carried-weapon path routes into either, and `ServerProjectileBridge.AuthoritativeFlight` has shipped **default-off since V7**. **Holds B-4 and B-14**, which move here from X-31 rather than staying on a row that is now closed. **Not a harness defect and not a one-line fix:** a server-authoritative thrown grenade is a design choice between routing the carried-weapon path into the projectile bridge and driving the gameplay weapon's own throw, and it needs a phase rather than a patch. `plans/verdict-closure/reports/2026-08-27-r1-programme-set.md` | unassigned |
| **X-43** | A weapon switch re-arms the clip, because the netcode session models one weapon | VERIFIED-OPEN — filed 2026-08-27 by verdict-closure R1 task R1.1 as a **named consequence of its own fix**, not discovered later | `ServerCombatBridge.AdoptTheWeaponTheBodyIsHolding` re-points the session at the weapon the body is holding, and the only weapon state it can reach is `ClientSession.ResetWeapon()` — a full clip. `ClientSession.Weapon` is a single `WeaponRuntimeState`, so there is nowhere to park the outgoing weapon's clip and nothing to restore the incoming one's; `NetServerActor.AmmoInClip` cannot supply it either, because the bridge WRITES that field from the session every frame and it therefore mirrors the session rather than the body. **So a player who switches away and back has a full magazine.** Recorded rather than fixed because per-slot ammo is state the session, the wire and the snapshot would all have to grow, which is a phase and not a line. **Bounded, and stated so the next reader is not surprised:** it is reachable only through a weapon switch, and the shipped keyboard client still does not produce switch bits at all (`InputButtonPacker`'s own remark) — so today only a scripted client can reach it. | unassigned |
| **X-44** | **No scripted client can walk to a vehicle**, so the vehicle programme set R1.3 needs cannot be written | VERIFIED-OPEN — filed 2026-08-27 by verdict-closure R1 while scoping R1.3, **before writing a programme rather than after one failed** | R2 closed X-30 and a client can now ask for a seat: `ScriptedInputStep.seatToggle` is an edge, `ClientSeatRequester` sends `C_SEAT_REQUEST`, and the arbiter answers. **Asking is not reaching.** `ClientSeatRequester.TryFindNearestSeat` only considers vehicles within `SeatArbiter.MaxSeatReachMetres` of where the player is standing, and the programme vocabulary has no verb that walks to one: `approach` resolves through `ScriptedTargetSolver.Solve(step.aimAtPlayer)`, which takes a **player display name**, and a vehicle has none. So a driver programme whose first step is *enter a vehicle* can only work if a vehicle happens to be parked within reach of the pinned spawn point, which is not a property any run controls. **This is what R1.3's stated dependency on R2 was missing** — the phase file reads "the driver programme's first step is *enter a vehicle*, and `SeatRequestMessage` has no production sender until R2 lands", and R2 landing is necessary rather than sufficient. **Holds check 7, check 9, check 12 and B-7/B-9/B-13** (through R1.3) **and check 5 / B-5** (E11 needs a client to enter a mounted turret). **What would close it:** an `approachVehicle` step verb resolving against the client vehicle registry, or a spawn pin chosen for vehicle adjacency — a decision, not an obvious pick. `plans/verdict-closure/reports/2026-08-27-r1-programme-set.md` | unassigned |
| **X-45** | **A networked player entering a vehicle seat throws on the server**, because the AI controller on its body dereferences a squad it has never had | **CLOSED 2026-08-27 (verdict-closure R5 task R5.1)** — found by the capability that R5 shipped, in its first ninety-second run | **The throw.** `NullReferenceException` at `AiActorController.StartSeated` line 1851 — `squad.MakeLeader(this)` — reached from `ServerSeatBridge.Apply` → `VehicleGameplaySource.TryEnterSeat` → `Actor.EnterSeat`. `IronfrontNetBindings.CreatePlayerBody` instantiates `ActorManager.actorPrefab`, which is the **bot** character, so a player-slot body carries an `AiActorController` — and no squad ever adopts it. `AiActorController.cs:644` dereferences `squad` unguarded, so a null squad on a genuine bot is an AI setup fault; on a player body it is the ordinary state. **Why disabling the component was not enough.** `NetServerActor.Claim` suspends the bot brain (`IAiDriver.Suspend` sets `enabled = false`), and `IAiDriver`'s remark says that stops `Update` and the eight coroutines — which it does. But `Actor.EnterSeat` calls `controller.StartSeated` **directly**, and a direct call runs on a disabled MonoBehaviour. That one method was the only AI path a networked player could reach. **What it cost.** The throw lands AFTER `Seat.SetOccupant` and the transform re-parent and BEFORE `Actor.EnterSeat` finishes, so the seat is booked, the body is welded to it, and the rest of the entry never runs. **Why it survived to 2026-08-27.** Nothing had ever executed the path: the shipped client had no `C_SEAT_REQUEST` sender until R2 (**X-30**) and lane A had none until R5 (**X-34**). The first lane-A Combat run hit it in 90 s (`artifacts/lane-a/r5/r5-combat-smoke-server.log`). **The fix.** `StartSeated` and `EndSeated` return early when the component is disabled — everything both methods write is AI steering state, and a suspended controller is by definition not steering this body. Guarded on `enabled` rather than on `squad != null` so a genuine AI setup fault still throws where it throws today. **Verified:** `artifacts/lane-a/r5/r5-combat-01-server.log`, 8 clients / 180 s with seats taken and 1,138 vehicle inputs sent — **zero** `NullReferenceException` in the whole log. | closed |
| **X-46** | **A networked player can take a driver's seat and the vehicle will not respond to them at all** — the driver input sink needs an `FpsActorController` that a player-slot body does not have | VERIFIED-OPEN — filed 2026-08-27 by verdict-closure R5, blocking check 11's *drive* verb and **B-10** | **The mechanism.** `ServerVehicleInputBridge.Install` calls `NetServerBindings.AttachDriverInput`, which resolves to `NetDriverInputSink.Attach`, which does `GetComponent<FpsActorController>()` and returns null without one. A player-slot body is `ActorManager.actorPrefab` — the bot character — so it carries an `AiActorController` and never an `FpsActorController`. The bridge counts `UnreachableControllers++` and returns; the authority keeps accepting `C_VEHICLE_INPUT` and there is nothing on the other end of it. **The sink's own remark predicted this exact case** — *"a networked PLAYER reaching a driver seat without one means that vehicle will not respond to them at all"* — and it had simply never happened, because no client could ask for a seat until R2 (**X-30**) and lane A could not until R5 (**X-34**). **The measurement, correlated in ONE run.** `artifacts/lane-a/r5/r5-combat-05` (8 clients / 150 s, 8/8 held): the server log names it twice by body — *"actor 43 took a driver seat and no driver input sink could be attached to `Ai Character Optimizations(Clone)`, so its vehicle will not respond to it"*, and the same for actor 46 — while the harness reports **1,285 `C_VEHICLE_INPUT` messages** from three clients holding driver seats and **no `Drive` verb at all**. The two halves of that sentence are in the same 150 seconds. `r5-combat-01` (8 clients / 180 s) is the same shape at 1,138 inputs. **`Ai Character Optimizations(Clone)` is the body name in every occurrence**, which is the prefab identity the mechanism above predicts. **`UnreachableControllers` was read by NOTHING** — no log line, no report field, no gate — which is why an inert vehicle presented as a silent nothing. R5 made both `Install` failure paths `Debug.LogError` naming the actor and the body, so the next occurrence says what it is rather than being inferred from a hull that did not move. That is the diagnostic, **not the fix.** **Not fixed here, and the reason is that it is a design choice rather than a guard.** The two candidates — put an `FpsActorController` on the server-side player body, or give the driver sink a controller-agnostic seam — both change the shipped control path, and R5's § 3 scopes it to a lane-A behaviour and a lane-B instrument. Filed rather than half-taken (**V-D2**, **V-D7**). **Blocks:** check 11's *drive* verb (**B-11**), and therefore its *burn* verb, which has no other route available to a client with no explosive; and any driving window **B-10** would be graded from. | unassigned |
| **X-47** | **A networked player in a helicopter's pilot seat throws once per physics step**, for as long as they sit there | **CLOSED 2026-08-27 (verdict-closure R5 task R5.1)** — X-45's defect at a second entry point, with a much louder symptom | **Same cause, different door.** `AiActorController.HelicopterInput` line 1752 opens `if (!squad.AllSeated() || ...)`, and a player-slot body carries an `AiActorController` with no squad — `IronfrontNetBindings.CreatePlayerBody` instantiates the bot character. **`Helicopter.FixedUpdate` asks its driver's controller for a stick position every physics step** (`Driver().controller.HelicopterInput()`), so the throw repeats rather than happening once. **Measured:** **309** `NullReferenceException`s in a 150 s lane-A Combat run (`artifacts/lane-a/r5/r5-combat-05-server.log`) and **1,204** in a 300 s one (`r5-combat-04-server.log`) — every one of them this line, and nothing else in either log. **Not caught by X-45's guard**, which covers `StartSeated`/`EndSeated` — the seat-ENTRY path, reached from `Actor.EnterSeat`. This is the per-step INPUT path, reached from the vehicle. Same class, different caller, and the second one was invisible until a synthetic client sat in a pilot seat for nineteen seconds. **Why the siblings did not need it:** `BoatInput` and `CarInput` both open with `if (!hasPath) return zero`, and a suspended AI has no path, so they were already returning a neutral stick. Only the helicopter reaches its squad before any such guard. `SwimInput` is guarded the same way. **The fix:** `HelicopterInput` returns `Vector4.zero` when the component is disabled — a suspended controller flies nothing. **Zero rather than the takeoff ramp**, because the real input for a networked pilot is supposed to arrive through `NetDriverInputSink`; that it does not is **X-46**, a separate row. What this method must not do is supply the BOT's opinion to a vehicle a player is sitting in. **Verified:** see the phase report § 3.3. | closed |
| **X-48** | **Every lane-B screenshot ever captured renders the deploy/loadout menu, not the game** — so checks 8 and 9, the two the artifact set exists to answer, have never had an artifact that could answer them | **CLOSED 2026-08-28 (playable-first track)** — filed earlier the same day by verdict-closure R6 while doing X-38's human pass, which is how a capture defect gets found: **by looking**. Closing it does NOT grade **B-8** or **B-9**: those need a fresh COMBAT run watched by a human, and the proof run below is the smoke set. | **90 frames, five runs, one answer.** The `< DEPLOY >` button region is **pixel-identical** (mean channel difference 0.0) in every PNG of `x25-torso-aim-02`, `x25-torso-aim-04`, `r1-grenade-03`, `r5-x47-smoke` and `r6-combat-01` — 2026-08-25 through 2026-08-28, combat, grenade and smoke sets alike. Not one frame shows a player body, a remote body, a muzzle flash or a ragdoll. **The frames are truthful, which is what makes this a real defect and not a screenshot bug:** `LaneBCheckpointRecorder.Capture` calls `ScreenCapture.CaptureScreenshot` on the live framebuffer (`LaneBCheckpointRecorder.cs:115`) and skips it under `-batchmode` rather than writing a zero-byte file, so what is in the PNG is what the client rendered. **Meanwhile the networked body is fully alive** — `r6-combat-01/driver-checkpoints.jsonl` carries `localActorId 41`, `inSnapshot true`, 30 predicted shots, 3 hitmarker hits and a kill all three clients agree on. So the client's *simulation* joins, spawns, aims and kills while its *presentation* never leaves the pre-deploy UI, on all three clients, at all seven checkpoints, with `localInputEnabled false` throughout. **What is established and what is not:** the universality and the truthfulness of the frames are measured; **the cause is not** — R6 is a verdict phase and did not chase it. Two candidates worth separating first: the scripted client never issues a deploy, or the deploy UI is never dismissed once it does. **Why it matters more than a cosmetic:** it is the reason B-8's and B-9's human halves read UNGRADEABLE rather than PASS or FAIL, and per `phase-3d-lane-b.md` § 5 an unartifacted green is a failed row — this makes every future run's frames unartifacted by construction until it is fixed. Holds **B-8** and **B-9**. **CAUSE LOCATED 2026-08-28 (playable-first track), from source and NOT yet from a run.** Neither of the two candidates above, quite: the networked client never runs the gameplay spawn at all. `FpsActorController.SpawnAt` (`FpsActorController.cs:441-450`) is the ONLY code that does three things — `SceneryCamera.instance.camera.enabled = false`, `EnableInput()`, and `FirstPersonCamera()` — and the only other writer of that camera flag anywhere in `Assembly-CSharp` is `GameManager.cs:117` on the spectator path. **A networked body deliberately never runs `SpawnAt`**, which is stated in three places already: `ClientPredictionStage.cs:79-84` (*'the gameplay spawn a networked body deliberately never runs'*), `IGameplayActorSource.cs:97-98`, and `ServerCombatBridge.cs:320-333`. The one remaining path back to input, `NetClientLocalCombatDriver.RestoreInput`, is gated on `_inputSuppressedByDeath` and so can never be the FIRST enable. **Therefore a networked client renders through the menu's scenery camera with the loadout screen never dismissed and input never enabled** — which is exactly `localInputEnabled: false` at every checkpoint, and exactly a pixel-identical `< DEPLOY >` region in all 90 frames. **MEASURED, THEN FIXED, THEN PROVEN 2026-08-28 (playable-first track).** **The measurement was already in the artifact and had been since before the row was filed** — `LaneBCheckpointRecorder.AppendCameras` writes `activeCameras` with every enabled camera's name and depth, so the field this row asked somebody to add was there the whole time and nobody had read it. That is a `search-before-you-build` miss on my part, and it is worth recording as such: the row named a new instrument when one `python -c` over the existing JSONL would have settled the cause on the day it was filed. Read now, it is unanimous — **`Scenery Camera` enabled at depth 100 in all 90 records of all five runs**, beside a gameplay camera at depth 0 (or 10 in the four post-death records). Unity draws higher depth LAST, and `SceneryCamera.Awake/Render` gives it a full culling mask and `CameraClearFlags.Skybox`, so at the highest depth in the scene it **repaints the entire framebuffer over the live FP camera**. The frames were truthful and the game was rendering underneath them. **Cause, confirmed at the source:** `FpsActorController.Start` turns the backdrop ON and calls `DisableInput()`; `SpawnAt` is the ONLY code that undoes either; a networked body deliberately never runs `SpawnAt`. So both stayed set for the life of the process — exactly `localInputEnabled: false` at every checkpoint of every run. **FIX:** `FpsActorController.EnterDeployedView()` — `SpawnAt` minus the transform write and `ResetVelocity`, which stay the server's (AD-1) — reached through a new `ILocalPlayerRig.EnterDeployedView` seam and called from `NetClientLocalCombatDriver` on both `OnSpawnActor` (first deploy) and `OnRespawned`. A `deployedView` flag closes the race where `GameManager`'s `Invoke("OpenPlayerLoadout", 1f)` re-opens the screen a second after S_SPAWN_ACTOR dismissed it. **PROVEN on `artifacts/lane-b/x48-smoke2`, not asserted:** `Scenery Camera` is **absent from every record on all three clients** (was: present in 90 of 90), `localInputEnabled` is **true** at every checkpoint (was: false in 90 of 90), and **the frames were looked at** — they render Dustbowl in first person with the live HUD, no `< DEPLOY >` button and no loadout tiles. **Gate G11's sibling G12** pins both call sites in `tools/ClientWiringGate`, and it was mutation-tested: removing the call from `OnSpawnActor` and from `OnRespawned` each turns it RED independently. **What this does NOT fix:** the Development Console overlay still covers a quarter of every frame — that is **X-49** and it is untouched here. | playable-first track |
| **X-49** | **A lane-B client emits a NullReferenceException cascade from its own AI, ~1,650 per run across three clients, and none of it reaches the server** | **CLOSED 2026-08-28 (playable-first track)** — filed earlier the same day by verdict-closure R6, read off the Development Console overlay burnt into every captured frame. **Two causes, not the two STACKS the row names**, and the second only became visible after the first was fixed. | **Counted, not estimated:** `r6-combat-01` logs 328 / 1,276 / 50 `NullReferenceException` on driver / observer-a / observer-b and **0** in `server.log`. **Two distinct stacks, both client-side AI, both a use-after-destroy:** (1) `AiActorController.FindPotentialTargets` (`AiActorController.cs:410`) filters with `List.RemoveAll`, whose predicate reaches `HasEffectiveWeaponAgainst` (`:1119`) → `Actor.Position()` (`Actor.cs:876`) → `Component.get_transform()` on a **destroyed** actor — so a body that has gone is still in the target list when the pass that would remove it dereferences it; (2) `AiActorController.NewWaypoint` (`:1381`) → `Vehicle.ShouldBeAvoided` (`Vehicle.cs:1048`) → `Vehicle.IsStill` (`:1043`) → `Rigidbody.get_linearVelocity()` on a null rigidbody. **Not X-45/X-47 re-opening:** those were the *server* throwing on seat entry through a squad dereference and are closed; these are the *client*, at two different members, and reproduce on develop **after** both fixes landed. **Two consequences, and the second is why this is not just noise:** it is per-frame garbage on the client AI path, and the Development Console it raises **occupies roughly a quarter of every lane-B frame**, which is an unlisted cosmetic in its own right and part of why check 9 cannot be graded (**X-48**). **FIXED IN TWO LAYERS, EACH MEASURED (2026-08-28).** **Layer 1 — the registry handed out corpses.** `Actor` had **no `OnDestroy`**, so a body destroyed while alive stayed in `ActorManager.actors` and `aliveActors[team]` for the rest of the match. `ActorManager.Drop` already existed for exactly this and had **zero callers** — present, never wired — while the vehicle twin `DropVehicle` was wired from `Vehicle.Die` (and so missed a vehicle destroyed without dying). **Unity's overloaded `==` is what turns a leak into a null-ref:** a destroyed object compares equal to null but NOT to any other object, so every `x != y` test in the consumers waves it through and the next member access on a native accessor throws. The row names two stacks; the run showed **three** call sites, the third being `ActorManager.AliveActorsInRange` itself (`ActorManager.cs:269`) — which is the argument for fixing the register rather than null-guarding each consumer, since a guard at the call site leaves the next consumer exposed and leaves the corpse in the list to be counted, sorted and distance-tested forever. Fix: `Actor.OnDestroy` -> `SetDead` + `Drop`, `Vehicle.OnDestroy` -> `DropVehicle`, and all three removals made teardown-safe because they are now reached from `OnDestroy`. **Measured on `artifacts/lane-b/x49-before` -> `x49-after`: 1,012 -> 50** NullReferenceExceptions across three clients (208/532/272 -> 10/20/20), server 0 throughout. **Layer 2 — a snapshot walked across `yield`s, which no registry fix can reach.** All 50 residuals came from ONE coroutine: `AiActorController.AiTarget` takes a private copy of `FindPotentialTargets()` and then `yield return new WaitForSeconds(0.2f)` **between elements** (`:798`), so on a long list it walks for seconds while bodies die under it. **The existing `!a.dead` guard is not a staleness check and never was:** `dead` is an ordinary managed field that a destroyed `Actor` answers perfectly happily, so only `a == null` — Unity's overloaded operator — can see the corpse. Guard added at the top of the loop body; the file was swept for other `foreach`-over-actors-with-a-yield-inside and this is the only one. **PROVEN on `artifacts/lane-b/x49-x51-after` (runner exit 0): 0 NullReferenceExceptions on all three clients and the server**, on a run that did real work — 7/7 checkpoints per client, 30 predicted shots, 42 remote actors. Gate **G13** pins both `OnDestroy` contracts and was mutation-tested: removing the call from `Actor.OnDestroy` and deleting `Vehicle.OnDestroy` each turn it RED. **What is left in the console is not this row:** the frames now show two deliberate diagnostics — the harness's `ACCEPT_UNSIGNED_TICKETS` config warning, and the `LevelBounds`-past-the-quantiser warning that is **X-39**. | playable-first track |
| **X-50** | **The shipped dedicated server joins its own match as a player** — `DedicatedServerSceneBootstrap` strips nothing, so a headless host is a listen server and `architecture.md` AD-1 was never enforced anywhere | **CLOSED** (2026-08-28, playable-first track) — **and it was found by deploying the thing and reading its log**, which is the only reason it was ever visible | **The server said it plainly, on the first deployment whose log anybody read** (`ironfront/game-server` `gameserver-v0.3.0`, sandbox node, `infra/k8s/gameserver-lan.yaml`): `[net] actor 41 (team 0) placed at spawn point 4 of 6 (1221.08, 32.66, 1776.87)`, `[net] conn 1 joined as actor 41 (127.0.0.1:59244)`, then `[net] connected as 1`, `[net] local actor is 41`, `[net] first snapshot applied at server tick 3`, and `[transport] congestion -> BAD (rtt 500ms)` about its own loopback traffic. **Confirmed one layer down rather than inferred from the log:** `ss -nup` shows the pair `127.0.0.1:59244 <-> 127.0.0.1:27015`, both ends owned by the same `Ironfront.Serve` pid inside the pod's cgroup. So a sixteen-slot server shipped fifteen, one connection went to a phantom, and a body stood at a spawn point with no human behind it. **The mechanism.** `Dustbowl` carries an active `NetServer` AND an active `NetClient` — phase 3D already recorded that as finding 6, and `LaneBHarness` strips the half its process is not. `DedicatedServerSceneBootstrap` only ever loaded a scene; it stripped nothing. So the general fact was known and the consequence for the SHIPPED server was not, because until now nobody had run the dedicated image and read what it printed. `architecture.md` AD-1 (*'Server-authoritative, no host/listen-server'*) and `feasibility-study.md:193` both forbid this; neither was enforced by anything. **Why the fix is a flag and not a strip.** `Ironfront.Net.Unity.Server` references only `Ironfront.Net.Unity.Shared`, so the server bootstrap cannot name `NetClientBootstrap` and the harness's `Destroy` is unavailable to it. `NetContext.IsDedicatedServer` is declared at `BeforeSceneLoad` by the one caller that knows (batch mode, no lane-B role) and read by `NetClientBootstrap.Awake`, which returns before it claims the role or publishes `Current`. **Deliberately NOT `NetContext.IsServer`:** that is settled by whichever bootstrap's `Awake` runs first, since each defers to the other, so gating on it would reintroduce the race X-9 closed and make an Editor Play session depend on component order. **Pinned by gate rule G11, mutation-proved 3/3:** guard deleted → exit 1; guard moved below `ResolveConfiguration` → exit 1 with the distinct position message (that misplacement was this fix's own first draft, which is why the rule checks order rather than presence alone); condition 'simplified' to `IsServer` → exit 1. Restored tree exit 0. **Why a gate and not an EditMode test, measured rather than assumed:** the fixture was first written as one, building the component on an inactive `GameObject` and activating it, and it reported PASS while executing nothing — Unity does not run `Awake` outside play mode (`NetServerActorSeamTests` had already written that down). Its control case — *an ordinary client still dials* — failed in the opposite direction and is the only reason the vacuous green was caught. What survives as a test is `DedicatedServerDeclinesLocalClientTests`, four assertions on what the flag MEANS and on its independence from the role, both directions. **Suites at the fix:** `dotnet test` 1,857 / 0 / 0 across 8 projects; Unity EditMode 91 / 91. **CONFIRMED END-TO-END 2026-08-28, on the deployed server rather than in a test.** The before/after is a count over each pod's whole log: the pre-fix pod carried exactly **1** `joined as actor .. (127.0.0.1:` line; the pod running the rebuilt image carries **0**, and both guard lines are present — `[net] this process is a dedicated server: no local client will be dialled.` (the BeforeSceneLoad declaration) and `[net] dedicated server: no local client will be dialled (AD-1).` (Awake declining). Corroborated one layer down, where a log cannot lie about it: `ss -nup | grep -c 127.0.0.1:27015` is **0**, against a pre-fix pair with both ends on the same pid. The server now reports `[net] role = Server` and `16 player slots ready` with none of them spent on a phantom. **The rebuild is a MANUAL step and that is a property of the pipeline, not an oversight:** `.github/workflows/images.yml:19-20` says GitHub-hosted runners have no Unity licence, so the `game-server image` job repackages a Linux artifact produced on a licensed machine and skips on every ordinary PR. Any future Unity-side server fix needs `tools/build-server.ps1` plus a local `docker build` and side-load before it reaches a running server. **Verified the binary carried the fix before deploying it**, rather than trusting the build: `strings -el` finds the guard's log string in both `Ironfront.Net.Unity.Client.dll` and `Ironfront.Net.Unity.Shared.dll` in `build/server` (`-a` alone finds nothing — .NET metadata strings are UTF-16, which reads as a missing fix if you stop at the first search). | closed |
| **X-51** | **The player build ships without Unity's reflection-probe convolution shaders, so every client logs 10 shader-not-found errors and the Development Console covers a quarter of every captured frame** | **CLOSED 2026-08-28 (playable-first track)** — filed and closed in the same session, found by reading the log of the run that PROVED X-48 | **Counted, not estimated:** `artifacts/lane-b/x48-smoke2` logs `Hidden/CubeCopy` ×4, `Hidden/CubeBlur` ×4 and `Hidden/CubeBlend` ×2 on **each** of the three clients and **0** on the server, in the repeating five-shader group Unity emits per reflection-probe update. Each says *"Make sure it is present in the Always Included Shaders list in Graphics Settings"*, and `ProjectSettings/GraphicsSettings.asset`'s `m_AlwaysIncludedShaders` does not list them. **Why this is its own row and not part of X-49.** X-49 is the AI NullReferenceException cascade, and it is **combat-only** — the smoke set reproduces **0** NREs and still shows a full console overlay, so the two produce the same symptom by different routes and closing X-49 alone will not clear the frame. The shader half was described once, inside **B-9**'s evidence prose, and never had a row; that is why it survived. **Two consequences.** Realtime reflection probes cannot convolve, so reflective surfaces are wrong in a shipped player as well as in the harness — this is the ordinary Windows player build, not a harness-only path. And the console overlay it raises is an unlisted cosmetic occupying ~25% of every lane-B frame, which is the other half of why check 9 is hard to grade even now that X-48 is closed. **Not yet attempted, and the reason is worth stating:** the three are Unity built-in `Hidden/` shaders, so the fix is an Editor-side append to `m_AlwaysIncludedShaders` resolved through `Shader.Find`, not a hand-written `fileID` — guessing those IDs is how a settings file gets a reference to the wrong shader silently. Holds part of **B-9**. **FIXED AND PROVEN 2026-08-28, same session it was filed.** `tools/AddReflectionProbeShaders.cs`, copied to `Assets/Editor/` and run through `-executeMethod`, resolves each name with `Shader.Find` and appends it to `m_AlwaysIncludedShaders` (16 -> 19 entries, all three resolved, 0 unresolved). **Written as a script precisely so no fileID was typed by hand:** the entries are fileIDs into Unity's own resource bundles, and a hand-written number produces a confident reference to the WRONG shader that serialises, loads, raises nothing, and presents the exact symptom being fixed. Unity also migrated the asset's `serializedVersion` 12 -> 16 as a side effect of writing it. **Measured on `artifacts/lane-b/x49-x51-after`: 10 -> 0** shader-not-found lines on each of the three clients. | playable-first track |
| **X-16** | `ClientCombatState.PredictFire` has **zero production callers** in the Unity client | **CLOSED as a call** (Phase 6 task 6.7, 2026-08-26) — **and the row understates it: the OUTPUTS have no production consumer either** | **The call exists.** `NetClientLocalCombatDriver.cs:168` — `if (_state.IsAlive && FirePressed()) _state.PredictFire(Time.time);` — from `Update`, with `FirePressed` at `:228` reading `IInputSource.Buttons & InputButtons.Fire` rather than `Input.GetMouseButton`, for the reason defect 4 of the phase-3D report gives about the respawn path: a keyboard-only check leaves every scripted client, controller and rebind with no path, so a recorded programme could fire on the server and never predict on the client. **Every frame the trigger is down, not on a rising edge, and that is correct rather than lazy:** `PredictFire` runs `ServerFireResolver.CheckCanFire` first, so cooldown, empty clip, reload and death reject the extra calls — an edge detector would have to model auto-fire, which is the same predicate written twice and free to disagree with the server's. **Safe by construction:** `PredictFire` never raycasts, never applies damage and never moves health; it stamps a local cooldown and decrements a predicted clip the next snapshot reconciles. **G4 fired on the new code and the finding was real-shaped:** `FpsActorController.instance` from a per-actor-scoped file. `FirePressed` is the same shape as the already-exempt `ScriptedRespawnPressed` — reached only from `Update`, no actor id in scope, the local player IS the subject — so it took the same named exemption with the same standing instruction: if a per-actor caller ever appears, delete the entry and guard the read. **Named residue, and it is bigger than the missing call was.** `PredictFire`'s own remark names what a caller plays on `FireRejection.None` — muzzle flash, recoil, a cosmetic tracer, an ammo readout. Scope: `grep -rn "AmmoInClip\|PredictedShots" Ironfront_Reborn/Assets/Scripts` (2026-08-26) returns exactly one client-side reader, `LaneBCheckpointRecorder.cs:486,490` — **no HUD reads either field**. So what this call buys today is that `predictedShots` stops being a constant zero and becomes evidence about the client, which is precisely what this row said was impossible; the presentation half needs a client-side prediction presenter that does not exist. That is not one call, and it is not in this commit. *Original finding:* `predictedShots: 0` at every checkpoint of `artifacts/lane-b/loadout-01` while the server emptied a clip. | closed (presenter owed) |
| **X-4** | Stale owner markers in tests name phases that have closed | **CLOSED** (Phase 2, 2026-08-19) | Both markers retired by the phase that took the decisions they were waiting on (C-10, C-11). `ExplosionEventTests.VehicleAndEnvironmentKindsAreDeclaredButUncalledInV1` was an **absence pin**, so per `pinned-baseline-test-companion.md` it was INVERTED rather than re-pinned: renamed to `VehicleAndEnvironmentKindsRoundTrip`, with the remark now recording that its assertions never actually checked the gap its old name claimed to pin. *Original finding:* `ExplosionEventTests.cs:257` `// V4 owns the caller` and `:258` `// V7 owns the caller` still stand, while V4 (`ce69391`) and V7 (`538c4c7`) have both merged without taking **C-10** / **C-11**. An absence pin naming a dead owner is how a gap loses its owner silently. Retire or re-point them in the same commit as C-10/C-11. | 2 |
| **X-5** | `LobbyShellOverlay` is in no scene, so the lobby shell never runs | **CLOSED** (Phase 1, 2026-08-19) | Placed on a `Lobby Shell` object in `Assets/Scenes/Menu.unity` — the main-menu scene, which is where a lobby shell belongs. It is IMGUI (`OnGUI()`, no Canvas, no prefab, no serialized reference to wire), which is precisely why nobody noticed it had never been placed: there was no missing-reference warning to notice. Its three serialized fields keep the LAN/plaintext C# defaults **A-12** records, which stays void — E9 asked for assignments that were never owed. **Pinned by** `AssetWiringDetectors.LobbyShellOverlayIsInAScene`. *Original finding:* guid `3a9b866060cb47f1af22ca5125dd4d71`, zero refs across `Assets/**` — the same family as **X-1**, but a distinct surface (lobby, not in-match). Not an E9 obligation; E9 is void (**A-12**). | 1 |
| **X-6** | `Actor.Damage`'s `ownsHealth` guard has no pin | **CLOSED** (Phase 6 task 6.3, 2026-08-26) | **Pinned by `ClientWiringGate` G8** (`tools/ClientWiringGate/ClientWiringDetectors.cs`, `FindUnpinnedHealthOwnershipGuard`), wired into the scan loop at `GateRunner.cs:162` and named in the summary line. **Not a test, and the reason is a constraint rather than a preference:** `Actor` compiles into `Assembly-CSharp`, which no test assembly can reference (**E-11b**), so the guard cannot be exercised from NUnit at all — the same wall G7 was built against. **Three clauses, because three different edits break it and one assertion would have passed on two of them:** (1) `ownsHealth`'s initialiser is a logical NOT of `.IsClient` — *polarity*, which is the fault X-6 actually names; (2) `health -= …` is guarded above by `ownsHealth`; (3) `Die(…)` is guarded above by `ownsHealth`. **Clause 1 is the one no existing rule could have caught:** `HasGuardAbove` deliberately does not model polarity (`ClientWiringDetectors.cs`, its own remark), because for G7 an inverted guard still leaves exactly one side applying damage — here it inverts ownership outright, and G7 stays green through it. **Mutation proof, 4 mutations / 4 REDs / 0 false greens** (`reports/2026-08-26-x6-mutation-proof.txt`): A — the `!` removed → clause 1 at `Actor.cs:934`, exit 1; B — the subtraction unguarded → clause 2 at `:936`, exit 1; C — the death branch unguarded → clause 3 at `:954`, exit 1; D — the rule's own scope re-pointed at a non-existent file → **exit 2**, the completeness guard in `Program.cs`, because an absence rule that scanned nothing has not passed but failed to answer (`green-that-proves-nothing`). Distinct lines on A/B/C, so fixing one cannot mask another. `Actor.cs` restored byte-identical after every run. *Original finding:* **C-14**/**D-3** confirm the guard is present, but no test asserts `ownsHealth` is false on a client. Under P-D5's author-then-pin rule this owes a pin, and it is the guard the whole `AuthoritativeFlight` cutover (**C-1**) depends on. | closed |
| **X-9** | **The network role is decided by an `Awake` race, and every client presenter latches a permanent `enabled = false` on the loser** | **CLOSED** (2026-08-21, `dc4fd73`) | `Dustbowl.unity` carries an ACTIVE `NetServer` (&629676505) and an ACTIVE `NetClient` (&629676520), both at `DefaultExecutionOrder(-1000)`. Only the client deferred (`NetClientBootstrap.cs:146`, `if (!NetContext.IsServer)`), so the server won every tie and `NetContext.Role` read `Server` for the whole `Awake` pass of a **client** process. `NetClientPresenterGuard.IsPresentable` is `NetContext.IsClient`, checked in `Awake`, and the failing branch sets `enabled = false` and returns **with no log** — so `OnEnable` never runs and the component is dead for that process's whole life. Two symptoms, one cause: `NetClientCombatPresenter` never subscribed to `OnPlayerList` (empty `PlayerNameTable` -> `namedPlayers: 0` -> scripted aim cannot resolve a name -> check 1/E7 unattemptable), and `NetClientBootstrap.Awake`'s `EnsureLocalCombatDriver()` `AddComponent`s the driver **inline**, inside the same window (`weaponId: 0`, `clipSize: 0`, `predictedShots: 0`). **Measured, not argued:** `artifacts/lane-b/combat-fix01/driver.log` line 70 `[net] role = Server`, line 160 the harness strip, line 173 `role = Client` — and `sceneLoaded` runs after every `Awake` by construction, so the strip could never have helped. **Fix:** `NetServerBootstrap` defers to a *declared* client, mirroring the client's own guard; `LaneBHarness.DeclareRole` declares at `BeforeSceneLoad`, ahead of every scene `Awake`. With no declaration the role is `Offline` and the server still claims it, so the Editor sandbox and the dedicated build are unchanged. **Pinned by** `ScriptedInputProgrammeTests.AClientProcessDeclaresItsRoleBeforeAnySceneAwakeCanReadIt`, which asserts BOTH halves and names `BeforeSceneLoad` explicitly — `AfterSceneLoad` compiles and is exactly the timing that was wrong, so a pin both spellings satisfy would have passed on the bug. Mutation-verified red on each half. | - |
| **X-10** | A **real rendered client** loading `Dustbowl` has the same race, and nothing declares its role | **CLOSED** (Phase 6 task 6.7, 2026-08-26) — **mechanism shipped; the default is deliberately unchanged** | **A shipped process can now declare its role before any scene loads.** `Assets/Scripts/Net/Shared/NetRoleBootstrap.cs:49` runs at `RuntimeInitializeLoadType.BeforeSceneLoad` — ahead of every scene `Awake`, which is the timing that matters — and resolves through `Ironfront.Net.Configuration.NetRoleDeclaration`, a pure function under 13 tests (`NetRoleDeclarationTests`). Signals, in order: `IRONFRONT_ROLE` (registered in `EnvRegistry`, `.env.example` regenerated), then `-ironfront-role=client|server` on the command line, then `UNITY_SERVER` or `Application.isBatchMode` → Server. **Explicit beats inferred**, and **an unrecognised value is `Undeclared`, never a guess** — `IRONFRONT_ROLE=sever` resolving to Server would honour a typo and resolving to Client would invert one; Undeclared is the only outcome a reader can predict. **The no-declaration default is NOT changed, and that is the load-bearing decision.** A rendered process with nothing set still resolves to `Undeclared`, so the bootstraps decide exactly as they always have — which is what keeps offline single-player and the Editor sandbox working (`NetServerBootstrap.Awake`'s own remark). Returning Client there is the obvious "fix" and would stop the server claiming the role, so nothing would simulate; `ARenderedProcessWithNoDeclarationIsLeftToTheBootstraps` pins that it does not. **Whether a rendered process SHOULD default to Client is the client-only-mode product decision and it stays open** — this supplies the mechanism it needs, not the answer. **The silent half is closed regardless:** `NetRoleBootstrap.cs:81` warns once, at startup, when a rendered process is undeclared, naming what goes dead — `NetClientPresenterGuard.cs:48` is `IsPresentable => NetContext.IsClient`, and every presenter behind it latches `enabled = false` in that same `Awake` pass and never re-checks. **Ordering against lane B is a non-issue and the remark says why:** both run at `BeforeSceneLoad` in arbitrary order; lane B first → this returns on `Role != Offline`; this first → lane B's unconditional `SetRole` overwrites it. Either way lane B wins and nothing has read the role in between. `IRONFRONT_ROLE` is deliberately NOT `IRONFRONT_LANEB_ROLE` — that one also installs the harness, strips a bootstrap and writes artifacts, so collapsing the names would let a player build drag scaffolding in. *Original finding:* `SetRole` had four call sites and a menu → `Dustbowl` transition reached only the two bootstraps. | closed (default still open) |
| **X-11** | `ClientCombatState.EquipWeapon` had **zero production callers**, so the local player could never learn its own weapon | **CLOSED** (2026-08-21) — **and the row's own prediction came true first** | The row said this was "not the cause of `combat-roster01`'s `weaponId: 0` (that is **X-12**) **but it will be the cause of the next one if X-12 is fixed alone**". X-12 was fixed, and the next one was measured on `artifacts/lane-b/respawn-02`: `weaponId 0`, `ammo 0/0` at every checkpoint, eight seconds of point-blank fire, zero damage. It had **two** halves, not one. **Server half** (not this row, but in the way of it): `ServerCombatBridge.PlaceAtSpawn` teleported the body without `Actor.SpawnAt`, so `SpawnLoadoutWeapons` never ran — its own remarks said so and declined to fix it, naming the two things it needed. Both now exist: `Actor.EquipLoadout()` (`SpawnLoadoutWeapons` and nothing else) behind a new `IGameplayActorSource.EquipLoadout` seam. **Deliberately not `SpawnAt`, and no `controller.EnableInput()`** — a networked body is driven by `MoveInput` from the server, so local input on a headless process is a second path nobody wants. **Client half** (this row): `NetClientLocalCombatDriver` subscribes to `OnSpawnActor` and calls `EquipWeapon(message.WeaponId)` for the local player only. **Green:** `artifacts/lane-b/loadout-01`, `weaponId 1`, `clipSize 30`, and the clip visibly draining `30/30 → 0/30 → 30/30` as the server fires and reloads. Report: `reports/2026-08-21-loadout-fixed-hits-not.txt`. | closed |
| **X-12** | **A join is a spawn, and `OnClientConnected` never treated it as one** | **CLOSED server-side** (2026-08-21, `5b19e38`) — the client half is **X-13** | The join set `Health` and `IsAlive`, then `WeaponId` / `ResetWeapon` / `AmmoInClip`: the respawn's five statements in the respawn's order, with the respawn's `MoveToSpawnPoint` missing from the middle. So the claimed body stayed where `Instantiate` left it. `combat-roster01`: `x=0, z=0`, `y` descending 996.73 -> 967.44 across all seven checkpoints, **zero** position corrections — the server agreed the body was at the origin. **And `IsAlive = true` was actively disqualifying:** `ActorManager.SpawnWave` is the only code in the project that ever calls `Actor.SpawnAt`, it selects on `actor.dead`, and `NetServerActor.IsAlive` is a pass-through to that flag — so the join cleared the one bit that would have let a wave place the body, and nothing else ever would. **Fix:** the shared sequence is now `ServerCombatBridge.PlaceAtSpawn`, called by both the join and `TryRespawn`, which deletes the half-duplicated copy that let the two drift. **Confirmed:** `combat-spawn02/server.log:326` `actor 41 (team 0) placed at spawn point 4 of 6 (1208.61, 31.86, 1770.10)`, `:419` actor 42 likewise. Both silent `return`s in `MoveToSpawnPoint` now warn and a success logs its coordinates — added because after the join was taught to call `PlaceAtSpawn` the body still did not move and nothing said which branch had fired. | - |
| **X-13** | **The client computed every correction and threw it away** | **CLOSED** (2026-08-21) — **and this row's own diagnosis was wrong twice** | **Measured, not reasoned:** `artifacts/lane-b/x13-measure-01/driver-checkpoints.jsonl`, seven checkpoints, `inSnapshot: true` at every one, `authoritative (1603.12, 42.28, 1437.68)` at every one, `corrections` climbing 88 → **901**, and the rendered body at `x=0, z=0` free-falling `y` 996.63 → 970.76. **(a) The delivery question was not the question.** This row asked "whether `OnSpawnActor` applies that position to the LOCAL actor"; the snapshot path already carried it, decoded, every tick. **(b) The evidence cited was vehicle-scoped.** `correctionSnaps`, `correctionBlends` and `lastPositionErrorM` come from `ClientVehicleStage.DrivenStats` and read zero on any on-foot programme whatever the actor is doing — the same misattribution `525f68b` fixed five fields over. The actor reconciler was never silent; it was ignored. **Root cause:** `NetMovementAgent.ApplyAuthoritativeState` sets the `MoveState` struct and never the transform, which its own remarks say is correct **for the server**, where `InputAuthority.ApplyPendingInput` has already driven the `CharacterController` — "harmless for position, which the CharacterController already moved". `ClientPredictionStage.OnSnapshotApplied` called it with the opposite precondition. **Fix:** `ApplyCorrectedState(in MoveState, bool hardSnap)` — teleport on `Resynchronised`, `CharacterMove` through collision on `Corrected`, velocity never reset. **Green:** `artifacts/lane-b/x13-fixed-01`, body within centimetres of authoritative at all seven checkpoints, corrections 901 → 369. Report: `reports/2026-08-21-x13-root-cause-and-fix.txt`. | closed |
| **X-7** | `Allocate` sorts servers by `Dictionary` order, not `AverageTickMs` | **CLOSED** (Phase 6 task 6.4, 2026-08-26) | **The ordering signal now exists.** `GameServerRegistry.cs:117` delegates to `IsBetterHostThan` (`:124`), which orders by `AverageTickMs` ascending (`:126`) and breaks ties on `ServerId` ascending (`:129`). **Why the old form decided nothing:** it read `server.CpuPercent < best.CpuPercent`, and every real game server sends `cpuPercent: -1f` (`ServerMasterReporter.cs:110`) — deliberately, per its own remark, because "a fabricated number on a matchmaking input is worse than an absent one — the master would sort servers by it." With every candidate at -1 the comparison is false for all of them, `best` never moved off the first entry iterated, and allocation was decided by `Dictionary` layout. The same remark names the replacement: "Tick time is the real load signal and is measured, so that is what is sent." **`CurrentPlayers` is deliberately NOT a tie-break** — every candidate has already passed `AssignedRoomId != 0`, so none is hosting a room and the field is ~0 across the whole candidate set; sorting on a column that cannot vary would read as load-spreading while doing nothing. **Pinned by 3 new tests in `Phase02MatchmakingTests.cs`**, mutation-proved **3 mutations / 3 REDs** (`reports/2026-08-26-x7-mutation-proof.txt`): A — the `CpuPercent` comparison restored → 3 of 4 RED; B — the tie-break degraded to "first seen wins" → 1 of 4 RED; C — the load comparison reversed → 3 of 4 RED. **B was GREEN on the first attempt, and that is the finding worth keeping:** the original determinism test registered three servers and asserted the first, which passes under BOTH rules because `Dictionary` yields insertion order while nothing has been removed — so "ascending id" and "whatever comes out first" were indistinguishable, exactly the shape AC-5 asks to be pinned against. The rewrite makes them disagree through the mechanism that produces the divergence in production rather than a contrivance: `RemoveConnection` frees a dictionary slot, the next registration reuses it and lands at the FRONT of iteration, so iteration reads 4, 2, 3 while ascending id says 2. A tie on tick time is the COMMON case at match start (idle servers all report the same number), so the tie-break decides most real allocations. **The pre-existing `AllocationPrefersTheLeastBusyServer` grades neither rule** — its cpu and tick values agree, so it passes under both; a remark now says so rather than leaving it looking like coverage. **Scope held:** `IsHealthy`'s inert `CpuPercent < 90f` clause (-1 always passes) is E-8's decision and was not reopened. *Original finding:* `2026-08-17-round8.md:580`. | closed |

---

## 8. Roll-up

**Computed, not decremented.** Every cell below is derived from the row tables in §§ 2–7 by
`python tools/recount_debt_ledger.py`, which reads each row's own status cell. Nothing here is
carried forward from a previous roll-up.

- **Recomputed at:** `f655c06` (`develop` HEAD), **2026-08-28**, by the playable-first track. Four rows moved in one session and the direction is worth reading: **X-48** closed (the client renders the game, not the deploy menu), **X-49** closed (the AI null-ref cascade, in two layers), **X-51** filed AND closed (the build shipped without three reflection-probe shaders), and the earlier `4f583fa` recount had added **X-50** the same way. Group X goes 51 rows with open/closed 10/41; the total open count falls 24 -> 22. **Three of those four were found by RUNNING the game and reading what came back** — X-50 by deploying the server, X-51 by reading the log of the run that proved X-48, X-49's second layer by re-measuring after fixing its first. **B-8 and B-9 stay open.** X-48 and X-49 were what made their frames ungradeable, and the frames are now legible — but grading them needs a combat run a human watches end to end, and the run that proved these fixes had no deaths in it at all.
- **Previously computed at:** `b23aa28`, **2026-08-26**, by the verdict-closure audit
  ([`plans/verdict-closure/plan.md`](../verdict-closure/plan.md) § 1). Three rows moved — **X-25**,
  **X-27** and **E-11b** — and none of the three was new engineering: X-25's and X-27's fixes,
  mutants and runs were already recorded inside their own rows while the status cell still read
  `VERIFIED-OPEN`, and E-11b's deliverable shipped with asmdef-seam C4c. The previous pin was
  `6e38afe`, computed by `phase-8-hygiene.md` ([spec deleted — index](phases/README.md)).
- **Re-run after any phase that moves a row.** `--check` exits non-zero the moment this table
  disagrees with the rows, so the next drift is a failing command rather than a careful reading.

The previous roll-up drifted **twice**. Its own opening sentence warned about the first drift, and
then it drifted again in the same way — Group X eleven rows behind, Group E two behind, and the
Partial column three behind. A warning is not a mechanism; that is why the count now lives in a
script.

| Group | Open | Closed | Void | Decided | Partial | Total |
|---|---|---|---|---|---|---|
| A - authoring | - | 9 | 4 | 1 | - | 14 |
| B - two clients | 10 | 4 | 1 | - | 2 | 17 |
| C - code | 2 | 13 | 1 | - | - | 16 |
| D - unverified claims | - | 4 | - | 1 | - | 5 |
| E - ops round 8 | - | 7 | 1 | 4 | 1 | 13 |
| X - found in Phase 0 | 10 | 41 | - | - | - | 51 |
| **Total** | **22** | **78** | **7** | **6** | **3** | **116** |

Two classification calls the script makes in the open, because they are judgment rather than
arithmetic. **A reasoned `won't-do` counts as `Decided`, not `Void`** — the legend in § "How to read
a row" puts "nothing is owed" under `DECIDED` and reserves `VOID` for rows whose subject no longer
exists; E-3a is the first of these. **B-16 and B-17 count as `Closed`** on the strength of their own
leading token, even though each says it "re-opens as V9's row at 16" — the V9 row does not exist in
this ledger, and inventing an open row for it here would be the hand-adjustment this section exists
to stop.

### What the recount disagrees with

Recorded, not reconciled — this phase's AC-4 forbids editing a total into agreement.

1. **Group X was eleven rows behind.** The table carried 29; the rows hold 40. `X-30`…`X-40` were
   filed on 2026-08-25/26 by phases 3D, 3E and 4 and never reached the roll-up.
2. **Group E was two rows behind.** The table carried 11; the rows hold 13. `E-3a` (Phase 7's
   reasoned won't-do) and `E-11b` (the asmdef split) were spun off and never added.
3. **The previous roll-up contradicted itself, and the total hid it.** Its Group X row read
   `12 open / 17 closed`; the note printed directly beneath it reported the same 2026-08-25 recount
   as `13 open, 16 closed`. Both sum to 29, so the total reconciled while the split did not. Neither
   pair is right today (19 / 21). Kept here because it is the failure mode in miniature: **a total
   that adds up is not a total that is correct.**
4. **The Partial column undercounted by three.** It carried "1 partial (A-2)". The rows carry four:
   `A-2`, plus `B-8` and `B-11` (both graded `PARTIAL` on 2026-08-25) and `E-11` (`SPLIT`,
   2026-08-21). Only the oldest one had ever been counted.
5. **§ 8 and § 9 disagree on what Phase 1 closed.** The paragraph below says six rows (A-1, A-5,
   A-7, X-1, X-2, X-5); § 9 says seven, adding **A-9**. `A-9`'s own row reads
   `**CLOSED** (Phase 1, 2026-08-19)`, so the row and § 9 agree and the paragraph below is the one
   that is wrong. Left standing rather than corrected, per AC-4.
6. **Every open row but three is owned by a phase that has already finished.** Of the 38 open and
   partial rows, only `C-5` and `C-12` (excluded by P-D10) and `X-14` (a product decision, gated by
   X-8's G10) are deliberately parked. The other 35 point at:

   | `closes in` | Rows | Why it is dead |
   |---|---|---|
   | `3D` | 15 — B-1, B-2, B-4…B-9, B-13, B-14, X-28, X-29, X-31, X-37, X-38 | phase 3D completed 2026-08-25 |
   | `3E` | 6 — B-10, B-11, X-32, X-33, X-34, X-35 | phase 3E completed 2026-08-25 |
   | `phase 6` | 4 — X-21, X-24, X-26, X-30 | **phase 6 never adopted them** — none of the four is named anywhere in `phase-6-rows-no-run-closes.md` ([spec deleted — index](phases/README.md)), and the phase has completed. X-30 was *touched* by task 6.5 (surfaced by X-8's G10) but not owned for closure. |
   | `4` | 2 — B-15, D-2 | assigned to Phase 4 by § 9; **neither appears anywhere in [`2026-08-26-phase-4-measure.md`](reports/2026-08-26-phase-4-measure.md)**, and the phase has completed |
   | `1` | 1 — A-2 | phase 1 completed 2026-08-19; the `_actor` half was never re-owned |
   | `own phase` | 1 — E-11b | no such phase file exists |
   | `closed` | 2 — X-25, X-27 | the ownership cell says `closed` while the row's own status says `VERIFIED-OPEN` |
   | `—` | 3 — X-36, X-39, X-40 | no owner was ever recorded |

   This is the shape § 9 already named once — *"nothing scheduled them, which is precisely the shape
   that turned group A into ownerless debt"* — and it now covers 35 rows rather than three. **The
   track has run out of phases before it ran out of rows.** Phase 8 reports it; assigning owners is
   a planning decision and not this phase's to take.

### Finding 6 discharged 2026-08-26 — the planning decision was taken

Phase 8 named 35 orphaned rows and stopped, correctly: its AC-4 forbade it reconciling anything, and
re-owning a row is a plan's job. **That plan now exists.**
[`plans/verdict-closure/plan.md`](../verdict-closure/plan.md) opens six phases (R1–R6) and
[`plans/asmdef-seam/phases/phase-c5-autoreferenced.md`](../asmdef-seam/phases/phase-c5-autoreferenced.md)
takes the asmdef track's last step.

**Finding 6's own six categories, each answered:**

| Was pointing at | Rows | Now |
|---|---|---|
| `3D` (completed) | 15 | B-2, B-4…B-7, B-9, B-13, B-14, X-28, X-29, X-31, X-37 → **R1**; B-1, B-8 → **R4**; X-38 → **R6** |
| `3E` (completed) | 6 | B-10, B-11, X-33, X-34 → **R5**; X-32, X-35 → **R3** |
| `phase 6` (never adopted them) | 4 | X-21, X-24, X-26 → **R4**; X-30 → **R2** |
| `4` (completed) | 2 | B-15 → **R5**; D-2 → **R6** |
| `1` (completed) | 1 | A-2 → **R6** |
| `own phase` / `closed` / `—` | 7 | E-11b, X-25, X-27 **closed** on evidence already in their own rows; X-36 → **R2**; X-39, X-40 → **R3** |

**Three rows are parked rather than owned, and the parking is now written into the cell** rather
than left as a dash a later audit reads as an orphan: **X-14** (two product decisions), **C-5** and
**C-12** (excluded by **P-D10**). Each is re-affirmed in R6 § 4 — the difference between a decision
and debt is whether the reason is on the row.

**The other five disagreements above stand unreconciled**, exactly as phase 8 left them. This
subsection re-owns rows; it does not edit a number into agreement.

**Phase 2 (2026-08-19) closed ten rows** — C-2, C-3, C-4, C-6, C-7, C-8, C-10, C-11, C-16 and
X-4 — and left C-1 **prepared but not flipped**, which is what task 2e was for. Group C went from
twelve open to two, and both survivors (C-5, C-12) are excluded by P-D10 rather than unfinished.
Two rows carried decisions the earlier phases had deferred rather than taken: **a wreck damages**
(C-10, V1-D5 → V4 → here) and **`ExplosionKind.Environment` gets a source** (C-11, V1 → V7 → here).
Two new `ClientWiringGate` rules ship with it — **G6**, which grades every `ServerEventWriter`
writer for a production caller and closes the server-side blind spot that let `WritePlayerList` sit
uncalled for four phases, and **G7**, which fails the build if an engine projectile damage site
loses its cutover guard. Both were **observed RED before the fixes**
([`reports/2026-08-19-phase-2-red-proof.txt`](reports/2026-08-19-phase-2-red-proof.txt)) and both
have red-path fixtures in `WriterCoverageGateTests`.

**Phase 1 (2026-08-19) closed six rows** — A-1, A-5, A-7, X-1, X-2, X-5 — and took A-2 down to its
`_actor` half. Each carries a named detector in `tools/ClientWiringGate` that was **observed RED
against the pre-authoring tree** before it was allowed to ship
([`reports/2026-08-19-phase-1-red-proof.txt`](reports/2026-08-19-phase-1-red-proof.txt)), which is
the author-then-pin rule (P-D5) doing the thing it was written for: these rows cannot silently
re-open.

Fourteen rows were already closed and seven were void — twenty-one items, three of which the design
of record had named as prime suspects and eighteen of which it had not. That is the outcome P-D1
was bought for.

---

## 9. Handoff

**Phase 1 — done 2026-08-19.** Closed **A-1, A-5, A-7, A-9, X-1, X-2, X-5**; **A-2**
partially. Task 1.6 landed after **C-4** as its acceptance criterion required, verifiable from git
history: **C-4** in #145, **A-9** in the commit that added
`AssetWiringDetectors.ScoreUiTextRefsAreAssigned` alongside the authoring, RED-proven first. **A-6**
is the one row Phase 1 leaves open, and no longer for an authoring reason — its YAML half closed
with **A-9**. What remains is a dedicated human-count element (a `.cs` change, Phase 2's kind of
work) and E5's runtime "timer hidden during `Playing`" clause (Phase 3). **A-4**'s opportunistic `WeaponIds` → prefab
confirmation was **not** performed and remains an open opportunity, not a blocker. The
Task 0.2 verdict held: **A3 void, A4 already closed**, neither gating anything. **A-14 was not
authored** — `SpecChecker` re-run at exit 0 confirms the exemption still holds in both directions.

**To Phase 2** — group C rows still open: **C-1** (prep only), **C-2, C-3, C-4, C-6, C-7, C-8,
C-10, C-11, C-16**, plus **D-1**, **E-6**, **E-9**, **X-3**, **X-4**, **X-6**. Three scope changes:
C-3 is smaller than planned (sender only), C-16 is half its stated size, and **X-3 is new and blocks
Phase 3**. The ordering constraint holds — **C-4 lands before A-6/A-9**.

**To Phase 3** — group B rows **B-1 … B-9, B-11, B-13, B-14**, all blocked on the same missing
two-process harness, and all blocked behind **X-3** for any check involving firing, respawning or
baseline acks. **They do not all go to the same lane, and this line used to read as though they
did:** [`phase-3d-lane-b.md`](phases/phase-3d-lane-b.md) § 2 owns **B-1…B-9, B-13, B-14** as lane B,
and `phase-3e-run-and-ledger.md` ([spec deleted — index](phases/README.md)) § 3 owns **B-11** as lane A —
it is "a headless server survives drive → damage → burn → death", which needs no rendered client at
all. Recorded here on 2026-08-21 because reading this paragraph alone suggested B-11 was a lane-B
row that lane B had dropped, and it never was.

**To Phase 4** — **B-15, B-16, B-17** (measurements), and **D-2**'s decision on the five
genuinely-unwritten tests. Note that B-16 and B-17 both have a first measurement on record already;
what is owed is the number under criterion 8/9 conditions, not a first number.

**B-10 left this paragraph on 2026-08-25.** It read as Phase 4's with the other three measurements, and it is not: [`phase-3-harness.md`](phases/phase-3-harness.md) § 2 lists check 10 as lane **A** inside phase 3's scope lock, and `phase-3e-run-and-ledger.md` ([spec deleted — index](phases/README.md)) § 3 claims it while naming B-15/B-16/B-17 as Phase 4's — so two of the three documents agreed and this line was the odd one out. Phase 3E ran lane A, found that **no lane can measure allocation at all** (**X-33**), and moved the row there. **B-15, B-16 and B-17 are untouched by phase 3E** and remain Phase 4's, per that phase file's § 3.

**To Phase 5** — **C-1**, holding **X-6**'s pin and Phase 4's numbers. **Discharged 2026-08-26:** X-6 landed first as `ClientWiringGate` **G8** (its own commit, per the hard ordering constraint), then C-1 was decided **OFF**. Both rows are closed above; `reports/2026-08-26-phase-5-cutover-gate.md` carries the workings.

**Re-owned 2026-08-23 — eleven rows whose `closes in` column pointed at a phase that had passed, at a
track with no owner, or at nothing at all.** Three of them (**X-10**, **X-14**, **X-16**) read `-` or
`own decision`: nothing scheduled them, which is precisely the shape that turned group A into
ownerless debt. They now point at `phase-6-rows-no-run-closes.md` ([spec deleted — index](phases/README.md))
(D-1, E-6, X-6, X-7, X-8, A-6, X-10, X-14, X-16), `phase-3f-x19-drawn-vs-held.md` ([spec deleted — index](phases/README.md))
(X-19) and `phase-7-ops-to-digest.md` ([spec deleted — index](phases/README.md)) (E-3). **X-7** moved off
"master-server track" for a stated reason: that track has no active owner, and a live matchmaking
defect parked against an inactive track is parked, not tracked.

**To the verdict-closure track — 2026-08-26, and this is the ledger's live handoff.** The
debt-closure track is closed; twenty-eight open rows moved to
[`plans/verdict-closure/plan.md`](../verdict-closure/plan.md) and one step to
[`asmdef-seam C5`](../asmdef-seam/phases/phase-c5-autoreferenced.md). Read the `closes in` column,
not this paragraph — it is the only place that is checked. Three parkings (**X-14**, **C-5**,
**C-12**) say so in their own cells rather than showing a dash a later audit reads as an orphan, and
**V-D1** binds the successor track: this file is not forked.

**Outside the five phases** — **E-3** (**closed 2026-08-26** on a digest; its old reason,
*blocked on Actions billing*, had died with the 2026-08-21 transfer, and its original
*"no image exists"* evidence turned out to have queried the wrong account — both kept visible on
the row. Spun off **E-3a**, a reasoned `won't-do`), **E-11b** (the asmdef split,
its own phase; **E-11**'s layering half closed on 2026-08-21 by a gate rather than an asmdef),
**X-7** (master-server track), **C-5** and **C-12** (out of scope by P-D10).

---

## 10. What this ledger does not claim

- **It did not open the Unity Editor.** Every prefab and scene fact above was read from force-text
  YAML. Where YAML could not settle a question — E5's runtime "timer hidden during `Playing`"
  clause (**A-6**), the `WeaponIds` → prefab map (**A-4**), and whether an Animator state `speed`
  multiplier rescales the throw clip (**D-1**) — the row stays open and says so.
- **It did not run the Unity test suite or a Play Mode session.** `dotnet test` was not run either;
  the two CI gates in § 1 were, and their exit codes are recorded.
- **It does not grade the quality of what it found closed.** A row marked `ALREADY-CLOSED` says the
  fix is present at the cited line, not that it is well designed or covered by a test. **X-6** is
  the one case where that distinction was material enough to record.
